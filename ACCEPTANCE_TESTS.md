# orderapp 验收清单与测试用例（截至 2026-02-13）

> 目标：给日常验收/回归测试用。每次上线前按此清单抽测。

## 0. 全局列表分页
- [ ] 订单列表、客户档案、操作日志、需求管理、出库日志、分配批次和客户履约订单等服务端分页列表，都显示总页数、当前页、跳转页和每页显示条数。
- [ ] 修改筛选条件或每页显示条数后，列表从第一页重新查询；翻页后仍保留当前筛选、客户范围或订单范围。
- [ ] 其他一次性加载的 Vue/Vite 表格列表由统一自动分页增强补齐同样的分页控件，不再各页面重复实现自定义 pager。

## 0.0 生产管理高频视图二期
- [x] PR-499-PRODUCTION-EXECUTION-HUB-PHASE2：`GET /api/produce/work-orders/:id` 返回 `execution_hub`，包含工单头、BOM/路线、工序进度、WIP、质检、Stock Entry、完工入库、成本和 `trace_timeline`；readiness 返回 `can_start`、`can_complete`、`blocking_reasons`、`next_handler`、`suggested_action`、`severity` 和 `related_links`。
- [x] PR-499-PRODUCTION-EXECUTION-HUB-PHASE2：生产视图、工位视图、生产工单和工序卡都能打开同一个工单执行枢纽，并能回答整体进度、卡点、下一处理人、当前工位做什么、下一件做什么和为什么不能做。
- [x] PR-499-PRODUCTION-EXECUTION-HUB-PHASE2：枢纽跳转库存作业、质检、成本和日志时保留 `work_order_id`、`job_card_id`、`running_item_id`、`material_id`、`shortage_g`、`batch_id` 等上下文，且不改变库存、WIP、质检冻结或完工入库核心规则。
- [x] PR-499-PRODUCTION-EXECUTION-HUB-PHASE2：工位负载展示队列数、阻塞数、预计分钟和负载状态；执行枢纽 timeline 支持全部、工序、库存、质检、成本、日志过滤；旧生产页面仍通过顶部生产切换条可达。

## 0.1 工作台模式
- [ ] 内部账号顶部可在“工厂总览”和“客户账户”之间切换；工厂总览保留内部菜单但不再展示“履约运营台”，客户账户只展示客户账户、客户商品与配方、客户财务三组入口，不展示门户与能力、生产计划/开始生产和工作台模式手册。
- [ ] 客户账户模式选择当前客户后，刷新页面仍保留 `workspace=customer&customer_id=...`。
- [ ] 客户账户模式进入 SKU设置、产品价格表、BOM配方维护、库存、费用管理和客户履约运营台，不需要在每个页面重复选择客户。
- [ ] SKU设置、BOM配方维护和产品价格表在客户账户模式下锁定顶部当前客户；页面内不再出现客户切换器，只能看到公共配置和当前客户自己的配置。
- [ ] 客户账户库存只展示当前客户相关成品库存，不展示工厂原料库存、跨客户追溯工具或其他客户库存。
- [ ] 客户账户模式进入录单时，客户默认选中当前客户；订单列表按当前客户过滤；费用管理新增费用默认关联当前客户且列表按当前客户过滤，不再要求页面内选择客户。
- [ ] 客户账户模式菜单不显示生产计划/开始生产、生产中、生产工单、生产质检、生产成本、生产手册或工作台模式手册；需要排产和开工时切回工厂总览。
- [ ] 渠道客户账号登录时不显示“工厂总览 / 客户账户”和“当前客户”选择器，左侧只显示“工作台”和“费用相关”两个菜单。
- [ ] 渠道客户账号的“工作台”只展示自己绑定客户的履约运营台和履约订单，不再内嵌费用明细或结算单。
- [ ] 渠道客户账号的“费用相关”包含费用明细、经营报告和结账相关；三个页面都自动按账号绑定客户过滤，不出现客户选择器。
- [ ] 渠道客户账号查看费用明细、经营报告和结账相关时，只看到当前客户订单收入与当前客户费用；费用明细和结账相关为只读展示，不允许客户侧新增/归集费用、执行结账、结账后调整或会计交接导出。
- [ ] 渠道客户账号直接请求客户费用/报表读接口时，后端按当前登录员工绑定客户派生客户范围；请求其他 `customer_id` 必须被拒绝。
- [ ] 权限仍按用户权限判断；没有权限的页面不会因为切到客户账户模式而出现。
- [ ] PR-315-SHELL-MENU-SCROLL-TOUCH：左侧菜单固定在单屏视口内独立滚动；点击任何功能菜单后功能页面回到顶部；手机端右滑呼出功能菜单，左滑隐藏功能菜单。
- [ ] PR-316-FORM-DRAFT-CACHE：录单、BOM配方维护、SKU设置 跳转到其他功能再回来时保留未提交表单，刷新浏览器后草稿清空。
- [ ] PR-387-VIEW-CONTEXT：顶部显示“当前视图”，支持工厂总览、客户、订单和外部客户固定视图；旧 `workspace=customer&customer_id=...` URL 仍能恢复客户视图。
- [ ] PR-387-VIEW-CONTEXT：客户视图进入商品管理默认显示客户商品名，产品价格表锁定当前客户并只显示该客户客户商品名；BOM 仍显示为商品档案的生产 BOM。
- [ ] PR-387-VIEW-CONTEXT：订单视图进入订单列表只显示该订单，录单/履约默认使用订单客户；生产计划和工单仍是工厂执行入口，只带客户/订单过滤和追溯标签。
- [ ] PR-387-VIEW-CONTEXT：保存当前视图、修改保存视图、停用保存视图都写操作日志；外部客户账号请求其他客户或其他客户订单上下文返回 403。
- [ ] PR-318-CUSTOMER-BEAN-LIST-PUBLIC-CATEGORY-SCOPE：芬纳咖啡等客户在 SKU设置 中关闭“是否使用公共商品分类”后，进入产品价格表客户范围时只展示客户自己的 SKU；公共分类、公共 SKU 和公共豆单内容不应出现在客户豆单预览或生成候选中。开启该开关后，公共分类下公共 SKU 才能作为只读引用进入客户豆单范围。
- [ ] PR-319-CUSTOMER-BEAN-LIST-SKU-CATEGORY-METADATA：芬纳咖啡在 SKU设置 新增“芬纳定制-红酒日晒-中深烘”这类客户自有/客户定制熟豆并挂到“咖啡豆 / 定制咖啡熟豆”后，产品价格表客户范围的商用批发豆单必须展示该 SKU，标题按公共豆单逻辑显示为实际豆单分类，例如“1、定制咖啡熟豆”，不再显示成旧公共豆单“源产地精选”或因缺少旧 Excel 豆单资料而消失。
- [ ] PR-339-CUSTOMER-BEAN-LIST-UNCATEGORIZED-SKU：岩师傅“曲奇拼配2.0”这类客户 SKU 未挂分类时，产品价格表客户范围显示在“未分类”，不显示“精品意式拼配”；挂入客户分类后才按客户分类标题展示。
- [ ] PR-321-CUSTOMER-SKU-CATEGORY-TOGGLE-FACTORY-BEANLIST-TITLE：芬纳咖啡在 SKU设置 勾选再取消“是否使用公共商品分类”后，客户自有产品类型“咖啡豆”和产品子类型“定制咖啡熟豆”仍保留且仍挂载 `芬纳定制-红酒日晒-中深烘`；点击“工厂总览”后 SKU设置 的 SKU归属回到公共SKU；客户产品价格表商用批发豆单显示 `1、定制咖啡熟豆`，并包含该 SKU。
- [ ] PR-322-CUSTOMER-SKU-CATEGORY-CLEANUP-BOM-VERSION-DEACTIVATE：芬纳咖啡保存“是否使用公共商品分类”开关后，客户自有产品类型只要仍有活跃产品子类型就不能被清理隐藏；在 SKU设置 失效一个有 active BOM 版本的 SKU 后，当前 BOM 显示已失效，该 SKU 的 active BOM 版本同步变为 disabled，历史版本和明细仍可追溯。
- [ ] PR-375-PROCESS-BOM-WORKORDER-SKU-MODEL：BOM 可维护预期损耗率并显示预期产出率；BOM API 同时返回 `expected_loss_rate`、`expected_yield_rate` 和兼容 `yield_rate`；工序卡可记录实际投入、实际产出、实际损耗、实际损耗率和异常原因；工单展示冻结的预期损耗、计划投料、预计产出和工序实际损耗汇总；SKU/BOM/成本/生产页面不再只表达咖啡“出品率”。

## A. 录单（/app/order）

### A1. 客户选择与联动
- [ ] 客户下拉仅显示 active=true 的客户（停用客户不出现）。
- [ ] 客户选择框支持客户名、拼音、首字母模糊搜索。
- [ ] 选择客户后：订单信息只读展示该客户资料中的客户类型、来源和订单类型，不再提供录单页手工修改控件。
- [ ] PR-327-CUSTOMER-RESPONSIBLE-EMPLOYEE：选择客户后录单页只显示客户资料中的负责人，不再显示可编辑订单负责人选择器；保存订单时负责人快照为该客户负责人。
- [ ] 新录订单默认付款状态为已付款、发货状态为未发货。
- [ ] 从客户档案、订单列表或其他页面带筛选/编辑参数时，点击左侧“录单”会清除 `edit_id` 并进入新建订单；保存后创建新订单，不会更新历史订单。
- [ ] 录单页点击“新增客户”从右侧打开抽屉，不离开当前订单；粘贴“姓名/电话/地址”后可自动识别并填充，默认来源为微信、订单类型为批发，必须选择客户负责人，保存后自动选中新客户。
- [ ] PR-345-ORDER-CUSTOMER-PROFILE-BEANLIST-DRAWER：客户资料新增或编辑时，客户类型、来源和订单类型都是必填项；缺少任一项时客户资料保存失败。选择客户后，录单/编辑订单显示这三个值但不能在订单信息区修改。
- [ ] PR-327-ORDER-ENTRY-CUSTOMER-DROPDOWN-LAYER：手机窄屏录单输入客户时，客户候选列表显示在客户负责人只读字段及后续字段上方；选择客户或离开客户输入后候选列表关闭，不遮挡后续字段操作。
- [x] PR-348-ORDER-BACKFILL-CONTINUOUS-MODE：录单页“订单补录”区域必须有可点击开关；关闭开关时可只改订单日期并点“保存订单”补录单张订单，打开开关时页面显示“保存并继续补录”和“保存并查看订单”两个动作。

- [ ] PR-340-ORDER-ENTRY-PRODUCT-DROPDOWN-LAYER：录单已有多条商品明细时，点击任意一行“商品”输入框，商品下拉必须显示在规格输入、后续商品行和价格梯度按钮上方，不被后续商品行的商品选择框遮挡。

### A2. 明细输入（规格 + 件数）
- [ ] 选择商品后：规格默认该商品第一个价格梯度的规格。
- [ ] 规格下拉包含 36g、80g、100g、227g、454g、500g、1000g、2.5kg。
- [ ] 数量(件)只能按整数处理（输入小数时，系统行为符合预期：建议前端拦截/或后端拒绝；以实际上线行为为准）。
- [ ] 总重量实时显示：xxx g / yy lb。
- [ ] 每条商品明细可以填写“条目备注”，保存后重新编辑订单时在原商品行回显。
- [ ] 保存 ERP 订单、履约客户订单或小程序订单后，订单明细商品行显示该行使用的豆单版本号；ERP 录单按熟豆、生豆、挂耳分别选择豆单版本，接口返回 `bean_list_publication_id` 和 `bean_list_version_no`，三处订单展示读取同一 `order_items` 记录。
- [ ] PR-345-ORDER-CUSTOMER-PROFILE-BEANLIST-DRAWER：订单信息区不显示熟豆豆单、生豆豆单或挂耳豆单选择器；商品明细标题区点击“选择豆单”打开抽屉，抽屉内可分别选择熟豆、生豆、挂耳已发布豆单。客户没有自定义豆单时，对应类型默认显示并使用公共豆单。
- [ ] PR-352-INSTANT-COFFEE-PRODUCT-KIND：在 SKU设置 创建 `instant_coffee` 速溶咖啡 SKU 后，录单商品下拉能看到“速溶咖啡”形态；保存订单时订单行保留该形态，生产计划缺 BOM 物料显示“速溶咖啡”原料而不是“产品名 生豆”。

### A3. 自动匹配阶梯价
- 用例：选择一个有阶梯价的商品。
  - [ ] 页面展示该商品所有规格/数量梯度和单价。
  - [ ] 规格 454g、数量 1：能算出单价/小计。
  - [ ] 规格 454g、数量 10：匹配到更高档（若存在）。
  - [ ] 商品只有 454g 豆单梯度时，规格 1000g、数量 30 袋：按 30000g ÷ 454 的折算磅数匹配 49lb+ 档；若该档为 48 元/磅，单价显示为 106 元/kg，小计为 3180。
  - [ ] PR-325-ORDER-ENTRY-MATCHED-TIER-HIGHLIGHT：芬纳咖啡选择 `芬纳定制-红酒日晒-中深烘`，规格改为 1000g、数量 20，自动价显示 117 元/kg 且行 `tier_id` 对应 `梯度 58` 时，价格提示中的 `454g 24-47件 53/磅` 按命中状态高亮。
  - [ ] PR-339-ORDER-ENTRY-KG-TIER-UNIT-VERSION-WARNING（价格范围行为由 PR-537 更新）：岩师傅选择 25–49kg、82 元/kg 的 KG 阶梯商品，规格改为 80g、数量 313（总重 25.04kg），录单/编辑行单价标签显示 `元/kg`，小计处显示 `梯度 82/kg` 而不是梯度 ID，豆单版本显示当前版本号而不是“未记录”，保存后 `order_items.unit_price=82`、`line_total=2053.28` 且价格来源保留 `price_unit=kg`。再将数量改为 1 时不命中该档，自动单价为空且不能保存。
  - [ ] PR-341-ORDER-ENTRY-STALE-BEANLIST-AUTOLINE：选择客户的旧版熟豆/生豆/挂耳豆单并录入商品后，商品行豆单版本标红并显示感叹号；桌面鼠标悬停或手机点击感叹号显示“非新版本豆单”。选择商品后列表底部自动补一个空明细；已有空明细时修改已选商品不会继续增加第二个空明细，“新增明细”按钮位于明细列表下方。
  - [ ] PR-342-ORDER-ENTRY-PUBLIC-BEANLIST-VERSION-SELECT：选择一个没有专属熟豆/生豆/挂耳豆单的客户时，对应豆单下拉仍列出公共豆单最新和历史发布版本，并默认最新；改选公共历史版本后选择商品，价格梯度按钮、行豆单版本、小计和保存后的 `order_items.bean_list_publication_id` 均来自该历史公共豆单版本。
  - [ ] PR-350-BEAN-LIST-PUBLISH-MANUAL-WITHDRAW：发布新版豆单后旧版仍保持已发布，只有点击版本列表的撤回操作才变为已撤回；已撤回豆单不出现在录单豆单选择器，接口显式提交已撤回豆单 ID 时保存失败。
  - [ ] PR-351-ORDER-ENTRY-PUBLIC-BEANLIST-NO-CUSTOMER：打开新建录单页且尚未选择客户时，商品明细标题区显示公共熟豆/生豆/挂耳豆单已发布版本；公共熟豆存在多个已发布版本时可点“选择豆单”切换版本，不显示“暂无”。
  - [ ] PR-352-ORDER-LIST-UNIFIED-SELECTION：订单列表每行只有一个复选框；勾选同一批订单后，批量失效、顺丰发货 Excel、组合销售单和组合出库单都读取这一批选择，不再出现单独的失效复选框或组合单据复选框。
  - [ ] PR-326-SALES-ORDER-LAYOUT-DISCOUNT-NOTE：含长商品名、行级优惠、整单优惠、运费和销售单备注的订单打开销售单预览时，商品名在商品列内换行不遮挡后续列，保存的销售单备注进入对外销售单；结算区最终排版按 PR-329 验收。
  - [ ] PR-329-SALES-ORDER-SETTLEMENT-SUMMARY-LAYOUT：含行级优惠和订单备注的销售单预览中，商品行“单价”为原价；订单备注显示在结算区倒数第二行，最后一行同排显示商品合计、优惠合计、运费和应收。
  - [ ] PR-330-SALES-ORDER-DISCOUNT-LINE-LAYOUT：销售单预览中规格显示为 `1000g/件`；有行级优惠时商品行显示 `￥-28元` 这类“优惠折扣”，每行显示优惠后的“总价”且备注在最后列；无优惠订单不显示“优惠折扣”列，也不显示“优惠合计”。
  - [ ] 曲奇拼配等 KG 专属梯度商品，规格 2.5kg、数量 10 袋时按 25kg 匹配对应梯度，不按 10 件匹配。

### A4. 无已发布价格档（阻断自动价）
- 用例：分别让行数量低于起订量、高于有限最高量和落在两个档位的空隙。
  - [ ] 页面出现“当前数量无已发布价格，不能保存”。
  - [ ] 自动单价和阶梯命中保持为空，不回退最低档、末档或相邻档。
  - [ ] 保存按钮禁用，直接调用保存流程也会被同一行错误阻断。
  - [ ] 调整数量到合法档位、补齐并重新发布价格档，或按已有授权流程输入大于 0 的手动单价后才可保存。

### A5. 手动单价覆盖
- 用例：对同一行选择“手动单价”，按当前规格显示单位输入单价；例如 1000g 行输入元/kg。
  - [ ] 小计按手动单价重新计算。
  - [ ] 保存后订单金额与页面一致。
  - [ ] 点击恢复自动价后，按当前商品/规格/数量重新匹配梯度价。

### A5.1 商品单价优惠
- [ ] PR-310-ORDER-ENTRY-UNIT-AMOUNT-DISCOUNT：录单商品行支持单价优惠。
- 用例：商品行优惠选择“单价优惠”。
  - [ ] 454g 批发商品按元/磅计价时，输入单价优惠后按该行总磅数扣减，页面小计立即变化。
  - [ ] 1000g 或 2.5kg 批发商品按元/kg 计价时，输入单价优惠后按该行总 kg 扣减。
  - [ ] 零售商品按件数扣减，挂耳商品按袋或盒数量扣减。
  - [ ] 保存后 `order_items.discount_type=unit_amount`，行优惠、订单优惠和应收金额与页面小计一致。

### A6. 保存
- [ ] 保存后跳转/提示“已保存订单号”。
- [ ] 收款状态为已付款、已收款或已支付时，录单/编辑必须选择收款方式；未选择时前端提示“请选择收款方式”或接口返回 `payment_method required`，选择后保存成功。
- [ ] PR-332-ORDER-LOGISTICS-RECEIPT-REQUIRED：录单/编辑订单中发货状态为已发货时，缺少物流公司或物流产品必须拒绝保存；收款状态为已收款时，缺少货款金额、运费金额或收款凭证必须拒绝保存；`/api/order/form` 返回物流公司和产品列表，物流设置页可维护启用/停用状态。
- [ ] PR-334-ORDER-ENTRY-GLOBAL-ERROR-TOAST：滚动到录单页底部后点击“保存订单”，如果缺少收款凭证、物流公司、物流产品、收款方式或商品明细，错误提示在当前视口内以全局弹出提示展示，能直接看到并可关闭。
- [ ] PR-335-ORDER-ENTRY-MOBILE-LAYOUT：用手机宽度打开录单或编辑订单，发货物流和收款凭证区域为单列，输入框、下拉框、收款凭证上传控件和全局错误提示不被左右边缘裁切，页面不需要横向滚动。
- [ ] PR-336-MOBILE-NOTICE-STACK：手机端同时存在新订单顶部通知和录单红色保存错误时，两类提示上下分开不重合；多条新订单通知最多 3 条以层叠卡片展示，点击或关闭其中一条只处理当前通知。
- [ ] PR-337-ORDER-ENTRY-CUSTOMER-EDIT-VALIDATION：录单选择客户后，“新增客户”右侧显示“编辑客户”；点击后打开客户资料抽屉并可保存客户资料。保存订单时缺少客户、收款方式、物流、收款凭证或商品明细时，全局弹窗展示错误，同时页面滚动到对应字段并标红；修正该字段后标红自动取消。
- [ ] PR-338-CUSTOMER-SELECTOR-CLEAR-DROPDOWN-SPACING：客户账户顶部“当前客户”选择器已有客户值时，清除选择按钮和展开下拉按钮不重叠，分别可点击；点击清除后当前客户变为空，可继续输入搜索或点下拉选择客户。
- [ ] PR-343-ORDER-NOTICE-DEDUP-PAYMENT-HINT：创建同一订单后，ERP 顶部只显示一条“新订单”通知，不因同一用户同时具备订单读取权限和生产角色而展示重复通知；录单/编辑订单需要填写收款凭证时，货款金额栏弹出“实时价格提示 货款”作为货款提示，运费金额栏弹出“实时价格提示 运费”作为运费提示，点击提示自动填入当前商品合计或物流费用，填入后仍可手动修改。
- [ ] PR-344-ORDER-ENTRY-CUSTOMER-COMMON-PRODUCT-SORT：选择有历史订单的客户后，商品下拉默认把该客户常用商品排在前面；常用商品按历史有效订单次数优先、明细次数和最近订单时间辅助排序；搜索、客户 SKU 范围和豆单范围过滤仍正常生效。
- [ ] PR-345-ORDER-CUSTOMER-PROFILE-BEANLIST-DRAWER：客户资料缺少客户类型、来源或订单类型时，录单/编辑订单保存失败并提示先维护客户资料；客户资料补齐后保存订单时，后端使用客户资料中的来源和订单类型，不接受前端隐藏字段绕过。
- [ ] PR-346-ORDER-SALES-NOTES-VOUCHER-SKU-SORT：录单填写物流费用后，页面订单合计包含物流费用，并在合计旁显示货款金额和物流金额；上传收款凭证后凭证区默认收起，点击摘要可打开大图预览；财务经营报告来源明细可打开订单收款凭证；销售单预览、PDF 和图片按快递费备注、订单明细备注、销售单备注三类备注分别成行显示，有值才显示；SKU 设置可保存商品名称修改，客户 SKU 和 BOM 商品列表按当前客户自定义商品优先、订单常用商品靠前排序。
- [ ] 保存订单时不进行快递处理，不生成快递录单 Excel 下载入口。
- [ ] 保存带条目备注的订单后，销售单预览、销售单 PDF/图片和出库单预览/PDF 均在对应商品行展示该条目备注。

---

## B. 订单列表（/app/orders）

### B1. 查询/筛选
- [ ] 支持关键字搜索。
- [ ] 支持日期范围筛选。
- [ ] 作废筛选：默认不显示作废单，可切换查看作废/全部。
- [ ] 订单失效筛选：点击“失效”后，默认订单列表不再展示该订单；切到“已失效/全部”能看到该订单，但不能恢复，只能复制为新订单继续处理。
- [ ] 订单批量失效：点击表头“失效”总复选框后，当前页正常订单全选；再点击该复选框，当前页取消全选；点击“批量失效”后这些订单默认不再展示，已失效订单不能被再次勾选批量失效。
- [ ] PR-288-ORDER-SOFT-VOID-SHARED-BACKBONE：同一订单失效后，ERP 正常订单列表、履约客户订单、小程序订单和小程序账单默认都不展示；失效不可恢复，只能复制成新订单，不能维护多套订单框架。
- [ ] 订单列表可在“全部订单 / 我的订单 / 履约客户订单”之间切换；“我的订单”只看当前登录员工负责的订单。
- [ ] “履约客户订单”只展示已绑定启用登录的 ERP 履约账号、已开通门户/工作台且客户门户能力模板暴露 ERP 工作台的客户通过客户门户提交的履约订单；渠道客户满足能力条件后可进入，零售、电商、未绑定履约账号、历史 active 绑定指向零售商城或其他不暴露 ERP 工作台模板、或绑定账号已停用登录的客户订单不进入该范围。
- [ ] PR-381-CUSTOMER-PORTAL-TEMPLATE-BOUNDARY：客户档案和录单客户抽屉只显示“开通客户门户/工作台”开关，不出现能力模板下拉、不自动推荐模板、不因开关打开要求模板；门户客户配置页仍能绑定客户门户能力模板。已开通但未绑定有效模板的客户出现在门户配置默认列表，但不进入履约客户候选和客户工作台入口；门户客户配置保存模板、启停、显示名、寄件人等变更后，操作日志可查。
- [ ] 手动请求 `/api/orders?scope=fulfillment_typo` 返回 400 `invalid scope`，且不会退化为展示全部订单。

### B2. 列表内联编辑 + 审计
- [ ] 修改收款状态/发货状态/备注后能保存成功。
- [ ] 订单列表或订单抽屉中，已付款订单在收款状态后显示收款方式，例如“已付款 / 银行转账”。
- [ ] ERP 订单列表金额列展示商品金额、运费、优惠、应收；有快递费说明或委外合计时同步展示。
- [ ] PR-287-ORDER-LIST-FEE-KG-SUBMISSION-REPAIR：ERP 订单列表金额列与履约运营台订单费用结构一致，展示商品、运费、优惠、应收并突出应收；有快递费说明或委外合计时同步展示。
- [ ] 点击 ERP 订单号打开抽屉后，能看到收件人、电话、地址、公司和费用明细。
- [ ] 修改会写入审计日志（/app/audit 可查询到）。
- [ ] 新订单通知点击后进入订单列表并自动高亮对应订单，新订单行为绿色。
- [ ] 未生产完成订单显示黄色，未发货订单显示蓝色，未付款订单显示红色；新订单高亮优先于其他状态色。
- [ ] ERP 顶部新订单通知来自消息中心接口；关闭通知会标记已读，刷新后不重复弹出同一条已读通知。
- [ ] 通知配置页可新增/修改规则，支持事件类型、通道、接收人类型、目标键、模板键、适配器键和 Payload 条件。
- [ ] 订单下单、生产完成、已发货分别发布消息中心事件，默认规则可通知生产角色、销售角色或订单相关接收人。
- [ ] 外部 IM 规则只写入通用投递/适配器框架，不要求业务代码直接调用微信服务号接口。

### B3. 可发货订单发货处理
- [ ] 点击“只看可发货”后列表筛选到生产完成、无需生产和库存待发货订单。
- [ ] 只有生产完成、无需生产或库存待发货订单可以勾选发货处理。
- [ ] 已失效订单不能勾选发货处理；只能复制为新订单后，再按新订单的生产/发货状态判断是否可发货。
- [ ] 勾选一个或多个订单后，可生成快递录单 Excel 并展示下载入口。
- [ ] 下载的 Excel 使用服务器快递模板，收件人来自客户档案，寄件人默认来自发货人设置的默认寄件人。
- [ ] 本次生成前可以选择默认寄件人；已勾选订单中的某几单也可以单独选择寄件人，Excel 对应行使用对应寄件人资料。
- [ ] Excel 中包裹件数为 1，托寄物为茶叶，重量为 0.1kg，备注包含对应订单明细但不包含单价和小计。
- [ ] 快递录单 Excel 生成失败时必须清理刚写入的文件，不会留下公开孤儿快递录单文件；排除错误后可重新生成快递录单 Excel。
- [ ] 订单列表将寄件人和快递单号合并展示为“快递信息”，将收款、发货、生产和发票合并展示为“订单状态”。
- [ ] 点击订单号打开订单抽屉后，“快递信息”里没有“加入本次快递录单”选项；寄件人默认跟随顶部本次寄件人，并可直接选择其他寄件人。
- [ ] 点击订单号打开订单抽屉后，可以在抽屉中回填一个或多个快递单号；订单列表顶部不再显示本批次单号输入框。
- [ ] 同一订单已有快递单号时，再回填新单号不会覆盖旧单号；多个单号用换行、逗号或分号分隔后会追加去重。
- [ ] 订单列表搜索任意一个快递单号都能定位到该订单；出库单预览和小程序订单查询继续读取完整快递单号汇总。
- [ ] 点击订单号打开订单抽屉后，可以在同一抽屉编辑并保存订单；列表和抽屉不再出现独立“编辑”跳转入口，保存后停留当前筛选分页。
- [ ] 上传顺丰寄件列表回传 Excel 后，系统按备注中的订单号回填对应订单快递单号，并将发货状态更新为已发货；同一订单多行单号会归并到同一个订单。
- [ ] 物流单号 Excel 上传超过 20MB 时必须拒绝，不能解析超大物流回传 Excel；接口返回 `file too large`。

### B4. 发货人设置
- [ ] 发货人设置页面展示寄件人列表。
- [ ] 可以新增/编辑寄件人，并设置一个默认寄件人。

### B5. 销售单预览与下载
- [ ] 订单销售左侧菜单不再出现“报价导出”入口。
- [ ] 直接访问 `/products/print` 不再打开报价导出页面。
- [ ] 销售单 PDF/图片导出仍在订单列表的销售单抽屉内可用，操作日志归类到“订单销售 / 销售单”。
- [ ] 订单列表点击某行“销售单”后，在当前页面弹出销售单抽屉，不丢失订单列表筛选和分页状态。
- [ ] 销售单抽屉内点击“销售单设置”后，右侧打开设置抽屉，可维护收款方式、个性化说明、收款码，并可上传、选择、去除背景后的共享公章。
- [ ] 进入销售单页面后，页面先展示销售单预览，预览加载前不能确认生成 PDF。
- [ ] 刷新预览不新增历史版本；点击“确认生成 PDF”后才新增一个销售单版本。
- [ ] 指定历史版本下载和下载最新版都返回 PDF，不出现 `invalid document id`。
- [ ] 公司名称在“公司设置”中维护，销售单设置页不再维护公司名称。
- [ ] 销售单预览展示收款码图片、公章图片、多行收款方式和多行说明；下载 PDF 与预览关键信息和版式一致。
- [ ] 销售单收款码上传只接受图片文件，不能上传 HTML 或脚本文件作为收款码；收款码图片超过 8MB 时必须拒绝，图片过大或格式不支持时接口返回无效请求；销售单收款码上传必须先填写标签，标签为空时不能写入收款码资产，补齐标签后再上传。
- [ ] PR-328-SALES-ORDER-PAYMENT-CODE-SOFT-DISABLE：销售单设置中点击收款码“停用”后，该码在设置页保留并显示停用状态，可点击“启用”恢复；停用期间不出现在销售单预览和下载文件中；不删除数据库中的收款码记录、素材记录和上传图片文件，操作日志动作显示为停用/启用而不是删除。
- [ ] 销售单公章上传或去除背景失败时必须清理刚写入的公章资产，不会留下公开孤儿公章文件；排除错误后重新上传公章。
- [ ] 销售单 PDF/图片生成失败时必须清理刚写入的文件，不会留下公开孤儿销售单文件；排除错误后重新生成销售单 PDF 或图片。
- [ ] 销售单预览、PDF 和图片导出的商品明细包含录单的条目备注。
- [ ] 销售单预览页可直接拖动公章位置并调整公章大小，点击公章内部开始拖动时公章不跳走，下载 PDF 中公章盖在公司名称附近。
- [ ] 导出 PDF 中公章按原图比例等比显示，透明背景或圆形印章不被横向拉伸。
- [ ] 公司设置页可维护公账收款户名、开户行、账号、纳税人识别号和公司地址，并支持一键复制公账收款信息；销售单设置页不再出现公账输入项。
- [ ] 销售单预览页可直接拖动公章，松开后自动保存；刷新预览、重新生成 PDF 或重新生成图片时沿用新坐标。
- [ ] 公章位置不再在销售单设置页用坐标输入维护；把公章往下拖后，销售单预览仍能看到公章并继续拖动，重新生成 PDF 或图片后沿用新位置。
- [ ] 公章设置点击“去除背景”后，当前公章变为透明 PNG，红色印章内容保留，销售单、出库单和合同盖章都可选择使用。
- [ ] 上传带白边的新公章后，系统自动裁掉白边；调整公章大小后不需要再点“保存设置”，重新生成 PDF 或图片会使用最新尺寸。
- [ ] 销售单预览按下载图片同一张 A4 页面比例显示；下载最新版 PDF/图片后，公章位置和大小与预览一致。
- [ ] 销售单预览区域显示 PDF 预览，页面和预览 PDF 内可见“PREVIEW 预览版”；刷新预览或拖动公章不会新增历史版本，点击“确认生成 PDF”后才新增正式 PDF 版本。
- [ ] 销售单预览中的圆章不显示为椭圆；拖动公章大小滑轨后预览尺寸立即变化并保存，重新生成 PDF 或图片使用新尺寸。
- [ ] 销售单预览和下载 PDF 中客户公司地址完整换行不截断；单个收款码放大展示，多个收款码自适应填充空白区域。
- [ ] 销售单收款码默认出现在第 1 页右侧且比旧版更大；在销售单预览中直接拖动/拉伸“文字位置和大小”“收款码位置和大小”后，刷新预览、重新生成 PDF 和重新生成 PNG 图片都使用新位置和尺寸；个性化说明在文字框内优先显示在公账收款前面，公账信息很长时不得把个性化说明挤没。
- [ ] 销售单生成 PDF 后，点击“分享PDF到微信”会调起系统分享面板并直接发送 PDF 文件，不发送链接。
- [ ] 销售单生成图片后，点击“分享图片到微信”会调起系统分享面板并直接发送 PNG 图片文件，不发送链接。
- [ ] 分享到微信的销售单 PNG 为高清图片，整体字体比普通预览更粗更清晰，收款二维码足够大并可被微信识别。
- [ ] 浏览器不支持直接分享文件时，页面提示下载最新版后手动发送到微信，不自动复制分享链接。
- [ ] PR-350-COMBINED-ORDER-DOCUMENTS：订单列表勾选同一客户至少两个订单后，“组合单据”区域可打开组合销售单；组合销售单预览 PDF 展示顶部单据日期、关联订单、各订单日期分组、商品明细、小计和汇总应收；刷新预览不新增版本，点击确认生成后返回下载地址，并能在操作日志查到“组合销售单文件”。
- [ ] PR-350-COMBINED-ORDER-DOCUMENTS：选择不同客户订单时，组合销售单预览或生成接口拒绝并提示同一客户要求；只选择一个订单时前端和接口都不能生成组合单据。
- [ ] PR-354-COMBINED-DOCUMENT-REUSE-SINGLE-UI：订单列表点击“组合销售单”后打开的弹窗与单张订单“销售单”一致，能看到销售单设置、客户信息、销售单备注、预览、PDF版本和图片版本栏目；刷新预览、确认生成 PDF、确认生成图片、刷新历史版本和下载版本都使用组合订单内容，不出现另一套简化组合界面。
- [ ] PR-355-COMBINED-SALES-DOCUMENT-LAYOUT-PAGINATION：组合销售单顶部客户信息不乱行，不显示组合单号或订单数，只显示客户、客户公司、联系电话、单据日期、关联订单和地址；分组标题只显示订单日期，不显示订单号。长销售单 PDF 在说明/收款码无法放在第一页时自动生成续页并显示页码；销售单图片和组合销售单图片导出为一张长图，不分页裁切。
- [ ] PR-356-SALES-ORDER-PAYMENT-OVERLAY-MULTIPAGE：销售单预览出现第 2 页后，文字位置和收款码位置拖拽框显示在第 2 页可见区域；拖动和拉伸后刷新预览，拖拽框仍可见且位置/大小可继续调整。
- [ ] PR-357-SALES-ORDER-PAYMENT-OVERLAY-CROSS-PAGE：销售单预览出现第 2 页后，先缩小文字位置或收款码位置拖拽框，再把框向上拖过第 2 页页顶做跨页拖动；框应切到第 1 页底部并可继续向上移动，保存刷新后不会被固定回第 2 页。反向从第 1 页拖过页底时应能切回第 2 页。
- [ ] PR-358-COMBINED-SALES-GROUP-ORDER-DATE：组合销售单每个订单分组第一行表头显示“订单日期 2026-xx-xx”，不得显示订单号；顶部“关联订单”仍完整展示订单号；PDF 版本和图片版本一致。

### B5.1 合同盖章
- [ ] 订单销售菜单能进入“合同盖章”页面。
- [ ] 上传 PDF 合同后，页面直接渲染合同 PDF，可选择公章并在页面上拖动位置。
- [ ] 上传 DOCX 合同后，后端先转换为 PDF，页面再渲染转换后的 PDF。
- [ ] 同一个合同的多页都能单独加盖公章并拖动到不同位置。
- [ ] 合同标题和备注可编辑，点击“保存合同”后刷新列表仍保留；点击“删除合同”后该合同从合同库隐藏。
- [ ] 合同盖章页公章大小滑轨可统一缩放待盖章印章，预览和保存后的盖章 PDF 中圆章不显示为椭圆。
- [ ] 点击“保存盖章PDF”后生成新盖章版本，下载最新版返回 application/pdf。
- [ ] 未选择公章、非法文件类型、DOCX 转换失败或盖章 PDF 保存失败时，页面显示错误，不新增不可下载版本。

### B6. 出库单维护与下载
- [ ] 已发货订单行点击“出库单”后，在当前页面弹出出库单抽屉，不丢失订单列表筛选和分页状态。
- [ ] 出库单抽屉可维护出库日期、出库仓库、发货方式、快递单号和备注。
- [ ] 出库单页面先展示预览，预览加载前不能确认生成 PDF。
- [ ] 出库单预览和 PDF 的商品明细包含录单的条目备注。
- [ ] 出库单预览区域显示 PDF 预览，页面和预览 PDF 内可见“PREVIEW 预览版”；刷新预览或拖动公章不会新增历史版本，点击“确认生成 PDF”后才新增正式 PDF 版本。
- [ ] 出库单预览中的圆章不显示为椭圆；拖动公章大小滑轨后预览尺寸立即变化并保存，重新生成 PDF 使用新尺寸。
- [ ] 刷新预览不新增历史版本；点击“确认生成 PDF”后才新增一个出库单版本。
- [ ] 未发货订单调用出库单预览或生成接口会被拒绝。
- [ ] 指定历史版本下载和下载最新版都返回 PDF，不出现 `invalid document id`。
- [ ] 出库单生成 PDF 后，点击“分享到微信”会调起系统分享面板并直接发送出库单 PDF 文件，不发送链接。
- [ ] 销售单 PDF、销售单图片和出库单 PDF 的分享都通过同一个 `/api/share-resources` API 定位文件，再通过同一个前端 helper 分享文件。
- [ ] 出库单点击“公章设置”只维护共享公章资产：上传公章、选择公章、去除背景；预览中拖动公章位置或调整大小后，新生成出库单 PDF 使用最新公章位置和大小。
- [ ] 出库单 PDF 生成失败时必须清理刚写入的文件，不会留下公开孤儿出库单文件；排除错误后重新生成出库单 PDF。
- [ ] PR-350-COMBINED-ORDER-DOCUMENTS：订单列表勾选同一客户且均已发货的至少两个订单后，可打开组合出库单；组合出库单预览 PDF 展示“关联订单”、各订单的单据日期、订单日期、出库日期、收件人、快递单号、出库仓和商品明细；确认生成后返回下载地址，并能在操作日志查到“组合出库单文件”。
- [ ] PR-350-COMBINED-ORDER-DOCUMENTS：所选订单只要有未发货订单，组合出库单预览和生成接口必须拒绝并提示已发货要求。
- [ ] PR-354-COMBINED-DOCUMENT-REUSE-SINGLE-UI：订单列表点击“组合出库单”后打开的弹窗与单张订单“出库单”一致，能看到刷新预览、下载最新版、公章设置、出库维护、预览和历史版本栏目；刷新预览、确认生成 PDF、刷新历史版本使用组合订单内容，不出现另一套简化组合界面。

### B7. 发票申请与文件
- [ ] 订单列表点击某行“发票”后，在当前页面弹出发票抽屉，不丢失订单列表筛选和分页状态。
- [ ] 发票抽屉点击“申请发票”后，订单发票状态变为“已申请”，订单列表同步显示状态。
- [ ] 开票后上传 PDF 发票文件成功，抽屉和订单列表都显示文件入口。
- [ ] 上传 PNG/JPG/GIF/WebP 图片发票文件成功。
- [ ] 上传文本、SVG 或其他非 PDF/图片文件时，接口拒绝并提示只支持 PDF 和图片。
- [ ] 发票文件上传必须先确认订单存在；订单不存在时返回 order not found，不会写入发票资产文件。
- [ ] 发票文件保存失败时必须清理刚写入的发票资产文件，不会留下公开孤儿发票文件；排除保存错误后重新上传发票。

---

## C. 订单详情（/app/orders/:id）
- [ ] 页面能正常打开。
- [ ] 数量/单价/小计显示正常（无 %!f(*float64=...) 乱码）。
- [ ] 支持订单失效（并且不真删除）；失效不可恢复，只能复制成新订单。

---

## D. 商品档案（/app/products）
- [ ] 商品搜索/筛选可用。
- [ ] 商品档案按分组模板展示时，每个分类独立分页并显示该分类筛选后的完整父商品总数；任一分类翻页或修改每页条数不影响其他分类，表格底部不再出现跨分类的全局分页；规格子 SKU 仍折叠在父商品内且不单独占分页条数。
- [ ] 商品阶梯价可维护。
- [ ] 录单商品选择框直接输入过滤并选择商品；支持拼音/首字母匹配；页面不再出现单独商品搜索框。
- [x] SKU设置页只保留统一的“客户SKU列表”展示入口，进入后默认展示公共SKU。
- [x] 在客户SKU列表选择履约客户后，只展示该客户的自定义SKU；无专属 SKU 的履约客户也能出现在选择列表，用于初始化客户 SKU。
- [x] SKU设置页顶部先显示“SKU归属”客户上下文；选择客户SKU后，客户SKU列表和客户自己的商品分类切到同一客户上下文。
- [x] SKU设置页默认公共SKU时隐藏“客户专属 SKU”模块；选中履约客户后隐藏“新增公共产品”，“客户专属 SKU”不再重复选择客户；商品分类区域可用“是否使用公共商品分类”引用公共分类，客户SKU列表区域可用“是否使用公共SKU”引用公共 SKU，梯度模板区域可用“是否使用公共梯度模板”引用公共模板；三个开关关闭后公共模板从当前客户视图移除，开启时只读展示公共模板，并写入操作日志。
- [x] 客户上下文中不再单独复制公共分类；分类和 SKU 统一通过“SKU复制”从公共 SKU 或其他客户复制到当前客户，后端自动补齐目标客户缺少的产品类型/产品子类型，并按同名同分类覆盖目标 SKU。梯度模板列表中的公共模板提供“复制为客户模板”，复制后打开客户模板继续改名和调整档位。派生结果保留 `source_template_id` 或兼容来源字段，公共数据不被直接修改。
- [x] SKU设置页创建客户专属 SKU 支持熟豆、生豆和挂耳；生豆不要求基础产品，只维护生豆属性和绑定熟豆，绑定熟豆下拉不显示历史公共 SKU 别名复制件或其他客户 SKU 导致同名重复，挂耳可维护每袋克重和每盒袋数，保存后 `/api/product-settings/custom-products` 接收并返回正确 `product_kind`，不会把生豆/挂耳覆盖成熟豆。
- [x] PR-313-CUSTOM-ROAST-NO-BASE：SKU设置页创建客户专属 SKU 时选择“定制烘焙度”后不展示基础产品、复制基础产品 BOM 或复制价格梯度；提交 `/api/product-settings/custom-products` 可在 `base_product_id=0` 时创建定制烘焙度 SKU；创建表单不再提供“定制拼配 BOM”选项。
- [x] SKU设置页选择履约客户后，“客户专属 SKU”创建区横向扩展为全宽工作区，桌面宽度下不会被左侧窄列限制，基础产品、绑定熟豆、备注、复制选项和创建按钮都在面板范围内。
- [x] SKU设置页的新增公共产品、客户专属 SKU 表单和客户SKU列表行内编辑均支持 SKU 备注；保存后 `/api/product-settings` 返回 `remark`，客户上下文中的公共引用行只读展示备注，备注列展示在客户SKU列表最右侧。
- [x] SKU设置页客户SKU列表可按类型过滤标准、公共 SKU 改名、定制烘焙和定制拼配；搜索关键词可匹配商品名称、类型标签和备注。
- [ ] PR-354-PRODUCT-TYPE-SUBTYPE-COMPAT：SKU设置中分类入口、筛选和列表列名显示“产品类型/产品子类型”，不再显示历史层级分类名称；现有 `product_kind` 数据仍可映射为熟豆、生豆、挂耳、速溶咖啡默认产品类型，产品价格表继续复用原产品豆单发布快照能力。
- [ ] PR-355-PRODUCT-SUBTYPE-CONFIG-UNIT-RULES：在 SKU设置 → 商品分类 → 产品子类型行点击“配置”，把“速溶咖啡 / 冻干速溶”产品子类型配置为库存单位 kg、报价单位盒、录单单位盒、`unit_conversion_json={"盒":{"kg":0.2}}`、`integer_unit=true` 后，`/api/product-settings` 返回该配置；SKU 覆盖字段可保存阶梯价模板、工序模板和单位规则覆盖，不影响已发布产品价格表快照。
- [ ] PR-356-CUSTOMER-PRODUCT-RULE-TEMPLATES：在 SKU设置 选择客户后可新增并绑定“大客户速溶规则模板”；冻干速溶产品子类型读取客户产品规则模板中的工序。同客户再设置客户专属覆盖阶梯价模板后，产品价格表客户范围预览、生成草稿和生产计划工序解析都使用客户专属覆盖的阶梯价、模板工序、产品子类型价格表规则。
- [ ] PR-357-PRODUCT-PRICE-LIST-GENERALIZATION：产品价格表页面显示“产品价格表/生成价格表/发布价格表”，生成入口和版本列表的类型选项来自 SKU设置 的产品类型分类；选择“速溶咖啡”等产品类型后只生成该产品类型下的 SKU。发布快照仍写入 `bean_list_publications` 并带 `product_type_category_id/product_type_name`；按 `product_type_category_id` 可查询指定产品类型价格表，旧 `list_type=commercial/green/drip/retail` 和 `/public/bean-list/:list_type` 仍可读取同一套发布快照。
- [ ] PR-358-PRODUCT-PRICE-LIST-ORDER-PRODUCTION：`/api/order/form` 的价格表版本选项带 `product_type_category_id/product_type_name`，商品选项带产品类型/产品子类型和单位规则；旧熟豆/生豆/挂耳豆单版本仍可用。速溶咖啡盒装 SKU 可按 `unit_conversion_json` 推出“盒（200g）”录单规格，无 BOM 生产计划使用“速溶咖啡”默认原料并带出产品子类型配置的工序模板 ID。
- [ ] PR-359-PRODUCT-KIND-MIGRATION-CLEANUP：启动迁移后，旧 `product_kind` SKU 自动补齐熟豆、生豆、挂耳、速溶咖啡默认产品类型/产品子类型；历史订单和历史豆单/产品价格表发布快照不改价、不改 `content_json`。按手册完整执行速溶咖啡新增、客户专属阶梯价、发布产品价格表、录单和进入生产场景。
- [ ] PR-360-SKU-CATEGORY-SUBTYPE-CLARITY：SKU设置新增公共产品和客户专属 SKU 时字段显示“产品类别”，下拉来自当前 SKU 归属的商品分类产品类型；选择“速溶咖啡”等产品类别后系统内部兼容推导 `product_kind`。打开产品子类型“配置”时能看到单位会影响产品价格表、录单和生产计划的说明，已发布价格表和历史订单不会被回改；配置面板在左侧分类栏内不会超出范围。
- [ ] PR-361-SKU-SUBTYPE-PARKING-STRUCTURED-RULES：在 SKU设置 新增“冻干速溶 200g 盒装”时选择产品类别“速溶咖啡”但不选择产品子类型，创建后该 SKU 出现在“停车场（待归类 SKU）”，不进入产品价格表生成；再次创建并选择产品子类型“冻干速溶”时才挂到该子类型。打开商品配置，价格表生成规则使用下拉/勾选项，销售单位换算通过“新增销售单位”维护 1 盒 = 0.2 kg，不要求输入 JSON。
- [ ] PR-362-PRODUCT-CONFIG-TEMPLATE：SKU设置只显示“商品配置”模板入口，不再显示“客户产品规则/客户规则模板/客户专属覆盖/纳入产品价格表”。新建“盒装速溶配置”并维护阶梯价模板、工序模板、报价单位盒、录单单位盒、`1 盒 = 0.2 kg`、整数单位后，可绑定到“速溶咖啡 / 冻干速溶”。客户复制公共产品子类型时会复制其商品配置；客户只打开公共商品分类但关闭公共 SKU 时，分类树显示公共分类但不显示公共 SKU。
- [ ] PR-347-SKU-BOM-JUMP-FOCUS-FILTER：在 SKU设置 的客户SKU列表中点击某行“维护 BOM”后，BOM 配方维护页自动切到该 SKU 的归属，商品选择为该 SKU，商品 BOM 列表只显示该 SKU 对应 BOM；点击“显示全部 BOM”后恢复当前归属完整 BOM 列表。
- [ ] PR-312-SKU-CATEGORY-RECREATE-AFTER-DELETE：芬纳等客户 SKU 归属下新增产品类型，删除该分类后，再次新增同名产品类型必须成功；同名检查只拦截 active 分类，不拦截软删除历史分类。
- [ ] SKU设置页不再展示价格试算、商用/零售/生豆豆单或生成豆单入口；产品价格表页面不展示独立价格试算工作区，只展示豆单预览、生成豆单入口和豆单版本列表，商用价格来源抽屉仍保留临时阶梯价试算。
- [ ] 公共SKU与每个客户SKU的分类互不挤占顺序；客户自己的商品分类只显示该客户SKU，拖入或排序不影响公共SKU分类。
- [x] BOM配置进入后默认公共SKU；选择客户SKU后，BOM 配方维护的商品 BOM 列表和商品选择只显示该客户需要维护 BOM 的熟豆、挂耳等 SKU；已绑定熟豆 BOM 的生豆 SKU 不出现在 BOM 配方维护候选中；无可维护 BOM SKU 的客户不出现在选择列表。
- [ ] 商品设置页可勾选一个或多个产品并点击“失效选中产品”；失效后产品不再出现在录单、报价、成本等新业务候选项中。
- [ ] 产品失效后，历史订单、历史豆单、历史成本记录和 BOM 历史版本继续可追溯。
- [ ] 单个产品行点击“失效”后，产品 `active=false`，对应 BOM 同步显示为 BOM失效，历史订单、历史豆单和配方版本不被删除。
- [ ] BOM 维护页点击“失效当前 BOM”后，列表和详情状态显示“已失效”，配方明细仍可查看；重新同步出品率、保存配方或启用历史版本后恢复有效。
- [ ] 成本核算页遇到 BOM失效 产品时，顶部和商品行显示 “BOM已失效” 警示；豆单预览区域也能看到警示，发布前不会静默忽略。
- [x] SKU设置页可新建梯度模板；每档允许自定义区间名，最小/最大数量输入单位跟随模板展示单位，后台必须换算并保存克重区间；模板内展示单位只能选元/磅、元/kg、元/227g、元/100g、元/250g 中的一种。客户归属下新建模板必须保存为客户私有模板。
- [x] 产品子类型可选择启用中的梯度模板，保存后该分类下产品的成本试算和商用豆单预览按模板档位和加价率生成价格；未绑定分类继续使用默认商用梯度。客户分类绑定公共模板时，系统先派生客户模板再绑定；梯度模板面板也可手动“复制为客户模板”，后续修改客户模板不影响公共模板。
- [ ] SKU设置页不提供产品级梯度模板覆盖入口，但“客户SKU列表”中公共 SKU 和客户自有/客户定制 SKU 可给单个产品填写产品级加价率覆盖；填写后该产品覆盖产品子类型梯度模板加价率，清空后恢复继承产品子类型模板。客户上下文中公共 SKU 引用行只读，需复制为客户 SKU 后再维护客户自己的覆盖值。

---

## E. 客户档案（/app/customers）

### E1. 列表点击名称打开抽屉编辑
- [ ] 客户档案列表不显示独立“编辑”操作列；点击客户名称后，右侧抽屉打开当前客户详情和编辑表单。
- [ ] 客户名/联系人/电话/地址、默认来源、默认订单类型和客户类型可在抽屉内修改并保存，保存后列表刷新且当前 URL 保留对应客户编辑状态。
- [ ] PR-327-CUSTOMER-RESPONSIBLE-EMPLOYEE：客户档案抽屉必须选择负责人才能保存，负责人候选只包含员工维护里的启用内部员工，不包含外部客户账号；列表展示负责人。
- [ ] 修改客户负责人后，“操作日志”能看到客户负责人字段的旧值和新值。
- [ ] 客户详情抽屉底部不再出现只包含当前客户的一条客户列表或二级编辑入口。
- [ ] 客户类型可选择零售客户、批发客户、电商客户、渠道客户；客户类型、默认来源和默认订单类型都是必填项，历史缺失客户需在保存前补齐。
- [ ] PR-383-CUSTOMER-MANAGEMENT-PORTAL-WAREHOUSE：客户档案编辑抽屉的客户类型和默认订单类型下拉旁有“+”新增入口；新增自定义客户类型或订单类型后可直接选中保存，刷新后仍在下拉候选中，相关新增操作能在操作日志查到。
- [ ] 客户类型改为零售或电商后，该客户默认具备小程序商城能力，可商城下单并查看商城订单记录。

### E2. 一键复制发货地址
- [ ] 点击“一键复制发货地址”后，剪贴板内容为：
  - 姓名：xxx（联系人为空则用客户名）
  - 电话：xxx
  - 地址：xxx

### E3. 删除（停用）
- [ ] 点击“删除”二次确认。
- [ ] 删除后客户被停用（active=false），录单客户下拉不再出现。
- [ ] 删除动作写入审计日志。

### E4. 快速粘贴解析（/app/customers/new 或 /:id）
- 用例：粘贴
  - `姓名：张三\n电话：13800138000\n地址：上海市...`
  - [ ] 一键解析后能填充联系人/电话/地址。

### E5. 客户档案附件
- [ ] 客户档案资产图片上传超过 8MB 时必须拒绝；PNG/JPEG/WebP 图片压缩到 8MB 内后再上传；客户档案资产元数据保存失败时必须清理刚写入的文件，不会留下公开孤儿客户资产，上传后重新打开客户档案确认附件列表。

---

## F. 操作审计日志（/app/audit）
- [ ] 可按日期范围查询。
- [ ] 可按关键字查询。
- [ ] 任一用户触发的业务写操作，包括新增、修改、删除、提交、导入、发布、作废、调整、转仓、上传和状态变更，完成后都能在操作日志查到操作者、对象、动作、字段和新旧值或摘要。
- [ ] 客户 create/update/delete 能在日志中查到。

---

## G. 豆单发布（/vue-shell?view=costing）
- [ ] 产品价格表页面的商用批发、挂耳、零售、生豆豆单预览都支持收起和展开；页面能直接看到“生豆豆单”预览区。
- [ ] 产品价格表页面顶部展示全局“豆单范围”，豆单版本列表内不再放范围下拉；切换范围后预览、生成和版本列表都使用同一范围。
- [ ] 生豆豆单预览区可直接修改每个档位销售价，并可点击“保存生豆价格”把修改保存为当前豆单范围下的生豆豆单草稿，修改值进入本次豆单草稿/发布快照。
- [ ] 生豆 SKU 所在分类没有旧生豆模板时不显示旧限制提示，也不要求去 SKU设置 调整分类；按通用固定价、阶梯模板或价格计算模板补齐具体 SKU 正数价格、单位和换算后可以生成新预览。
- [ ] 产品价格表只保留一个外层“生成价格表”按钮，不再展示独立“生成挂耳豆单”按钮；进入生成抽屉后可在“产品类型”中选择挂耳对应的产品类型。
- [ ] 生成豆单抽屉不再展示“发布归属”下拉和客户选择；当前“豆单范围”为公共豆单时发布公共豆单，当前范围为某个履约客户时发布该客户豆单。
- [ ] 生成豆单抽屉不再展示“复制已有豆单配置”；产品选择、分类、样式和标签按当前 SKU 设置及本次抽屉设置直接生成。
- [ ] 豆单范围为“公共豆单”时发布后官方公开链接正常访问。
- [ ] 豆单范围为某个履约客户时，可选择一条官方已发布豆单作为“复制官方价格来源”。
- [ ] 客户豆单发布时保存官方价格来源；不再保存历史样式来源。
- [ ] 客户豆单发布后，内容和价格以发布时快照为准；后续官方豆单更新不会自动改写该客户豆单。
- [ ] PR-BEANLIST-VERSION-001：产品价格表直接展示豆单版本列表，页面顶部范围下拉列出“公共豆单”和每个履约客户（如岩师傅、用户A），选择某一项后只展示该归属版本，并能按 SKU设置 中的产品类型查看历史版本；同一产品类型价格表多次发布后版本列表保留每个已发布版本；小程序或 PDF 下载第一次生成后写入 `bean_list_publication_assets`，后续同版本复用缓存。
- [ ] PR-431-CUSTOMER-RESALE-BEAN-LIST：产品价格表版本列表可按用途筛选“工厂供货豆单 / 客户转售豆单”；默认录单和价格表生成只使用 `factory_supply`，切到 `customer_resale` 时管理员能看到客户发布版本、来源供货版本和用途标记，但这些版本不进入录单默认价格版本。
- [ ] PR-431-CUSTOMER-RESALE-BEAN-LIST：阶梯价模板编辑区有“允许客户转售豆单使用”开关；小程序接口只返回 active 且已授权模板，未授权模板不能被客户发布接口使用。
- [ ] PR-431-CUSTOMER-RESALE-BEAN-LIST：客户转售发布接口只能基于当前客户可见的 `factory_supply` 豆单快照生成 `customer_resale` 快照；跨客户来源、其他客户转售版本、撤回或不可见来源都被拒绝。
- [ ] PR-431-CUSTOMER-RESALE-BEAN-LIST：客户转售价格按授权模板档位匹配来源豆单价格，再应用统一加价、倍率加价和单品覆盖；单位不一致或来源缺少可匹配价格时发布失败，不生成 0 元价格。
- [ ] PR-309-BEAN-LIST-VERSION-DOWNLOAD：产品价格表“豆单版本列表”每行都展示“下载 PDF”；点击公共、客户、草稿、已发布或已撤回版本时，下载内容必须来自该行 `content/config` 快照，不能因当前商品价格、当前抽屉样式或当前筛选状态变化而改变历史豆单。
- [ ] PR-317-BEAN-LIST-STORED-PDF-DOWNLOAD：点击产品价格表版本列表“下载 PDF”后，浏览器不弹出打印窗口；系统先调用后端生成/缓存该版本 PDF，返回 `/api/costing/bean-list/publications/:id/pdf` 下载地址，再直接下载文件。再次点击同一版本复用 `bean_list_publication_assets` 已保存 PDF；客户专属豆单下载必须校验当前客户范围。
- [ ] PR-320-BEAN-LIST-PREVIEW-STYLE-PDF：在生成豆单抽屉点击“生成 PDF”时，页面不得调用 `window.print()` 或打开系统打印窗口；系统保存当前预览快照为草稿，后端按预览卡片样式生成 PDF 并下载。发布豆单、保存草稿和版本列表下载均得到同一套预览卡片样式；旧文本版缓存会因新版 cache key 不匹配而重新生成覆盖。
- [ ] PR-323-BEAN-LIST-DOWNLOAD-MATCH-PREVIEW-TYPOGRAPHY：生成抽屉预览中的豆单和“生成 PDF”/版本列表“下载 PDF”得到的文件必须字体大小、粗字重、卡片行列、分类标题、商品名称换行和报价块排版一致；旧 `bean-list-preview-style-v1` 缓存下载时必须按 `bean-list-preview-style-v2` 或 `bean-list-preview-style-v3` 重新生成覆盖。
- [ ] PR-332-BEAN-LIST-PDF-DENSE-PAGINATION：公开豆单和版本下载 PDF 必须先用 normal/compact/dense 三档压缩卡片再分页，减少截图中分类标题后大面积空白；截图形态的“工厂量单 1 个卡片 + 庄园精品豆 2 个卡片”应可压在同一页内；旧 `bean-list-preview-style-v2` 或 `bean-list-preview-style-v3` 缓存下载时必须按 `bean-list-preview-style-v4` 重新生成覆盖。
- [ ] PR-333-BEAN-LIST-PDF-NO-OVERLAP：公开豆单和版本下载 PDF 中，分类标题、商品卡、商品名和报价块不得互相覆盖。截图形态中“2、庄园精品豆...”标题不得被两张商品卡的“报价”或价格块压住；如果当前页剩余空间不足，标题和第一行商品必须一起换页。旧 `bean-list-preview-style-v3` 问题缓存下载时必须按 `bean-list-preview-style-v4` 重新生成覆盖。
- [ ] PR-324-FENNA-ORDER-ENTRY-CUSTOMER-BEAN-LIST-SCOPE：芬纳咖啡录单商品下拉只展示已进入芬纳商用豆单且带 `commercial_wholesale_tiers` 发布价格档的两个定制产品，不展示公共商用产品或未发布到该豆单的客户 SKU；选择 `芬纳定制-红酒日晒-中深烘` 或 `芬纳-曲奇定制（20%乌干达，15%云南厌氧日晒，65%云南水洗）` 后自动价格来自客户商用豆单发布快照，不再为空。
- [ ] PR-331-ORDER-ENTRY-PUBLIC-SKU-SCOPE：芬纳咖啡在 SKU 设置中 `use_public_sku=false` 时，录单和修改订单的商品下拉不展示岩师傅的 `孟连水洗A`、`红酒日晒-2026` 生豆 SKU，也不展示其他公共 SKU；芬纳自己的两个定制熟豆 SKU 仍可选且价格梯度正常显示。
- [ ] 产品价格表生成抽屉按当前豆单范围自动使用公共或客户上下文，发布保存不会串到错误归属。
- [x] PR-BEANLIST-VERSION-002：ERP 录单按熟豆、生豆、挂耳分别显示豆单版本选择并默认各类型最新；保存订单后商品行 `order_items.bean_list_publication_id` 和 `order_items.bean_list_version_no` 对应该行商品形态所选版本。没有专属豆单的客户按对应类型使用公共豆单。
- [ ] PR-349-ORDER-ENTRY-BEANLIST-SUMMARY-WARNING：录单/编辑订单商品明细标题区把熟豆豆单、生豆豆单、挂耳豆单分别按行显示；选择低于最新的公共或客户豆单版本后，已有商品行重新按该版本取价，右侧“豆单版本”标红并显示感叹号，点击或悬停提示“非新版本豆单”。
- [ ] PR-350-BEAN-LIST-PUBLISH-MANUAL-WITHDRAW：产品价格表连续发布两个版本后，旧版仍保持已发布并可在录单作为历史发布版本选择；手动撤回某版本后，该版本从录单豆单选择器消失，保存订单时不能显式使用该撤回版本。
- [ ] PR-351-ORDER-ENTRY-PUBLIC-BEANLIST-NO-CUSTOMER：/api/order/form 返回 `customer_id=0` 的公共已发布豆单版本；前端未选客户时使用这些公共版本作为摘要和豆单选择器候选，并对旧接口重复公共兜底数据去重。
- [ ] PR-352-ORDER-LIST-UNIFIED-SELECTION：表头复选框空状态点击后全选当前页正常订单，再点一次取消全选；手动只选部分条目时表头显示三态横杆。失效和组合单据区域没有“清空”按钮，取消选择通过表头或行复选框完成。
- [ ] PR-353-ORDER-ENTRY-HISTORICAL-BEANLIST-PRODUCT-TIERS：公共熟豆存在多个已发布版本时，选择历史熟豆豆单后，商品下拉仍显示该历史豆单中的熟豆商品和对应梯度，不会只剩生豆商品；`/api/order/form` 同一商品的阶梯中同时包含多个 published publication 的 `price_source_json.publication_id`。
- [ ] PR-BEANLIST-VERSION-003：小程序首次按新版豆单下单时弹出更新提示，内容展示新增、下架和调整摘要；确认后写 `customer_bean_list_acknowledgements`，同一客户同一版本后续不重复提示。
- [ ] PR-383-CUSTOMER-MANAGEMENT-PORTAL-WAREHOUSE：客户门户配置不再展示“豆单展示版本”开关；客户侧豆单按已发布客户专属豆单和公共兜底规则读取。
- [ ] 商用批发价格旁可打开“价格来源”，看到模板、当前价格和公式步骤。
- [ ] 价格来源中的生豆成本来自 BOM 物料当前可用批次的加权平均成本；没有可用批次时才回退物料默认采购价。
- [ ] 商用价格来源抽屉提供临时生豆成本、出成率和加价率输入，点击“重新试算”后只更新当前来源抽屉的临时试算结果；长期调整必须回到成本参数、BOM/产品出成率或梯度模板保存。
- [ ] 修改快速成本参数或梯度模板后，未发布豆单预览变化；已发布豆单和录单交易价格不静默变化，必须重新发布后才生效。

---

## G2. 生豆销售（PR-289-GREEN-BEAN-SALES）
- [x] SKU设置可新增/编辑生豆产品，生豆产品无需烘焙度、BOM 出品率或销售价，但必须选择单品/拼配并绑定熟豆 BOM。
- [x] 客户 SKU 列表 · 公共 SKU 支持形态过滤、名称搜索、产品类型过滤和产品子类型过滤，且不提供销售价列。
- [x] 原料入库支持产季、产地、产家风味描述；入库质检支持工厂风味描述、水分和密度。
- [x] 产品价格表可切换并导出生豆豆单，PDF 标题、价格档和最新通过生产质检信息体现生豆销售报价。
- [x] ERP 录单产品选择器可选择生豆产品，行内显示生豆标记，规格支持选择或直接输入自定义克数，并按所选生豆豆单版本的已发布 `green_bean_sale_tiers` 保存。
- [x] ERP 录单对没有已发布生豆豆单价格的客户生豆 SKU（如岩师傅“兰卡拼配生豆”）保留 `product_kind=green_bean`，`/app/api/order/form` 不返回绑定熟豆梯度，保存时拒绝并提示缺少生豆豆单价格，不回退熟豆阶梯价。
- [x] ERP 录单对已有客户生豆豆单价格的生豆 SKU，`/app/api/order/form` 返回的梯度来自该客户最新已发布 `green` 豆单快照；岩师傅“兰卡拼配生豆/曲奇拼配2.0”展示“岩师傅 - 生豆模板”生成并发布的 1KG/60kG 生豆价，不显示同名熟豆 SKU 梯度。
- [x] ERP 录单展示生豆 KG 梯度时，梯度区间和单价单位都保持 `kg`；例如岩师傅“兰卡拼配生豆”显示 `1-59kg 51.75/kg`、`60kg+ 62/kg`，不再把 KG 模板误显示为 `/磅`。
- [x] 生豆豆单按绑定熟豆 BOM 的原料成本快照生成默认档位价；用户可在生豆豆单草稿中手工修改每个模板档位价格，KG 模板手工价写入 `price_unit=kg` / `price_per_kg`，发布后订单只读取该快照价格。
- [x] 订单列表同一行能显示生豆/熟豆标记，混合订单同时显示两种标记。
- [x] 小程序履约客户可选择当前客户可见的生豆产品并提交订单，ERP 订单行保存 `product_kind=green_bean` 并按生豆豆单价格入单。
- [x] 小程序商城可上架生豆商品，商品卡、购物车和商城订单承接保留生豆产品形态。
- [x] PR/DEV 需求种子包含 PR-289-GREEN-BEAN-SALES；验收证据包含 `OP_MANUAL_GREEN_BEAN_SALES.md`、`OP_MANUAL_ORDER_SALES.md`、`docs/acceptance/2026-05-18-green-bean-sales.md` 和 `docs/acceptance/2026-05-19-green-bean-list-manual-pricing.md`。

---

## G. 用户权限与账号（/app/login / /app/vue-shell）
- [ ] 手机从 `/app` 入口打开系统时，只需输入一次外层 `order` 认证和一次系统账号。
- [ ] 登录成功、刷新、页面跳转和退出回登录都不跳出 `/app` 前缀，不再反复要求登录。
- [ ] 旧 Bearer token、系统账号过期或 API 401 时，页面应回到系统登录或得到 JSON 401，不应反复弹出浏览器原生 `order` 密码框。
- [ ] 员工维护和用户权限页只展示内部用户；外部用户不进入用户权限页，不显示账号类型切换。
- [ ] 内部用户账号可勾选角色、设置/重置密码、启停登录并保存可见功能。
- [ ] `/api/auth/internal-accounts` 只返回内部员工账号供用户权限页维护；外部用户由客户门户配置页管理。
- [ ] 用户权限页不展示“公共SKU代发客户”“代加工客户”两个客户业务角色选项。

---

## H. 生产制造（/vue-shell?view=productionAcceptance / producePlan / workOrders / warehouseInventory）
- [ ] 生产管理菜单可进入“工艺模板”，商品与配方菜单可进入“行业字段模板”；能创建服装/鲜果等非咖啡字段模板，不需要新增咖啡专用字段。
- [ ] 工艺模板可绑定 SKU、BOM 版本和行业字段模板，并配置多道工序；发布模板后写入操作日志。
- [ ] 开始生产后，生产工单展示冻结的工艺模板名、工艺路线和工序执行汇总；后续修改模板不影响已生成工单。
- [ ] 工序卡可维护计划投入、实际投入、实际产出、实际损耗、损耗率和异常原因，保存后操作日志可查。
- [ ] 生产验收页展示仓库、原料库存、WIP、生产工单、WIP 占用、生产日志、质检和成品追溯检查项。
- [ ] 生产计划页不再显示“物料需求计划”；默认筛选为待计划需求，切换到“全部”时标题显示“生产需求”；库存不足和库存充足分开展示，库存充足只提示且不能勾选进入生产计划。
- [ ] 库存不足（需生产）标题左侧提供唯一批量复选框；勾选即全选库存不足行，取消即全不选，单独勾选部分行时显示半选减号；顶部不再有“库存不足全选/全取消”“全选库存不足”“全取消”批量按钮。
- [ ] 生产计划烘焙建议可手动修改推荐机器、每锅数量和锅数；修改后最终投料数实时更新，并按最新值用于“开始生产”。
- [ ] 修改“每锅数量(g)”或“锅数”后，“预计成品(kg)”实时更新，计算口径为 `最终投料 × BOM出品率`。
- [ ] 当某商品 BOM 出品率为 `81.5%` 时，“生产计划（缺口 > 0）”与“烘焙建议”都显示 `81.50%`，不再出现一边 81.5%、一边 82%。
- [ ] 开始生产时如果 WIP 仓存在多个物料不足，接口一次返回汇总错误并列出全部不足物料，不会只提示第一个不足物料。
- [ ] 录单保存时如果成品批次足够，页面提示可用批次；确认使用后订单生产状态变为“库存待发货”。
- [ ] 录单保存时如果操作员拒绝使用现有批次，订单记录为待生产，生产计划即使看到物理库存也按库存不足缺口展示。
- [ ] 使用成品批次或历史库存余额的订单，生成顺丰发货 Excel 时不扣库存；回填快递单号标记已发货时扣减对应成品库存并写库存流水，重复回填不会重复扣减。
- [ ] 已发货但生产状态为空的历史订单不再进入生产计划；库存待发货的未发货预留会占用成品可用库存。
- [ ] 历史订单明细 `product_id` 为空或无效时，打开生产计划和调用 `/api/produce/unproduced` 不报 `cannot scan NULL into *int64`，且这些未绑定商品 SKU 明细不会生成生产计划行。
- [ ] 多品项订单只完成其中一个生产品项时，订单仍保持“生产中”；只有同一订单所有已绑定 SKU 明细都没有剩余生产缺口后，才会推进到“生产完成”。
- [ ] 库存不足商品生成计划后，物料需求汇总仍展示 WIP 可用、建议领到 WIP 和采购建议；豆袋等非重量物料按件/个等业务单位显示，不以 0g 或空采购建议误导。
- [ ] 订单列表“只看可发货”同时包含“生产完成”“无需生产”和“库存待发货”订单，排除已经发货订单；“库存待发货”订单可生成顺丰发货 Excel。
- [ ] 录单保存后若订单存在可生产商品但生产状态尚未写入，订单列表显示“生产：待计划”，生产计划页同一订单显示为待计划需求。
- [ ] 生产工单列表展示 WIP 已占、已耗和剩余占用。
- [ ] 仓库库存点击“WIP占用”后，可按工单查询占用、调整占用克重、释放废弃工单占用。
- [ ] 原料入库、WIP领退/转仓、成品转仓都能在物料/成品下拉框内直接输入并过滤候选项，不出现独立搜索框。
- [ ] 原料入库保存的单价写入原料批次成本，物料档案采购价不作为历史批次成本的直接修改入口。
- [ ] 库存调整单选择“批次成本”后，可按物料选择当前可用原料批次，填写目标成本/千克并提交；提交后该批次和对应库存批次成本更新，调整单记录调整前后成本和价值变化，库存数量不变，并能在操作日志按“库存调整单”查到批次号、旧成本和新成本。
- [ ] 库存调整单选择“库存数量”提交后，库存数量和库存流水更新，并能在操作日志按“库存调整单”查到调整前数量、调整后数量和调整原因。
- [ ] 成本核算对同一 BOM 物料有多个可用批次时，按当前可用克重加权平均成本参与生豆成本；待处理或不通过批次不参与。
- [ ] 库存数量补录原料时可填写补录成本/千克；未填写时新补录批次使用物料默认采购价，不以 0 成本进入后续加权平均。
- [ ] 四个库存作业页面使用一致的面板和网格排版，输入框高度、列宽和间距对齐。
- [ ] 生产中提交完成时如果 WIP 库存不足，页面右侧自动打开库存作业抽屉，并默认进入 WIP领退/转仓。
- [ ] 生产质检结果支持通过、待处理/待定、不合格/不通过；把工单质检改为“不通过”或“待定”时不会再提示 `scope, reference_no and result required`。
- [ ] 生产质检页面采用仓库库存式工作台布局；工单质检显示“选择工单”、原料质检显示“选择原料批次”、产品质检显示“选择产品批次”，右侧抽屉打开后只展示当前类型对应对象并回填单据号/批次号。
- [ ] 工单质检为“待处理”或“不通过”后，对应 `FP-...` 成品批次显示质检状态；原料批次、WIP 仓库存和成品批次列表也显示同一状态。
- [ ] 冻结批次不能继续领料到 WIP、不能被生产完工扣料，也不能被成品出库/转仓误用。
- [ ] 生产验收仓库相关检查项点击“打开”后进入对应仓库和库存类型，例如 WIP 检查项进入 WIP 在制仓，不停留在“全部仓库”。
- [ ] 在“仓库库存”输入 `FP-...` 成品批次，可看到成品、工单、生产日志和消耗原料批次；当消耗日志缺物料批次号时，追溯详情能从同生产运行库存流水补出 `MB-...` 批次。
- [ ] 生产日志列表直接展示本次完工的 `FP-...` 成品批次和消耗的 `MB-...` 物料批次；生产成本追溯链路不显示空单据、空物料、空批次且数量为 0 的占位行。
- [ ] 输入 `MB-...` 或 `LEGACY-MAT-...` 原料批次，可看到该原料批次的供应/期初说明和当前仓库位置。
- [ ] 批次追溯详情展示质检状态；待处理或不通过批次以冻结状态提示。
- [ ] `LEGACY-MAT-...` 页面说明这是系统升级时按旧物料库存生成的期初批次，点击查询不再提示 `batch not found`。
- [ ] 生产手册同步说明生产验收、录单批次确认、库存待发货、拒绝使用批次进入生产计划、建议领到 WIP、WIP 占用调整/释放、生产质检和批次追溯的操作。
- [ ] 访问未知 `view` 参数时 Vue shell 显示未知页面提示，不回落到录单页；访问 `view=mallSettings` 进入商城管理页面。
- [ ] 管理员账号可以发布/撤回豆单；客户账号看不到发布/撤回入口，保存修改后只能生成 PDF 下载。
- [ ] 客户账号直接调用发布/撤回接口会被拒绝，保存草稿接口返回该客户自己的 owner。
- [ ] 产品价格表生豆预览和生成抽屉明确显示“梯度按 KG，单价按元/KG”，并提示“生成并发布新版豆单后，录单才会使用新价格”。
- [ ] 客户豆单打开“生成豆单”时，若当前客户已有 `V1` 发布版本，版本号默认填 `V1.01`；若复制官方 `V1` 价格源且客户还没有版本，也默认填 `V1.01`；用户手动修改版本号后仍按用户输入发布。

---

## I. 财务月结（/vue-shell?view=financeDashboard / financeClosing）
- [ ] 财务首页选择月份后展示收入、主营成本、期间费用、毛利、净利和税费估算。
- [ ] 历史订单只有 `total_amount` 且 `grand_total` 为默认 0 时，财务收入和来源明细按 `total_amount` 计入；全额折扣等明确 0 `grand_total` 订单仍按 0 计入，作废订单不计入收入。
- [ ] 财务设置可选择咖啡烘焙厂、咖啡贸易商、咖啡壳豆加工厂和综合咖啡工厂。
- [ ] 小规模纳税人和一般纳税人税费估算字段正确展示。
- [ ] 费用管理可新增期间费用或主营成本，并影响月度报表。
- [ ] 费用管理新增费用时可选择关联员工；保存后列表显示员工名，点击员工名后只展示该员工相关费用，并可清除员工筛选。
- [ ] 费用管理新增费用时只能选择在职员工；停用员工通过接口提交为经办人时返回 `employee inactive`，且不写入 `finance_expenses`。
- [ ] 费用管理新增费用时，类别和付款方式可从常用候选中选择，输入关键字可模糊搜索，候选外的自定义值也能保存。
- [ ] 费用管理新增费用时，可填写订单ID、客户ID、商品ID、批次号和维度说明；保存后列表展示这些业务维度。
- [ ] 费用管理新增费用时，订单ID、客户ID、商品ID 如填写必须能找到对应业务对象；缺失时接口返回 `finance dimension ... not found`，且不写入 `finance_expenses`。
- [ ] 费用管理新增费用时，如果同时填写订单ID和客户ID，客户必须与订单归属客户一致；不一致时接口返回 `finance dimension customer does not match order`，且不写入 `finance_expenses`。
- [ ] 费用管理新增费用时，如果同时填写订单ID和商品ID，商品必须属于该订单明细；不一致时接口返回 `finance dimension product does not match order`，且不写入 `finance_expenses`。
- [ ] 月度结账页面展示月结前检查，包含来源异常、来源明细、票税台账、成本配比和会计交接检查项。
- [ ] 经营报告页面展示来源明细，能看到收入、主营成本、期间费用和票税来源的单据、往来方和金额。
- [ ] 订单收入来源明细展示订单收款方式；会计交接 Excel 的 Drilldown 工作表包含 Payment method 列并在订单收入行写入该收款方式。
- [ ] 票税台账页面可按月份查询，并可新增销售发票、采购发票、税款缴纳或其他票税记录。
- [ ] 票税台账重复录入同类型非空发票号或凭证号时返回 `tax ledger invoice already exists`，且不会新增第二条 `finance_tax_ledger`。
- [ ] 会计交接 Excel 下载入口可用，下载文件包含经营汇总、月结前检查、来源明细、票税台账和凭证草稿。
- [ ] 默认强锁账；结账后新增同月费用被后端拒绝。
- [ ] 锁账模式切换入口只对白名单用户显示，接口也校验权限和白名单。
- [ ] 结账后金额调整可保存，报表状态变为已调整。
- [ ] 已调整月份重复结账后仍保持已调整，不会降回已结账。
- [ ] 月度报告同时展示老板简报和明细表，PDF 和 Excel 下载入口可用。
- [ ] 财务需求进度已录入 PR/DEV；测试和验收证据按 Superpower/TDD 流程保留。

---

## J. 客户门户小程序主题（/vue-shell?view=customerPortalSettings / 微信开发者工具）
- [ ] ERP 客户门户配置页中，同一客户可以选择“咖啡工厂专业风 / 清爽业务工具风 / 品牌会员高级风”之一并保存。
- [ ] 小程序登录该客户后，首页和服务页视觉跟随 ERP 选择的主题；切换客户后主题跟随当前客户变化。
- [ ] 未配置主题的客户默认显示咖啡工厂专业风；小程序用户端没有自行切换主题入口。

---

## K. 客户门户商城（/vue-shell?view=mallSettings / 微信开发者工具）
- [ ] ERP “门户客户配置”列表只展示客户档案中已打开“开通客户门户/工作台”的客户，不再按批发客户写死；渠道客户打开开关后可出现在配置列表。
- [ ] 左侧菜单存在“客户管理”，且客户档案、门户客户配置、客户门户能力模板、客户履约手册都在该菜单下；能力模板页面名称显示为“客户门户能力模板”。
- [ ] 从 SKU 设置切到客户档案、门户客户配置、库存或其他页面时，页面内容必须立即切换，不需要刷新浏览器。
- [ ] 门户客户配置不显示“代加工仓库”编辑项，只展示该客户通过仓库库存绑定的客户仓库；仓库绑定客户在 库存管理 → 仓库库存 完成，保存后操作日志可查。
- [ ] ERP “客户门户配置”每个客户行直接展示“外部用户”区域，可创建外部用户、设置或重置密码、启停登录，并维护唯一 active 账号关联；该关联本身不授予 ERP 工作台。
- [ ] 客户门户配置页不请求 `/api/auth/accounts`，不出现渠道客户账号下拉框，也不包含 `saveERPBinding` 直接保存绑定逻辑。
- [ ] 同一外部用户不能 active 绑定第二个客户；客户门户配置页创建或复用外部用户后，列表立即展示姓名、手机号、登录状态和绑定状态。
- [ ] 禁用登录的外部用户不能继续作为有效工作台绑定；历史 active 绑定对应账号被禁用后，客户工作台和客户履约选择器按无有效绑定处理。
- [ ] 零售商城模板不能保存 ERP 工作台字段；保存零售商城能力模板时如果误填 ERP 权限或 ERP 视图，ERP 工作台字段会被拒绝，零售商城客户不绑定 ERP 工作台账号。
- [ ] 零售商城、其他不暴露 ERP 工作台或无法识别的能力模板不能绑定 ERP 工作台账号；客户门户配置页和客户履约内部 API 都返回 `ERP workbench unavailable for capability template`，且不写 active binding 或隐藏角色。
- [ ] 客户引用能力模板后保持实时引用；管理员修改模板能力、主题、小程序入口或 ERP 页面映射，引用该模板的客户刷新后立即按新配置展示和校验，不自动生成子模板。
- [ ] 管理员可以手动复制能力模板并改名；复制时只输入显示名称，系统自动生成安全模板 key，复制出的模板紧贴母模板下方缩进显示。模板列表同一时间只展开一个详情，其他模板只显示名称和说明；客户需要差异配置时由管理员手动绑定新的子模板。
- [ ] 能力模板可标记失效；失效模板仍在模板设置页可见，保存模板按钮仍可用且不置灰，但失效模板不出现在客户配置可选模板中；引用失效模板的客户保存/应用/绑定时返回 `capability template invalid`，小程序登录或刷新上下文时显示“客户配置已更新，请联系管理员处理”，必须重新选择启用模板。
- [ ] 客户档案修改客户类型后，系统不因类型变化自动改写能力模板或删除历史绑定；关闭“开通客户门户/工作台”或在门户客户配置中改为不暴露 ERP 工作台的模板后，客户不能继续进入履约工作台。
- [ ] ERP “客户门户配置”保存能力模板时，非空但无法识别的 `capability_template_key` 返回无效请求，不能被保存成手工能力配置；清空模板 key 时仍可保存手工能力配置。
- [ ] ERP “客户门户配置”加载到历史未知能力模板 key 时，客户列表和详情 API 保留原始 key，页面显示“未知能力模板”和原始 key，保存按钮不可直接静默清空；必须重新选择有效模板，或清空模板并停用门户后再保存。
- [ ] 能力模板列表包含“零售商城客户”，模板启用商城首页和商城下单能力。
- [ ] 公共 SKU 小批量客户通过小程序服务页提交 1000g 等非 454g 规格现货/代发订单且未手填单价时，订单明细按 `<14lb` 使用 `15-28lb` 豆单重量档折算后的价格保存，不回退产品默认价。
- [ ] 代发批次不能为空：公共 SKU 代发或代加工客户在小程序提交代发批次时，订单行数必须大于 0；订单行数为 0 时前端提示，接口返回无效请求且不能提交空代发批次。
- [ ] 公共 SKU 小批量客户打开小程序“现货下单”服务页时，现货下单商品可见范围只能包含公共商品和当前客户专属商品，不能显示其他客户专属商品；手工提交其他客户专属商品 ID 时接口返回无效请求且不生成订单。
- [ ] 小程序“一件代发”“代加工”“现货下单”服务表单使用商品/库存选择器，不显示“产品ID”“生豆物料ID”“目标产品ID”等要求客户手填系统 ID 的字段；代发/现货商品选择器和代加工目标产品选择器只列出当前客户可见商品，代加工投入物料选择器只列出当前客户可用托管原料。
- [ ] 客户网页版工作台和小程序“一件代发”“代加工发货”“现货下单”提交订单时，页面不提供单价、运费或优惠输入；即使手工请求传入 `unit_price=0.01`、`shipping_amount=12`、`discount_type=percent` 等字段，API/后端也按商品默认价或小批量阶梯价计算订单明细和订单合计，并忽略客户侧运费/优惠。
- [ ] PR-287-ORDER-LIST-FEE-KG-SUBMISSION-REPAIR：客户网页版工作台或小程序提交曲奇拼配等 KG 梯度商品时，后端按总 KG 选档；例如 1000g × 25 按 25kg 命中 24-49kg 梯度，81.91 元/kg 展示落单为 82 元/kg，商品金额 2050，运费 59 时应收 2109。
- [ ] 客户专属豆单 PDF 只能由归属客户访问：客户 A 不能下载客户 B 专属豆单 PDF，不能下载其他客户专属豆单，接口返回未找到；官方已发布豆单可作为公共兜底访问，客户 A/B 都可以下载官方已发布豆单。
- [ ] PR-431-CUSTOMER-RESALE-BEAN-LIST：小程序“我的豆单”显示工厂供货豆单和“我的销售豆单”；打开编辑器后可修改品牌名、豆单说明、版本说明、背景、样式、商品选择、统一加价、倍率加价、单品价覆盖和上新/推荐标签。
- [ ] PR-431-CUSTOMER-RESALE-BEAN-LIST：客户发布销售豆单后版本号按客户自己的转售版本递增，工厂供货豆单不被改写；列表显示新版本并可下载 PDF 和服务端长图 PNG。
- [ ] PR-431-CUSTOMER-RESALE-BEAN-LIST：客户 A 不能读取、编辑、发布或下载客户 B 的转售豆单；客户转售豆单草稿/发布写操作必须校验 mini token、当前客户绑定和 `bean_list` 能力，并能在操作日志追溯。
- [ ] 代加工申请库存与目标产品范围按当前客户隔离：投入物料必须来自当前客户托管库存且克重足够，目标产品必须为公共商品或当前客户专属商品；不能使用其他客户库存或其他客户专属产品，手工提交时接口返回无效请求且不生成申请。
- [ ] 批发客户打开小程序结算服务页时，只能看到当前客户自己的费用明细和结算单，不能显示其他客户的费用明细、结算单号或金额。
- [ ] ERP “客户门户配置”中可为客户选择小程序首页为“订单处理”或“商城”；保存为商城后，客户登录小程序直接进入商城页。
- [ ] 小程序登录页默认展示手机号快捷登录，点击微信手机号验证组件后，验证手机号匹配已启用登录且已绑定客户的渠道客户 ERP 账号时可直接登录并进入对应客户首页。
- [ ] 小程序登录页支持密码登录；用户名/手机号必须匹配已启用登录且已绑定客户的渠道客户 ERP 账号，密码错误、账号停用、内部员工账号或未绑定客户时不能登录。
- [ ] 小程序登录成功后 token 写入最大有效期，不因固定 7 天或 30 天自动过期；退出、账号禁用或绑定失效仍应阻止继续访问。
- [ ] 切换或重新登录零售商城客户后，`/api/mini/me` 和 `/api/mini/services/orders` 返回 `miniapp_entry_mode=mall`，小程序不会因为会话刷新或从商城进入“我的订单”退回订单处理首页。
- [ ] 同一小程序用户绑定多个客户时，切换当前客户后，“我的订单”只展示切换后的客户订单，不能显示切换前客户的订单号、商品或金额。
- [ ] 同一小程序用户绑定多个客户时，底部“个人中心”入口能切换客户和退出登录；在“个人中心”选择其他客户后调用 `/api/mini/current-customer`，并回到首页重新加载当前客户能力。
- [ ] PR-278-MINIAPP-MAIN-TABS：发布版客户进入小程序不会停在空白页；未登录自动进入登录页，已登录进入首页；底部固定显示“首页、订单中心、费用中心、个人中心”四个大入口；首页不再重复显示订单/费用入口，订单中心加载订单明细、销售单、出库单和物流信息，费用中心加载账目汇总、订单账单、费用明细和结算单，个人中心可退出登录和切换用户。
- [ ] PR-287-MINIAPP-BILLING-ORDER-RECEIVABLES：岩师傅这类已有订单但暂未生成费用明细/结算单的客户，打开小程序底部“费用中心”时能看到自己的订单账单和金额；接口 `/api/mini/services/settlement` 返回当前客户 `orders`，不能返回其他客户订单。
- [ ] PR-288-MINIAPP-ACCOUNTING-BILLS：账单页默认展示本月订单日期账期，可切换本周、本月、本年或自定义时间段；顶部展示应收总额、未付款金额、未付款订单数和已付款金额；账单列表只展示订单号、订单日期、应收金额、付款状态和收款方式，并支持订单号跳转订单页；账单页不重复展示收件人、商品明细、物流、销售单或出库单。
- [ ] 小程序订单和账单读取 ERP 同一订单底层；ERP 将订单标记为已失效后，小程序订单和账单不展示该订单；失效不可恢复，只能复制成新订单后按新订单展示。
- [ ] PR-276-MINIAPP-ERP-PASSWORD-LOGIN：小程序登录页密码模式展示用户名/手机号、密码和登录按钮；`/api/mini/login/password` 使用启用登录且已绑定客户门户的 ERP 渠道客户账号返回 mini token 和当前客户上下文；错误密码返回 401，内部员工、未绑定账号和禁用登录账号不能登录；个人中心提供切换用户和退出登录。
- [ ] PR-277-MINIAPP-PASSWORD-SKU-ALIAS：小程序登录页密码输入必须以掩码显示；岩师傅这类已维护定制 SKU 的客户进入现货/代发/代加工商品选择器时，只看到定制 SKU，不再看到被 `base_product_id` 替代的公共基础款。
- [ ] 停用客户不能继续作为小程序当前客户；停用客户不会出现在小程序当前客户 bindings 中，旧绑定也不能切换进入。
- [ ] ERP “客户门户配置”中可启停“商城下单”能力；未启用时小程序不展示商城入口，直接访问商城 API 返回无能力。
- [ ] ERP “商城管理”可选择已有产品，填写标题、副标题、描述、规格、售价、模板、状态和排序并保存。
- [ ] 商城商品公共目录范围只允许公共商品：商城管理不能上架客户专属商品，小程序商城不能显示客户专属商品，手工提交客户专属商城商品 ID 时接口返回无效请求且不生成订单。
- [ ] ERP “商城管理”上传商品图片后，图片 URL 写入商品并可通过 `/assets/mall_products/...` 公开访问；商城商品图片上传只接受图片文件，不能上传 HTML 或脚本文件，图片文件格式不支持时接口返回无效请求；商城商品图片上传超过 8MB 时必须拒绝，不能截断后保存为公开商品图片，图片文件过大时接口返回无效请求；商城商品图片上传必须先确认商品存在，缺失商品或图片更新失败时不能留下公开孤儿资产，商品不存在时接口返回无效请求。
- [ ] ERP “商城管理”右侧预览随当前表单内容实时变化，不需要先保存。
- [ ] 小程序商城只展示已上架商品，商品图片、标题、规格和售价可见。
- [ ] 小程序商城可把商品加入购物车、增减数量、填写收货信息并提交订单。
- [ ] 提交后 ERP 订单列表能看到新订单，订单来源服务为 `mall`，订单明细与小程序购物车数量一致。
- [ ] 批发客户通过客户门户提交代发/代加工发货订单后，ERP 顶部弹出新订单通知；点击通知进入订单列表“履约客户订单”范围并高亮该订单。
- [ ] 进入“客户履约账户”选择客户时，只能选到客户档案类型为批发客户，且存在 active 外部用户已配置手机号、已设置密码并启用登录的客户；未配置手机号、未设置密码、登录已停用或仅剩 inactive 绑定的客户不出现在选择器中；载入后“代发订单”能看到客户门户提交的代发订单。
- [ ] PR-311-CUSTOMER-PORTAL-ACCOUNT-PROFILE：在客户门户配置里给“芬纳咖啡”这类批发客户创建或启用外部登录账号后，不刷新整页也能在顶部“客户账户”当前客户下拉中搜索到该客户；客户门户配置模板摘要中的按钮显示“打开客户档案”，点击后打开该客户的客户档案抽屉。
- [ ] 客户履约账户不再展示“外部用户”配置区域，页面明确提示外部用户改到“客户门户配置”维护。
- [ ] 选择岩师傅这类公共 SKU 小批量批发/代发客户后，客户履约运营台只展示该客户已启用能力对应的导入类型、提交面板和列表；如果 `processing=false`，页面不显示代加工工单导入、提交加工工单或加工工单列表，表格列固定对齐且长文本不会撑乱布局。
- [ ] ERP 内部“客户履约账户”手工提交订单面板仍保留运费和商品优惠控件；客户网页版工作台对应面板不显示这些控件，客户只能按实价提交。
- [ ] 在客户履约账户载入批发履约客户后，运营台最底部出现“履约客户订单”全宽列表，字段包含订单号、日期、客户/类型、收件信息、订单费用、快递信息、订单状态和操作。
- [ ] 履约客户订单复用 ERP `/api/orders?scope=fulfillment` 和同一 `orders.is_void` 过滤；ERP 订单失效后履约客户订单默认隐藏，不支持恢复，只能复制成新订单后重新显示新订单。
- [ ] 履约客户订单列表的“订单费用”列展示商品金额、运费、优惠和应收；点击订单号可打开明细抽屉，看到商品行、收件信息、状态和备注。
- [ ] 履约客户网页版工作台登录后，底部“履约客户订单”能正常加载；`scope=fulfillment` 订单列表允许具备 `customer_processing.read` 的渠道客户账号读取，但只能返回当前账号绑定客户的订单，不能拼接 403 和订单 JSON 导致前端显示“请求失败”；客户提交订单后点击订单号能打开订单详情，不要求 `orders.write` 权限。
- [ ] 在履约客户订单明细中可打开销售单和出库单；销售单支持下载最新版、微信分享销售单；已发货订单的出库单支持下载最新版、微信分享出库单。
- [ ] 客户履约账户应用导入批次（Excel）时，代加工、代发、结算批次分别要求 `processing`、`direct_ship`、`settlement` 能力；未开通对应能力时接口返回 `customer capability ... unavailable`，并且不写加工工单、代发订单、费用或结算批次。
- [ ] 客户履约账户把同一外部 SKU 编码用更正名称或烘焙度的 Excel 重传并应用后，客户专属商品仍只有 1 条，商品名称/烘焙度和工作台选择器都显示最新值。
- [ ] 客户履约账户把同一外部代发订单作为不同 Excel 批次重传并应用后，ERP 只保留一张代发订单，代发明细和 ERP `order_items` 按行号更新，不会从 2 行变成 4 行。
- [ ] 客户履约账户把同一外部代发订单用更正订单日期、收件信息或备注的 Excel 重传并应用后，ERP 代发订单头显示最新订单日期、收件人、电话、地址和备注。
- [ ] 客户履约账户把同一外部代发订单用更正序号的 Excel 重传并应用后，代发导入订单和 ERP 订单仍各只有 1 条，序号更新为最新值，订单头和明细仍显示最新 Excel。
- [ ] 客户履约账户把同一外部代发订单用更正发货状态的 Excel 重传并应用后，ERP 代发订单发货状态显示最新值，例如从待发货变为已发货。
- [ ] 客户履约账户把同一外部代发订单用少行修正版 Excel 重传并应用后，代发明细和 ERP `order_items` 以最新行数为准，已删除旧行不会继续保留。
- [ ] 客户履约账户把同一外部代发订单用更正运单号的 Excel 重传并应用后，订单只显示最新导入运单号，不继续保留已更正的旧导入运单号。
- [ ] 客户履约账户把同一外部代发订单用空运单号的修正版 Excel 重传并应用后，订单不再显示旧导入运单号，但手工或其他来源运单号仍保留。
- [ ] 客户履约账户把同一外部代加工生产工单用多行生豆投料导入后，工单投料明细保留多种生豆且工单投豆量等于投料合计；用只剩一种生豆的修正版 Excel 重传并应用后，以最新投料集合为准裁剪旧生豆投料，剩余投料数量和工单投豆量按最新 Excel 更新。
- [ ] 客户履约账户把同一转换单号用更正转换前产品、转换后产品或数量的库存转换工单 Excel 重传并应用后，只保留 1 条转换工单，不会生成重复转换工单，转换前后产品和数量显示最新 Excel 值。
- [ ] 客户履约账户把同一包装子工单编号用更正产品、包装耗材或数量的包装子工单 Excel 重传并应用后，只保留 1 条包装子工单，不会生成重复包装子工单，产品、包装耗材和数量显示最新 Excel 值。
- [ ] 客户履约账户把同一外部生豆入库流水作为不同 Excel 批次重传并应用后，托管库存台账仍只有 1 条，生豆余额不会从 1500g 重复增加到 3000g。
- [ ] 客户履约账户把同一外部生豆入库和出库流水用更正数量的 Excel 重传并应用后，托管库存台账仍各只有 1 条，台账增减数量改为最新值，托管库存余额按差额修正。
- [ ] 客户履约账户把同一 Excel 行号的生豆入库和出库流水用更正生豆名称的 Excel 重传并应用后，旧生豆余额回退为 0，新生豆余额显示最新净增减数量，库存台账仍各只有 1 条并挂到最新生豆。
- [ ] 客户履约账户把同一外部生豆余额和耗材余额用更正盘点数的 Excel 重传并应用后，托管库存余额显示最新数，库存台账 delta 汇总也等于最新余额。
- [ ] 客户履约账户把同一 Excel 行号的生豆库存余额和耗材库存余额用更正物料名称的 Excel 重传并应用后，旧物料余额回退为 0，新物料余额显示最新盘点数，盘点调整台账仍各只有 1 条并挂到最新物料。
- [ ] 客户履约账户把同一外部结算费用行作为不同 Excel 批次重传并应用后，费用明细仍只有 1 条，费用总额不会从 80 元重复增加到 160 元。
- [ ] 客户履约账户把同一外部结算费用行用更正未结金额的 Excel 重传并应用后，费用明细仍只有 1 条，费用金额改为最新值，后续月结按修正后的金额汇总。
- [ ] 客户履约账户把同一结算费用行用更正费用名称的 Excel 重传并应用后，费用明细仍只有 1 条，不会生成重复结算费用，费用名称和金额显示最新 Excel 值。
- [ ] 客户履约账户把同一结算费用行用更正费用类型的 Excel 重传并应用后，费用明细仍只有 1 条，不会生成重复结算费用，费用类型、费用名称和金额显示最新 Excel 值。
- [ ] 客户履约账户手工提交代加工工单或代发订单时，未开通对应能力的客户会返回 `customer capability ... unavailable`，并且不写客户工单、代发导入单或 ERP 代发订单。
- [ ] 客户履约账户手工调整托管库存时，未开通 `inventory_custody` 能力的客户会返回 `customer capability inventory_custody unavailable`，并且不写库存物料、库存台账或库存余额。
- [ ] 客户履约账户内部 ERP 绑定接口不能给不暴露 ERP 工作台或无法识别模板的客户写 active 绑定；零售商城或未知模板客户走内部绑定 API 时返回 `ERP workbench unavailable for capability template`。
- [ ] 历史遗留 active ERP 工作台绑定如果指向零售商城、其他不暴露 ERP 工作台模板、无法识别模板或登录已停用账号，或手动 URL/API 指向未绑定客户，客户工作台 overview 返回 403/400 或 `customer ERP binding not found`，不会展示该客户工作台数据。
- [ ] 客户履约账户生成月结时，未开通结算能力的客户不能生成结算批次；接口返回结算能力不可用且费用保持未结算。
- [ ] 商城相关需求进度必须录入 PR/DEV；测试与验收证据按 Superpower/TDD 流程保留。

## K6. 订单补录双日期（PR-342-ORDER-BACKFILL-DATES）
- [ ] 新建普通订单时，录单页同时显示“单据日期”和“订单日期”，默认都是当天，且两个日期都可以手工编辑。
- [ ] 保存订单时传入 `document_date=2026-05-23`、`order_date=2026-05-20` 后，后端保存命令和订单编辑回显保留两个日期；未传 `document_date` 的旧请求自动按 `order_date` 保存单据日期。
- [ ] 新订单号按“单据日期”生成，例如单据日期为 2026-05-23 时订单号前缀为 `SO-20260523-`。
- [ ] 订单列表日期列同时显示“单据”和“订单”两行，订单详情抽屉标题也能区分两个日期。
- [ ] 销售单 PDF/PNG 显示 `单据日期` 和 `订单日期`；出库单 PDF 显示 `单据日期`、`订单日期` 和 `出库日期`。
- [ ] 需求进度录入 PR/DEV；测试与验收证据按 Superpower/TDD 流程保留。

## K7. 订单补录连续录单（PR-348-ORDER-BACKFILL-CONTINUOUS-MODE）
- [x] “订单补录”不是纯提示，录单页必须有“连续补录”开关。
- [x] 开关关闭时，两个日期仍可编辑，点击“保存订单”后保持普通保存和跳转行为。
- [x] 开关打开时，页面显示“保存并继续补录”和“保存并查看订单”；点击“保存并查看订单”按普通保存进入订单结果页。
- [x] 点击“保存并继续补录”后，页面不跳转，提示已保存订单号，并保留客户、单据日期、订单日期、客户资料带入字段、收款方式、发货状态、物流公司、物流产品、运费、取整设置和三类豆单版本。
- [x] 连续补录保存后，商品明细重置为一条空行，快递单号、收款金额、运费收款金额、收款凭证、订单备注、订单优惠和外协费用清空。
- [x] 需求进度录入 PR/DEV/UT/API/REV；测试与验收证据按 Superpower/TDD 流程保留。

## K8. 统一商品模型五期改造（PR-386-PRODUCT-MODEL-OVERHAUL）
- [ ] 用户界面主入口显示“商品管理”，主表区域显示“商品档案”，BOM 使用方式显示“生产 BOM”，产品价格表顶部显示“价格表归属”。
- [ ] 商品管理中可以维护“客户商品名”：选择客户、绑定商品档案、维护客户商品编号、品牌名、展示分类、排序、是否进入价格表和启停状态；工厂自营作为内置客户走同一套维护流程。
- [ ] 产品价格表生成候选只来自当前价格表归属客户下启用且纳入价格表的客户商品名；发布快照同时冻结客户商品名和商品档案。
- [ ] 录单、客户门户和履约展示客户商品名；库存、生产、成本和工单使用商品档案与生产 BOM，并拒绝跨客户 alias。
- [ ] 旧客户 SKU 继续可追溯；迁移检查只读建议贴牌-only 旧客户 SKU 转为客户商品名，不自动迁移、不删除旧 SKU、不回改历史数据。
- [ ] 所有客户商品名写操作、价格表发布/撤回、BOM 使用方式变更、订单保存和生产状态变化都能在操作日志查到。
- [ ] 覆盖工厂自营、贴牌、客户专属价格、客户专属包装、客户改配方、固定 BOM 版本、一次性订单、受托加工/客户来料、服装布料损耗、鲜果加工损耗等场景的手册说明已更新。
- [ ] 需求进度录入 PR/DEV；RED/GREEN 单测、API 测试、Vue build、`scripts/verify_kferp.sh changed`、浏览器完整流程和手册验收证据按 Superpower/TDD 流程保留。

## K9. 通用视图上下文（PR-387-VIEW-CONTEXT）
- [ ] 顶部不再只表达“工厂总览 / 客户账户”，而是显示“当前视图”；内部账号可切换工厂总览、客户和订单视图，渠道客户账号固定为外部客户视图。
- [ ] 客户视图绑定 `customer_id`，刷新后 URL 保留视图上下文；旧 `workspace=customer&customer_id=...` URL 仍兼容进入客户视图。
- [ ] 订单视图绑定 `order_id/order_no` 并派生订单客户；进入订单列表只显示该订单，进入录单、履约和追溯页面时默认带订单客户。
- [ ] 视图上下文只影响菜单呈现、页面默认过滤、URL 保留和跨页面参数传递；没有权限或跨客户请求仍由后端拒绝。
- [ ] 商品管理客户视图默认打开“客户商品名”，绑定商品档案，不直接编辑 BOM；BOM 页面仍以“生产 BOM”表达商品档案制造定义。
- [ ] 产品价格表在客户视图下锁定当前客户，候选只来自该客户启用并纳入价格表的客户商品名。
- [ ] 生产计划和工单保留工厂执行入口，可以按客户/订单过滤和展示追溯标签，但不变成客户专属生产页面。
- [ ] 保存当前视图、修改保存视图、停用保存视图写操作日志；操作日志可按“保存视图”对象查到。
- [ ] 需求进度录入 PR/DEV；RED/GREEN 前端单测、API 测试、Vue build、`scripts/verify_kferp.sh changed`、浏览器流程和手册验收证据按 Superpower/TDD 流程保留。

## L. 操作手册强制治理（PR-DOCS-001）

### L1. 手册规则
- [ ] `HOW_TO_WORKFLOW.md` 明确要求操作手册更新先于 REV。
- [ ] `AGENTS.md` 和 `AGENTS.zh-CN.md` 明确要求开发时同步更新操作手册。
- [ ] 手册规则包含“单个大功能一个独立操作手册”和“现有手册查缺补漏”。

### L2. 手册文件
- [ ] 存在 `orderapp-remote/docs/OPERATION_MANUALS.md` 总索引。
- [ ] 当前主要大功能已有初版 `orderapp-remote/docs/OP_MANUAL_*.md` 或对应既有用户手册。
- [ ] 根目录不存在 `OPERATION_MANUALS.md` 或 `OP_MANUAL_*.md` 手册副本，操作手册只维护 `orderapp-remote/docs` 一份。

### L3. 部署与验证
- [ ] `deploy_orderapp.sh` 使用 `orderapp-remote/docs/` 作为线上 `/docs` 来源，不从根目录同步手册副本。
- [ ] docs raw route 能读取 `OPERATION_MANUALS.md`。
- [ ] PR/DEV 需求种子包含 PR-DOCS-001；测试和验收证据按 Superpower/TDD 流程保留。

### K4. 大功能菜单内展示手册（PR-DOCS-002）
- [ ] ERP 左侧不新增单独“手册”菜单。
- [ ] 订单销售、生产管理、库存管理、商品与配方、财务管理、设置、客户管理、需求管理各自菜单内能看到对应操作手册入口。
- [ ] 客户履约手册可从客户管理菜单进入，客户履约账户页面顶部也保留手册入口。
- [ ] 手册页面读取 `OP_MANUAL_*.md?raw=1`，界面内容与 `orderapp-remote/docs` 源文件一致。
- [ ] 被合并的旧 `*-user-manual.md` 不再出现在 `orderapp-remote/docs/` 和线上 docs 列表。

### K5. 客户账户模式隐藏履约运营台（PR-299-CUSTOMER-ACCOUNT-HIDE-FULFILLMENT）
- [ ] 设置菜单出现“界面设置”，管理员可保存“客户账户模式隐藏履约运营台”开关；刷新后仍保持保存值。
- [ ] 默认开启隐藏；客户账户模式下不显示内部“履约运营台”入口，但客户侧工作台、客户门户配置、履约导入/订单 API 和现有履约运营台源码均未删除。
- [ ] 关闭开关后，客户账户模式按原权限显示“履约运营台”；重新开启后再次隐藏。
- [ ] 保存开关会写入 `/api/ui-settings`，并能在操作日志/审计中追溯 `ui.customer_account.hide_fulfillment_console` 的旧值和新值。

### L5. 手册图示化（PR-DOCS-003）
- [ ] 每个大功能 `OP_MANUAL_*.md` 都包含 `## 流程图`。
- [ ] 每个大功能手册的流程图使用 Mermaid `flowchart` 源码维护。
- [ ] ERP 通用手册页把流程图展示为节点和箭头图示，而不是整段纯文字。
- [ ] 流程图无法识别时，页面保留原始代码块，不出现空白手册。
- [ ] PR/DEV 需求种子包含 PR-DOCS-003；测试和验收证据按 Superpower/TDD 流程保留。

### K10. 商品 UOM 与销售换算（PR-501-PRODUCT-UOM-SALES-CONVERSION）
- [ ] 商品档案可维护库存单位、默认销售单位、销售单位换算和整数销售单位规则，API 返回结构化字段并保留 `unit_rule_override_json` 历史键。
- [ ] 商品价格管理不维护单位换算；商品价格表发布时后端按商品档案换算固化 `price_unit/inventory_unit/inventory_conversion_json`，缺少换算禁止发布。
- [ ] 录单保存价格表快照里的销售数量、销售单位、价格单位和库存单位数量；生产计划读取快照换算后的库存单位需求。
- [ ] BOM 产出单位仍只取商品库存单位，不允许手填销售单位或价格单位；历史价格表、订单、BOM、工单和库存流水不回改。

### K11. 商品引用单位模板（PR-502-PRODUCT-UNIT-TEMPLATE-REFERENCE）
- [ ] 单位模板作为商品 UOM 主数据模板，商品档案新增/编辑必须选择 `单位模板`，并显示有效库存单位和有效默认销售单位。
- [ ] 商品档案新增/编辑不展示 `不引用单位模板`、`高级单位覆盖`、`库存单位`、`整数库存` 或 `销售单位换算` 直填入口；旧商品级覆盖只做兼容读取，并保留 `unit_rule_override_json` 中其他历史键。
- [ ] `/api/product-settings` 返回 `unit_template_id/unit_template_name/unit_rule_source` 和有效单位字段；`/api/bom/products`、BOM 产出单位和生产链路读取有效库存单位。
- [ ] 商品价格表发布按 `product_id + price_unit` 重新读取商品有效单位规则并固化快照；价格模板/阶梯价模板不定义单位换算。
- [ ] 批量 `设置单位模板` 可处理同类商品；单位模板修改只影响未覆盖商品和后续新业务，不回改历史快照。

### K12. 单位模板多销售单位（PR-503-PRODUCT-UNIT-TEMPLATE-MULTI-SALES-UOM）
- [ ] 商品档案 → 单位模板 页面只展示 `模板名称`、`库存单位`、`默认销售单位`、`销售单位换算` 和整数规则，不展示 `报价单位`、`录单单位`。
- [ ] 新建单位模板时库存单位默认 `kg` 但可修改；同一模板可新增多个销售单位，每行显示为 `1 销售单位 = 数量 库存单位`，库存单位自身固定 `1:1` 且不可删除。
- [ ] `/api/product-settings` 单位模板返回 `default_sales_unit/sales_units/unit_conversion_json`；保存时 `default_sales_unit` 双写旧 `quote_unit/order_unit`，旧模板能兼容解析为默认销售单位和可销售单位集合。
- [ ] 默认销售单位必须来自可销售单位；缺少销售单位到库存单位换算时保存被拒绝。
- [ ] 商品价格表价格单位只能来自商品有效单位模板的可销售单位；发布时非法价格单位被拒绝，合法价格单位按模板换算固化快照。
- [ ] 商品档案仍强制选择单位模板且不显示商品级单位覆盖；BOM 候选商品和生产 BOM 仍读取库存单位，不受销售单位影响。

### K13. 销售规格模板派生 SKU（PR-505-SALES-SPEC-TEMPLATE-DERIVED-SKU）
- [ ] 销售规格模板维护库存单位和多条 `1 规格名称 = 库存数量 库存单位` 换算，商品档案必须选择销售规格模板。
- [ ] PR-520-INVENTORY-UNIT-LOCK：编辑已有物料档案时库存单位下拉置灰，绕过前端提交不同库存单位返回 `库存单位保存后不能修改`；物料名称、采购价、行业字段等其他基础字段仍可保存。
- [ ] PR-520-INVENTORY-UNIT-LOCK：编辑已有销售规格模板时库存单位下拉置灰，保存 payload 保留原单位；仓储层拒绝 `product_unit_templates.inventory_unit` 从旧单位改为新单位。
- [ ] 保存父 SKU 后自动派生子 SKU，商品档案配置抽屉的 `销售规格模板明细` 展示规格名称、换算和 SKU 编号。
- [ ] 商品档案配置抽屉不再出现重复的 `销售规格 / SKU` 大块说明，也不提供手动新增子 SKU 表单。
- [ ] 模板规格移除后历史 SKU 默认不展示；打开 `显示历史规格` 后显示 `模板已移除` 和 `历史 SKU 保留用于历史单据，不参与新建业务`。
- [ ] 商品价格表、BOM、录单、生产日志和库存调整等新建业务候选默认过滤 `template_removed` 派生 SKU；历史单据仍能按 SKU 编号回显。

### K14. 价格计算模板试算可解析销售单位（PR-507-PRICING-RULE-TRIAL-RESOLVABLE-UOM）
- [ ] 商品价格管理打开 `价格试算` 并选择商品后，`销售单位` 默认继承该商品有效默认销售单位或销售规格。
- [ ] `销售单位` 下拉只展示当前商品可解析到库存单位或标准重量单位的候选；没有换算的 `盒`、`袋`、`条` 不得仅因全局单位存在而出现。
- [ ] 销售规格子 SKU 可通过 `unit_conversion_json` 或净含量解析，例如 `227g袋装` 可换算到 `kg/g` 时可试算，缺少换算时提示先维护销售规格或单位换算。
- [ ] `/api/costing/pricing-rule-trial` 收到不可解析销售单位时返回明确错误，不得静默按 `kg` 试算。

### K15. 生产 BOM 原料损耗比（PR-508-BOM-MATERIAL-LOSS-RATIO / PR-511-BOM-MATERIAL-LOSS-BOM-LEVEL）
- [ ] PR-511-BOM-MATERIAL-LOSS-BOM-LEVEL：生产 BOM 草稿版本设置区显示 `原料损耗比` 开关；打开后可输入 `损耗比例 %`，范围 `0 <= 损耗 < 100`，并说明 `开启后组件消耗单位只能使用比例 %`。
- [ ] 开启原料损耗比后，组件消耗单位下拉只保留 `比例 %`；已有固定数量物料或商品组件时保存失败，不允许静默改成比例行。
- [ ] 保存 `比例 40%`、`原料损耗比 20%` 后，BOM 详情 API 的版本返回 `material_loss_rate=0.2`，物料比例明细保留运行快照；商品组件或非比例行即使前端传入该字段，后端仍保存为 0。
- [ ] `合计比例` 只统计配方净比例，显示为“不含原料损耗”；明细行展示原比例、损耗比和折算后有效比例，例如 `40% / (1 - 20%) = 50%`。
- [ ] 新建生产计划或工单时，生产基准 `1kg`、A 原料比例 `40%`、损耗 `20%` 的物料需求显示并冻结为 `0.5kg`，开始生产、WIP 占用和完工消耗按该数量执行。
- [ ] BOM 成本明细和价格计算模板试算按有效比例计入材料成本；历史 BOM、历史工单、历史库存流水和已发布价格快照不回改。

### K16. 端到端原料-下单-生产主链路（PR-509-E2E-RAW-MATERIAL-ORDER-PRODUCTION-FLOW）
- [ ] 从物料档案新建或复用原料，原料入库后能在原料批次、仓库库存和库存流水中看到对应库存单位、数量、批次和成本。
- [ ] 从商品档案创建或复用商品 / SKU，维护销售规格模板和生产 BOM 后，录单可选择具体销售规格并保存订单；订单明细保留价格表快照和可进入生产计划的商品对象。
- [ ] 下单后库存不足需求能创建生产计划，提交后生成生产工单和工序卡；生产工单可开始生产，库存作业能完成领料到 WIP、工单消耗或完工入库所需动作。
- [ ] 完工入库后能看到成品/半成品批次、库存流水、生产日志和订单/库存追溯；原料入库、订单保存、生产计划/工单/库存作业等用户触发写入能在操作日志中查到。

### K17. 商品价格管理试算瀑布说明（PR-510-PRICING-RULE-TRIAL-WATERFALL-EXPLANATIONS）
- [ ] 商品价格管理打开 `价格试算`，选择 `榛巧拼配227g袋装` / `SKU-000573` 后，点击 `BOM+工序成本` 能看到每个物料/工序的 BOM 用量、单位成本和金额。
- [ ] 分别用 `kg` 和 `袋` 试算时，BOM 明细金额和瀑布卡片单位随销售单位变化，不得只替换单位文本而复用同一金额。
- [ ] 点击 `其他成本` 能看到 `other_cost_details` 明细、来源和设置位置，文案说明价格计算模板编辑区 `其他成本` 与本次试算抽屉临时值的优先级。
- [ ] 点击 `加价增加` 能看到 `profit_explanation` 的加价率、参数来源、损耗后成本、加价金额、税前价和公式，文案说明 `临时加价率` 会覆盖模板参数。
- [ ] 三个说明面板均为只读展示，不保存或回写 Pricing Rule、商品价格表、订单或发布快照。

### K18. 商品价格试算来源说明与成本配置修正（PR-512-PRICING-RULE-TRIAL-SOURCE-COST）
- [ ] BOM 成本明细已包含 `原料损耗比` 且未填写 `临时损耗率` 时，价格试算 `yield_loss_amount=0`，页面不显示 `损耗增加` 瀑布卡片。
- [ ] 显式填写 `临时损耗率=20%` 时仍计算并显示 `损耗增加`，用于本次模拟，不保存到 Pricing Rule 或价格表。
- [ ] 试算抽屉顶部显示 `取整规则：...（来自价格计算模板）`；`取整调整=0` 时不显示取整瀑布卡片，非 0 时显示调整金额。
- [ ] 税率为空或模板税率为 0 且计税模式开启时，试算读取财务设置全局税率并展示 `来自财务设置`；模板税率非 0 时展示 `来自价格计算模板`；不计税时税额为 0。
- [ ] 价格试算字段显示 `工艺路线`，默认来自所选 BOM 版本路线；切换工艺路线只影响路线选择，不从路线模板读取或追加工位产能成本。
- [ ] 在 `生产管理 -> 工艺路线` 维护路线工序时，页面不出现 `工位产能` 和 `计划工序成本`，保存接口即使收到旧字段也会清空。

### K19. 物料档案分类模板（PR-513-MATERIAL-CLASSIFICATION-TEMPLATE）
- [ ] 物料档案列表使用通用 `分组模板` 控件，包含模板选择、目标分类和 `移动到分类`，不再显示本地 `增加分类`、`新增小分类` 或 `移动到小分类`。
- [ ] 选择物料分组模板后，物料按大类、小类和 `未分类` 分组展示；搜索、状态过滤、勾选、分组内全选和批量失效仍可用。
- [ ] 移动物料分类时写 `/api/business-group-assignments`，语义为 `usage_key=material_catalog`、`object_key=material`、`object_id=materials.id`，并写操作日志。
- [ ] 移动到 `未分类` 只清除物料在 `material_catalog` 下的归组，不影响库存、批次、成本、单位和行业字段。
- [ ] 旧物料分类上线后迁移到 `物料档案默认分组`，历史大类、小类和物料归属可继续展示。

### K20. 工位成本组件与产能批量折算工序成本（PR-514-WORKSTATION-COST-COMPONENTS）
- [ ] `生产管理 -> 工位/设备` 可维护 `机器成本/小时`、`人工成本/小时`、`其他成本/小时`，页面显示 `小时成本合计`，保存后 API 返回三项成本和合计小时成本，并写操作日志。
- [ ] `工位产能` 表单只维护适用工序、标准批量、单位、标准分钟/批和状态，不出现可编辑 `小时费率`；列表显示继承的工位小时成本。
- [ ] `生产管理 -> 工艺路线` 的路线工序只维护工序、工序名称快照、是否记录损耗和质检项，不展示或保存 `工位产能`。
- [ ] 公式验收：创建草稿生产计划后，在工序产能拆分中选择工位小时成本 `5元/小时`、标准批量 `5kg`、标准分钟 `60` 的产能，承担 `5kg` 时计划工序成本为 `5元`，折算单位成本为 `1元/kg`；该单位使用库存/产能单位，不使用销售单位。
- [ ] 商品价格管理试算选择该工艺路线后，只更新路线模板来源说明，不从路线模板追加工位产能或计划工序成本。
- [ ] 生产计划拆分、提交工单和工序卡冻结本次拆分的标准批量、计划分钟、小时成本和计划工序总成本；后续修改工位成本或产能不回改历史工单。

### K21. 标准制造成本与价格试算收敛（PR-515-STANDARD-MANUFACTURING-COST-PRICING）
- [ ] 工位小时费率由机器、人工和其他小时成本相加；PR-518 后标准工序成本来自 BOM 发布时冻结的工序成本快照。
- [ ] 价格试算读取标准制造成本，不要求传生产计划产能或批次数，并返回 `cost_source=standard_manufacturing_cost`、物料单位成本、标准工序成本和标准制造成本。
- [ ] 商品价格表发布时重新读取 BOM 冻结的物料/工序成本快照并固化价格计算模板快照。
- [ ] 生产计划/工单选择真实工位、产能和批次只影响实际成本追溯，不回改历史价格表。

### K22. 工序标准成本与工位适用工序（PR-517-OPERATION-STANDARD-COST-MASTER）
- [ ] PR-517 作为历史过渡口径保留；PR-518 后 `生产管理 -> 工序` 不再作为标准工序成本录入口。
- [ ] `生产管理 -> 工位/设备` 维护 `适用工序`；工位产能表单不再出现适用工序控件，保存产能时即使前端传旧 `applicable_operation_ids` 后端也忽略。
- [ ] 新增表 `manufacturing_workstation_operations` 保存工位与工序关系，并从历史工位产能适用工序回填一次；工位产能列表可继承工位适用工序以兼容生产计划自动拆分。

### K23. 工序多产能成本选择与 BOM 工序成本快照（PR-518-MULTI-CAPACITY-OPERATION-COST）
- [ ] 工位产能档展示候选单位成本：`小时费率 × 标准分钟 / 60 / 标准批量 = 元/批量单位`。
- [ ] 工艺路线工序必须选择 `标准成本产能档`；候选只来自启用工位、工位适用当前工序和启用产能档，不自动按排序或价格选择。
- [ ] BOM 发布时冻结工序成本快照，包含工序、工位、产能档、小时费率、标准分钟、标准批量和折算后的 `元/库存单位`。
- [ ] 修改工位小时成本、工位产能档或工艺路线后，已发布 BOM 的工序成本快照不回改；重新发布新 BOM 后才影响新试算。
- [ ] 商品价格管理试算读取 BOM 工序成本快照，明细来源显示 `BOM工序成本快照`。
- [ ] BOM 绑定工艺路线但没有冻结工序成本快照时，价格试算返回警告，商品价格表发布失败并提示 `请先发布包含标准成本产能档快照的 BOM`。

### K24. 生产计划拆分产能差距预览（PR-519-PRODUCTION-PLAN-SPLIT-DEMAND-GAP）
- [ ] `POST /api/production-plans/:id/operation-splits/preview` 返回 `coverage_summary`、`operation_coverage` 和 `material_summary`，且不写入拆分表。
- [ ] 20kg 实际需求安排 12kg 为 `short`，安排 20kg 为 `matched`，安排 24kg 为 `over`。
- [ ] 拆分抽屉显示 `产能安排总览`、`用料需求差距`、`实际需求`、`已安排`、`差距`，修改承担产量后实时刷新颜色。

### K25. 商品档案父商品与规格 SKU 分层展示（PR-526-PRODUCT-MASTER-PARENT-SKU-LIST）
- [ ] 商品“金色山脉”引用含 4 个规格的销售规格模板后，商品档案列表只出现 1 条“金色山脉”父商品，不再出现 4 条同级规格商品。
- [ ] 展开父商品行内的 `4 个规格 SKU` 可看到各规格名称和 SKU 编号，点击具体规格仍能打开商品档案配置抽屉。
- [ ] 商品列表数量、分页、表头全选和批量操作只统计父商品；搜索 `227g袋装` 或对应 SKU 编号仍返回“金色山脉”。
- [ ] 后端 products 子 SKU、`parent_product_id`、价格表、BOM、库存和订单引用不变，不执行数据迁移。

### K26. 行业字段模板移除计算预览（PR-527-REMOVE-INDUSTRY-CALCULATION-PREVIEW）
- [ ] 行业字段模板页不显示“计算预览”、业务预设及相关计算输入和预览按钮。
- [ ] 模板名称、状态、说明、字段定义、新增字段、保存模板和停用模板仍可正常使用。
- [ ] 现有行业字段模板和商品生产配置数据不被修改，不产生数据迁移。

### K27. 分组模板分类直接删除（PR-529-GROUP-TEMPLATE-CATEGORY-DELETE）
- [ ] 分组模板的大类、小类均显示删除入口，不显示分类启用开关或停用文案。
- [ ] 删除小类后分类行被物理删除，原引用对象在商品、BOM、仓库、物料等页面进入当前模板 `未分类`。
- [ ] 删除大类会同时删除全部小类并把全部相关引用归入未分类；业务对象和历史快照不删除，操作日志可查。

### K28. 商品菜单与业务/系统设置整合（PR-530-BUSINESS-SETTINGS-IA）
- [ ] 左侧一级菜单显示 `商品`，不再显示 `商品与配方`；商品档案、商品价格管理和商品价格表仍可进入。
- [ ] 设置菜单显示 `业务设置`，进入后依次可切换销售单设置、物流设置、发货人设置、分组模板和全局单位字典五个 Tab。
- [ ] 设置菜单不再独立显示上述五项、通知配置或设备产能配置；系统设置页可切换系统基础设置和通知设置。
- [ ] 旧 `salesOrderSettings`、`logisticsSettings`、`senderSettings`、`groupTemplates`、`groupManagement`、`notificationSettings` 和 `machines` 地址仍能打开原功能，既有配置数据不变。

### K29. 设置入口归并（PR-531-SETTINGS-ENTRY-CONSOLIDATION）
- [ ] `设置 / 公司设置` 同时显示公司资料和公章设置，可上传、选择和去除公章背景。
- [ ] `商品 / 商品价格管理` 显示价格计算模板管理，不显示已过时的成本参数设置。
- [ ] 主菜单不显示独立 `公章设置`、`成本参数设置` 或 `代加工模板设置`；公司设置和商品价格管理入口正常。
- [ ] 旧 `costingSettings` 页面不再提供，`outsourceSettings` 地址仍可访问；既有公章、历史成本参数数据和代加工模板数据不变。

### K30. 生产与系统菜单归并（PR-532-PRODUCTION-SYSTEM-MENU-CONSOLIDATION）
- [ ] `系统` 栏显示 `系统设置`，`设置` 栏不再显示系统设置；打开后系统基础设置和通知设置两个 Tab 正常。
- [ ] `生产管理 / 生产配置` 显示工艺路线、工序、工位/设备三个 Tab，原三个独立菜单不再显示。
- [ ] 生产管理主菜单和生产模块顶部切换条不显示生产成本；工单追溯上下文及旧 `productionCosts` 地址仍可读取既有成本数据。
- [ ] 生产菜单显示 `生产计划`，不显示 `生产计划/开始生产`；打开后原生产计划步骤和功能不变。
- [ ] 旧 `processTemplates`、`manufacturingOperations`、`manufacturingWorkstations` 地址仍可访问且既有配置不变。

### K31. 生产流程页面归并（PR-533-PRODUCTION-FLOW-PAGE）
- [ ] 生产管理侧栏显示 `生产流程`，不再独立显示生产计划、生产工单、工序卡、生产质检、生产验收。
- [ ] 打开生产流程后可依次切换生产计划、生产工单、工序卡、生产质检、生产验收五个 Tab，且不重复显示嵌套的生产模块顶部导航。
- [ ] 生产页面顶部切换条显示 `生产流程`，不再分别显示生产计划、工单、工序卡、质检。
- [ ] 生产手册位于生产管理菜单最后一项。
- [ ] 旧 `producePlan`、`workOrders`、`jobCards`、`qualityInspections`、`productionAcceptance` 地址仍可访问，既有业务数据和操作能力不变。

### K32. 商品档案通用分组模板候选兼容（PR-534-PRODUCT-GENERIC-GROUP-TEMPLATE-OPTIONS）
- [ ] 在业务设置创建一个包含大类/小类但没有旧用途绑定的分组模板后，商品档案的“选择分组模板”可选到该模板。
- [ ] 商品档案选择模板后显示完整大类、小类和未分类；现有商品及归类数据不被改写。
- [ ] 带明确 `production_bom` 等其他用途且不包含 `product_catalog` 的历史专用模板不混入商品档案候选。

### K33. 删除过时成本参数设置（PR-535-REMOVE-OBSOLETE-COST-PARAMETERS）
- [ ] 商品价格管理只显示价格计算模板、价格试算和新建模板，不显示成本参数设置或对应 Tab。
- [ ] 商品价格表/成本核算顶部不显示 `参数设置`，页面中不存在 `快速成本参数设置` 抽屉。
- [ ] 旧 `view=costingSettings` 不再打开设置页面；历史参数数据、旧成本记录和价格计算模板不被删除或改写。

### K34. 物料成本计价单位与生产 BOM 损耗口径修正（PR-538-MATERIAL-COST-UNIT-LOSS）
- [ ] 通过物料正式 API 新建库存单位为 `g`、成本计价单位为 `kg`、采购价为 `54` 的重量物料后，保存和查询响应均返回 `unit=g`、`cost_unit=kg`；再次编辑时库存单位和成本计价单位只读，后端拒绝把 `cost_unit` 改为 `g`，且成功保存可在操作日志中追溯。
- [ ] 数据库升级后，历史 `g/kg/lb` 等重量物料幂等回填 `cost_unit=kg`，计件物料回填当前库存单位；原采购价数值、历史批次成本、库存数量、BOM、工单和价格表快照不被批量改写。
- [ ] 物料档案、采购/入库成本录入和价格试算均区分库存单位与成本计价单位；库存单位为 `g` 的生豆采购价和试算明细显示 `54元/kg` 或 `54/kg`，不得显示 `54元/g` 或 `54/g`。
- [ ] 不传整体预期损耗新建生产 BOM 时，版本返回 `yield_rate=1`、整体预期损耗为 `0`；只设置 `material_loss_rate=0.2` 时，比例物料仅按 `比例÷0.8` 放大一次，不得再套用隐藏的 80% 产出率。
- [ ] 显式同时配置整体预期损耗 20% 和原料损耗 20% 的历史/测试 BOM 仍按连续两次除以 0.8 的兼容公式计算，价格试算返回同时包含 `整体预期损耗 20%`、`原料损耗 20%` 和 `连续放大` 的警告，系统不静默忽略任一项。
- [ ] 开发环境“榛巧拼配”修复为整体预期损耗 0、原料损耗 20%，三行原料折算成本分别为 `40.50元/kg`、`19.50元/kg`、`20.50元/kg`，BOM 物料成本合计 `80.50元/kg`；标准烘焙工序成本 `2.04元/kg`，标准制造成本为 `82.54元/kg`，不再是 `102.68元/kg`。
- [ ] 开发修数前有数据库备份；BOM 新版本、发布/默认切换和商品生产配置修正通过正式 API/服务并在操作日志可查。生产环境未部署、未写入、未切换入口。
- [ ] 材料 schema/规范化/单位锁定、物料 API、BOM 默认损耗、价格试算双损耗警告和前端单位展示的 RED/GREEN 测试通过；Vue/Vite 构建、相关 Go 测试、开发 API 烟测及三份操作手册路径记录在 `orderapp-remote/docs/acceptance/2026-07-16-material-cost-unit-loss.md`。

### K35. 价格计算模板统一加价率与右侧编辑抽屉（PR-539-PRICING-MARKUP-DRAWER）
- [ ] 新建或保存价格计算模板时，API 固定返回 `profit_method=markup`；输入加价率 `0.8`、成本基数 100 执行试算，税前价和无税无取整的最终价均为 180，不得为 500。
- [ ] 同一试算返回实际毛利率约 44.44%，最低毛利阈值高于该值时仍有预警；修改最低毛利阈值不得改变 `成本基数 × (1 + 加价率)` 的税前价结果。
- [ ] 读取历史 `gross_margin=0.8`、缺少方式且参数为 `0.8`、以及历史整数百分数 `gross_margin=80` 时，均规范为 `markup=0.8` 并试算为 180；已明确为 `markup=1.2` 的模板仍保持 120% 加价率。
- [ ] 历史 `fixed_add` 或不支持方式升级后保留原方式/参数说明并置为失效隔离，不会被静默解释为加价率；隔离模板的复制和直接保存入口禁用，必须新建加价率模板并人工录入确认后的参数；新保存或试算未规范固定加价模板时返回明确错误。
- [ ] 商品价格管理列表不再在表格下方常驻价格计算模板编辑表单；点击模板名称或 `新建价格计算模板` 时，右侧打开 `价格计算模板编辑` 抽屉。
- [ ] 复制普通启用或失效模板成功后直接打开新模板的右侧编辑抽屉；历史 `fixed_add` 等隔离模板不可复制。抽屉可用关闭按钮或点击遮罩关闭，再打开时不保留上一次的过期错误或成功提示。
- [ ] 规范化模板和修改加价率后，只有新试算、未发布草稿或重新生成的价格行使用新结果；已发布价格表快照、订单行冻结价格、财务凭证和历史单据未被迁移或重算。
- [ ] 模板保存和失效通过正式 API 完成并在操作日志可追溯；后端、API、迁移、前端 RED/GREEN、Vue/Vite 构建、开发环境 API/UI 烟测记录在 `orderapp-remote/docs/acceptance/2026-07-17-pricing-markup-drawer.md`。

### K36. 商品销售规格与阶梯模板数量单位严格匹配（PR-540-PRICE-TIER-UNIT-COMPATIBILITY）
- [ ] “初晓”默认销售规格为 `磅`、“咖啡熟豆”阶梯模板有效档位数量单位为 `kg` 时，返回 `阶梯模板不可用：商品规格“磅”与阶梯规格“kg”不匹配`；`kg/公斤/千克` 互相兼容，`lb/lbs/磅` 互相兼容，但 `kg` 与 `磅` 不因可换算而兼容。
- [ ] 在“初晓”商品自己的 `计价` 配置中，“咖啡熟豆”模板选项显示不可用原因且不能选中；若旧草稿仍保存该模板，当前商品行立即显示红色阻断提示。
- [ ] 价格表默认、父类或子类继承“咖啡熟豆”模板时，只有销售规格不匹配的具体商品被阻断；对应平铺价格行标红、最终价不可编辑，不能通过手工改价绕过，其他兼容商品仍正常生成。
- [ ] 不兼容商品不请求 Pricing Rule 试算，预览和 PDF 不显示该商品的 0 元、试算价、旧发布价或其他来源价格；草稿快照保留精确阻断原因供修正，不伪装成有效价格。
- [ ] 绕过前端直接调用保存草稿或发布 API 时，后端从数据库解析当前商品/子 SKU 默认销售规格、客户别名实际商品和阶梯模板有效档位；伪造模板名称、单位、兼容标记、最终价或手工调整仍返回 400，混合单位或无有效档位同样拒绝。
- [ ] 改用与商品规格相同或同义的阶梯模板后，模板可选、档位正常展开、Pricing Rule 试算成功，价格行固化商品销售规格、阶梯数量单位、模板和档位来源。
- [ ] 本校验只影响新草稿、重新生成和新发布；历史已发布价格表快照、订单行冻结价格、财务凭证和历史 PDF 保持原值且继续可追溯。
- [ ] 前端单位/工作流/预览合同、Costing 应用层/PostgreSQL/HTTP API、支持合同、Vue/Vite 构建和开发环境“初晓”浏览器烟测均通过，证据记录在 `orderapp-remote/docs/acceptance/2026-07-19-price-tier-unit-compatibility.md`；生产环境未部署、未写入、未切换入口。

### K37. 商品价格表按商品选择销售规格与默认规格（PR-541-PRICE-LIST-PRODUCT-SPEC-SELECTION）
- [ ] 商品档案中，同一父商品的规格明细只允许一个有效默认规格；切换默认规格后 `default_sku_id/effective_default_sku_id/is_default_sku` 投影一致，跨父商品、停用或 `template_removed` SKU 被拒绝，成功操作可在操作日志中查询。
- [x] 数据库升级按“现有唯一默认 → 销售规格模板默认 → 第一个有效规格”幂等回填默认规格；模板同步保留仍有效的商品默认规格，默认规格被移除时确定性回退且不修改历史发布、订单或 PDF。
- [ ] 商品价格表“选择分类和产品”一个父商品只显示一行；首次选择商品或分类只带入默认规格，用户可同时勾选 227g、磅和 1Kg，计数显示“X款商品 / Y个规格”，分类全选不会覆盖已经手工选择的规格。
- [x] 每个选中规格拥有独立计价入口和固定价；标题、价格模板试算、平铺价格行及预览分别显示“初晓 · 227g”“初晓 · 磅”“初晓 · 1Kg”，不同规格的固定价和手工覆盖不会串用。
- [x] 阶梯模板只按销售规格件数定义档位。选择 454g 后，数量 1、10、100 展示为 1个454g、10个454g、100个454g；模板中历史 kg/lb/g 字段不再导致新草稿或新发布不匹配。
- [x] 草稿往返保存 `product_spec_selections`；商品默认规格改变后旧草稿保留具体 SKU 并提示确认，失效或跨商品 SKU 阻止发布，不能通过伪造前端快照绕过后端校验。
- [x] 新发布价格行冻结具体 SKU、父商品、销售规格和 `quantity_basis=sales_spec_count`；PDF 和订单只匹配相同 SKU，订单件数直接命中该规格阶梯，同父不同规格不能串价。
- [x] 缺少新数量口径的历史发布、历史 PDF、订单及财务快照继续按旧逻辑读取且不回写；后端、API、迁移、前端 RED/GREEN、Vue/Vite 构建和开发环境浏览器/API 冒烟记录在 `orderapp-remote/docs/acceptance/2026-07-20-price-list-product-spec-selection.md`，生产环境未部署。

### K38. 商品价格表行式规格与商品名/规格分离（PR-542-PRICE-LIST-INLINE-SPEC-DISPLAY）
- [x] “选择分类和产品”中，同一商品的227g、454g等规格横向排列并自动换行；每项包含勾选、规格、默认标识和计价入口，活动计价面板在规格行下方全宽展示。
- [x] 平铺价格行主标题只显示“白月光瑰夏”，独立显示“规格：227g”或“规格：454g”；两个规格的错误信息仍可准确区分。
- [x] 计价面板分别显示商品和规格，不出现“白月光瑰夏227g”或把“白月光瑰夏 · 227g”作为商品名。
- [x] 同一父商品选择227g和454g后，预览、新草稿、新发布内容、公开页及PDF中的商品名均保持父商品名或客户显示名，规格属性分别正确，其他行业属性未丢失或重复。
- [x] 两个规格仍冻结不同 `sku_id/sku_name/spec_label/effective_sales_spec`，固定价、阶梯价、试算和手工覆盖互不串用。
- [x] 历史发布、历史PDF、历史订单和财务快照未重写；开发环境部署和浏览器/PDF冒烟通过，现有价格表未重新发布，生产环境未部署。

### K39. 工位产能计件成本与工序成本分列（PR-563-WORKSTATION-PIECE-COST）
- [x] 工位产能 API/UI 可选择 `按工时` 或 `按计件`；按工时显示标准分钟和继承的工位小时成本，按计件显示 `计件成本（元/销售规格件）`。刷新后字段保持，历史/缺失 `cost_method` 的产能读取为 `time`。
- [x] 新增计件产能的标准产能单位固定为 `件`，其中一件等于当前具体 SKU 的一个销售规格；API/UI 拒绝袋、盒、个、kg、g、lb 等其他口径，避免不同包装层级共用费率。合法保存、修改和停用可在操作日志看到成本方式、费率、`sales_spec_count` 口径及前后值。
- [x] 按工时产能保持 `小时成本 × 标准分钟/60 × 批次数`；按计件计划成本严格为 `计划数量 × 计件费率`，实际成本严格为 `实际产出数量 × 冻结计件费率`，两条公式不混用、不重复计费。
- [x] 工艺路线选择计件标准成本产能档并发布 BOM 后，快照冻结成本方式、计件费率、费率单位和 SKU 权威换算；后续修改产能费率不回改已发布 BOM、已提交工单或历史实际成本。
- [x] 100件“初晓拼配 227g”使用权威换算 `1销售规格件=0.227kg` 得到22.7kg，不解析商品名；该 SKU 的100件即100袋。包装产能为 `piece`、标准产能单位件、费率0.5元/销售规格件时，计划和实际产出100件的包装成本都为50元。
- [x] 同一生产计划/工单的烘焙、色选使用各自按工时产能，包装使用按计件产能；API和页面分别展示三道工序的计划/实际成本，成本合计等于明细之和，不能只显示总数。
- [ ] 临时 PostgreSQL、定向/完整测试、Vue/小程序构建及 development API/静态只读冒烟已通过；应用内登录态视觉验收被开发域名 `ERR_CERT_AUTHORITY_INVALID` 阻断，待证书信任恢复后补验。证据记录在 `orderapp-remote/docs/acceptance/2026-07-30-workstation-piece-cost.md`。

### K40. 开发环境公网域名（PR-567-DEVELOPMENT-PUBLIC-DOMAIN）
- [x] `DEV-567-DEVELOPMENT-URL`：development 构建和发布只使用 `https://dev.qacoohee.com/app`，production 仍只使用 `https://erp.qacoohee.com/app`。
- [x] `DEV-567-PUBLIC-INGRESS`：两个域名的公开证书均有效，开发和生产分别命中 `erp_orderapp`、`erp_prod_orderapp`。
- [x] `DEV-567-DEPLOYMENT-GUARD`：入口更新有校验、备份、热加载和失败恢复，生产应用和数据库未重启。
- [x] 功能分支预检、development 部署、严格 TLS 双域名烟测和证据文档全部通过。

### K41. 小程序开发/生产环境隔离与防误发（PR-568-MINIAPP-ENVIRONMENT-SEPARATION）
- [ ] 缺少构建环境或 API 地址、环境与地址不匹配时，小程序构建门禁失败；普通本地构建不会静默回退到生产地址。
- [ ] development 构建只包含 `https://dev.qacoohee.com/app`，所有页面显示“开发环境 · 测试数据”；development 包处于微信 `release` 正式版本时 API 请求被明确阻断。
- [ ] production 构建只包含 `https://erp.qacoohee.com/app`，不显示开发环境标识，既有 production 固定目录和发布人工确认流程保持不变。
- [ ] 登录令牌、豆单缓存键包含 development/production 环境命名空间，旧 `kferp.mini.token` 不会自动迁入任一环境。
- [ ] development 部署把通过 `RELEASE_INFO` 校验的产物原子同步到 `/Users/yiiiple-work/KFerp-miniapp-mp-weixin-dev`；production 仍同步到 `/Users/yiiiple-work/KFerp-miniapp-mp-weixin`，两者不会互相覆盖。
- [ ] 小程序工程开启合法域名校验；下载域名错误按当前包显示 `dev.qacoohee.com` 或 `erp.qacoohee.com`，不再提示关闭校验。
- [ ] 两套服务器微信参数均已配置且使用同一 AppID；定向小程序测试、类型检查、双环境远程预检、development 部署和开发包静态/API 冒烟通过，production 未部署。

### K42. 小程序客户维护、多商品录单与订单草稿（PR-569-MINIAPP-CUSTOMER-DRAFTS-MULTI-ITEM）
- [ ] `DEV-569-CUSTOMER-PERMISSION`：销售新增客户时服务端忽略或拒绝伪造负责人并固定为当前员工；销售修改本人负责客户成功，修改其他员工负责客户或尝试改派负责人返回 403，客户和操作日志均无越权写入。
- [ ] 管理员可以新增、修改任意客户并调整负责人；管理员改派后，原销售立即失去该客户修改权，新负责人获得修改权。合法客户新增、字段修改和负责人变更均能在操作日志按客户和操作人查到。
- [ ] `DEV-569-ORDER-CUSTOMER-QUICK-EDIT`：录单选中本人负责客户后可原地打开客户维护，保存后不离开录单页并刷新客户名称和默认值；本单收货资料未手改时同步最新资料，已手改时可选择“同步”或“保留本单”。选中他人负责客户时只读，绕过按钮直接保存仍返回 403。
- [ ] `DEV-569-MULTI-ITEM`：一张新订单可连续加入至少三条不同商品/规格明细，支持编辑和删除任意行；每行商品、规格、数量、销售单位和单价独立，页面合计等于全部有效行小计之和。
- [ ] 切换客户后全部商品行逐行重新校验公共/客户商品范围；存在新客户不可用商品时不能静默提交。正式提交只生成一张订单，订单明细数量、顺序、具体 SKU、数量、单价和合计与页面全部有效行一致。
- [ ] `DEV-569-ORDER-DRAFT`：销售和管理员分别保存时，每个登录员工只产生一份自己的服务器草稿；再次保存覆盖本人草稿，双方不能读取或覆盖对方草稿。
- [ ] 离开录单页、重新进入或重新登录后，恢复草稿仍保留订单日期、客户、收货快照、系统带入的收款/发货状态默认值、备注、全部商品行及行顺序；增加、删除或修改明细后再次保存和恢复结果一致。
- [ ] 保存草稿不生成订单号，不写正式订单和订单明细，也不触发生产、库存、发货或财务。单次正式提交只生成一张订单，提交处理中前端禁用重复操作，创建成功后服务端清理当前员工草稿。
- [ ] `DEV-569-AUDIT-DOCS-DELIVERY`：客户和草稿合法写操作进入操作日志；根目录与线上需求/验收、PR/DEV 种子、三份相关操作手册、总索引和本需求验收记录均已同步，自动化与部署证据见 `orderapp-remote/docs/acceptance/2026-08-01-miniapp-customer-drafts-multi-item.md`。

### K43. 录单规格以价格表为准与订单详情恢复（PR-570-ORDER-ENTRY-PRICE-SPECS-ORDER-DETAIL）
- [ ] `DEV-570-PRICE-TABLE-SPEC-SCOPE`：选定发布价格表后，含 100g/227g/454g 三个商品档案规格、但该表只给 100g 定价的商品，规格下拉和商品候选计数只显示一个 100g 可售规格；默认 227g 无价时自动选择 100g，不能显示或保存无价 227g/454g。
- [ ] 切换价格表后当前 SKU 在新表无价时，页面保留父商品但清空规格与自动价，明确要求重新选择；数量低于起订量或落在价格档位空隙时仍保留该已发布规格，并由现有数量缺价提示阻止保存。
- [ ] `DEV-570-ORDER-DETAIL-SCHEMA-COMPAT`：点击至少一张已有订单的订单号后详情接口返回 200，页面正常展示；`price_overridden=true` 的历史行仍向 API 输出 `price_override:true`，日志中不再出现 `column oi.price_override does not exist`。数据库没有新增 `price_override` 列，订单数据未被改写。
- [ ] `DEV-570-HISTORY-DOCS-DELIVERY`：历史订单中当前价格表已无的冻结规格仍以只读历史规格显示；复制同一订单时按当前价格表清空无价规格并要求重选。根目录/线上需求验收、PR/DEV 种子、销售手册和验收记录已同步。
- [ ] 定向测试、Vue/Go 受影响套件、支持合同、远程 development/production 预检、双分支合并与双环境发布通过；两个域名均完成认证后的只读订单详情/API/静态与日志冒烟，全程不创建测试订单，证据见 `orderapp-remote/docs/acceptance/2026-08-01-order-entry-price-specs-order-detail.md`。

### K44. 小程序 DevTools 自动预览产物完整性与项目刷新（PR-571-MINIAPP-DEVTOOLS-PREVIEW-SYNC）
- [ ] 构造 `app.json` 声明 `pages/employee-customers/employee-customers` 但删除对应 `.js` 时，服务器构建校验和下载端 `PAGE_FILE_MANIFEST` 复验都明确失败并报告缺失路径；补齐 `.js/.json/.wxml/.wxss` 后通过。
- [ ] development 和 production 固定包的全部主包/分包页面均通过四类文件闭包校验，`RELEASE_INFO` 的环境、API 地址和提交与目标分支一致。
- [ ] 固定目录替换前继续保留同环境上一包，失败时不留下半包；发布完成输出明确要求已打开项目的 DevTools 关闭项目后重新导入固定目录。
- [ ] 使用部署后重新导入的 production 固定目录进行自动预览时，上传清单包含 `pages/employee-customers/employee-customers.js`，不再返回 `800059 file not found`；只清编译缓存不是本项验收步骤。
- [ ] 发布/测试说明和 `orderapp-remote/docs/acceptance/2026-08-01-miniapp-devtools-preview-sync.md` 已同步；本需求没有业务写操作或操作日志影响，微信上传、审核和发布仍由人工执行。

### K45. 小程序订单完整详情、单据导出分享与录单末尾新增商品（PR-572-MINIAPP-ORDER-DETAIL-DOCUMENT-SHARE）
- [ ] 员工小程序“查看订单”点击订单卡可进入独立详情页；页面展示订单号、单据/订单日期、客户、收件/物流、收款/发货/生产/开票状态、费用、商品明细及小计/备注/价格表版本、报价/生产来源、负责人、录入人和订单备注，不再只有概要。
- [ ] 销售账号只能打开本人范围内订单；管理员可打开全部订单。销售手工请求其他员工订单 ID 时详情及四类单据接口均返回 404，且后端不继续读取订单详情或文件；客户账号和缺少权限的员工不能进入员工订单接口。
- [ ] 详情页提供“销售单 PDF、销售单图片、发货单 PDF、发货单图片”四个入口。已有最新版时直接分享该正式文件；无版本时有写权限员工可生成后分享。PDF 响应为 `application/pdf` 且以 `%PDF` 开头，图片响应为 `image/png` 且 PNG 签名正确，文件名包含订单号和版本。
- [ ] “导出并微信分享”在订单加载成功后只出现一次，并作为首个业务卡片显示在订单概要和全部详情区块之前；四个入口、版本状态和分享行为保持不变。
- [ ] 新生成出库单时 PDF 与高清 PNG 使用同一冻结快照和版本；任一渲染、文件、资产或文档写入失败均回滚数据库并清理两类文件。历史只有 PDF 的版本不被回写，图片请求返回未找到，生成新版本后两种格式同时可用。
- [ ] 小程序分享 PDF 调用微信文件转发、分享图片调用微信图片菜单，分享的是本地临时文件而不是 URL；能力不可用或调用失败时分别回退到 `openDocument(showMenu=true)` 和图片预览，下载合法域名错误仍提示当前构建域名。
- [ ] 录单页“新增商品”按钮只有一个，位于最后一条商品明细下方、商品估算合计上方；新增三条商品后无需滑回页首即可继续新增，删除明细和提交全部有效明细行为不变。
- [ ] 新建正式销售单、销售单图片或出库单版本后，操作日志能按订单号和当前员工查到既有单据生成记录；只查看详情、下载或再次分享已生成文件不新增操作日志。
- [ ] 定向 Go/Vitest 测试、后端受影响套件、小程序类型检查与 development 构建、Vue/Vite 构建、PR/DEV 支持合同和开发环境接口/固定包冒烟通过；手册与证据见 `orderapp-remote/docs/acceptance/2026-08-01-miniapp-order-detail-document-share.md`。生产环境未部署。

### K46. 小程序按客户当前价格表录单与生产前订单编辑（PR-573-MINIAPP-ORDER-CATALOG-EDIT）
- [ ] `DEV-573-CUSTOMER-PUBLISHED-CATALOG`：开发环境中，所选客户当前默认已发布价格表包含商品 A、但不包含启用商品“红岩”时，小程序新建和编辑订单只返回商品 A 的当前有价规格；搜索“红岩”无结果。历史、归档、非默认价格表中的商品/规格不能因商品档案启用而出现，手工提交其 ID 时接口拒绝且不写订单。
- [ ] 切换新建订单客户后重新加载该客户当前默认已发布目录；客户专属商品、公共回退和不同商品类型价格表与 ERP 同一客户录单结果一致。目录加载中不能打开旧客户商品选择器，恢复草稿也必须按草稿客户重新校验全部商品行。
- [ ] `DEV-573-PREPRODUCTION-ORDER-EDIT`：订单详情顶部显示“编辑订单”；允许编辑的订单进入编辑页后正确回显商品、具体规格、数量、成交价、运费、优惠、收件资料和订单备注，客户显示为只读且不能切换或打开客户编辑。保存后详情合计和全部字段与提交一致。
- [ ] 当前默认已发布价格表已移除的历史冻结规格在编辑页明确标记为历史内容，不进入商品/规格候选；未删除或未替换时不得保存为新的当前订单行。替换后保存的商品、规格、价格表发布 ID、成交价及手工改价状态正确冻结。
- [ ] `DEV-573-CONCURRENT-STATE-AUDIT`：未进入生产计划、无生产工单、无生产执行且未发货的正常订单允许修改；已进入计划、已有工单、已开始生产、生产完成或已发货订单不显示可用编辑入口，直接调用编辑接口返回冲突且所有订单字段和明细不变。
- [ ] 并发场景中，员工打开可编辑订单后另一端先进入生产计划、生成工单、开始执行或发货，再点击保存时后端在同一事务内拒绝修改；不存在只改订单头、只改部分商品、费用已改但明细未改等部分写入。普通销售仅能编辑本人订单，管理员按现有范围编辑，缺少写权限和越权请求分别拒绝。
- [ ] 合法编辑在操作日志中可按订单号和员工查到修改记录及前后摘要；被权限、目录、生产/发货状态或并发冲突拒绝的请求不产生成功编辑日志。
- [ ] 编辑前已生成的销售单/发货单历史版本仍可追溯但不再作为当前版本返回；再次导出基于保存后的商品、费用、收件和备注生成新版本。详情、列表和后续生产读取修改后的订单，不继续使用旧页面缓存或旧单据快照。
- [ ] 定向 Go/Vitest 测试、后端受影响套件、小程序类型检查与 development 构建、PR/DEV 支持合同和开发环境 API/固定包冒烟通过；证据见 `orderapp-remote/docs/acceptance/2026-08-02-miniapp-order-catalog-edit.md`。只合并部署 development，production 未部署。

### K47. 商品价格表完整祖先继承、SKU 固定价与生豆通用计价（PR-574-PRICE-LIST-FIXED-INHERITANCE-GREEN-TEMPLATE）
- [ ] 构造至少三级分类祖先链，在不同层分别配置计价方式或模板；商品按 `商品 > 当前分类 > 逐级祖先分类（由近到远） > 价格表` 选择最近有效设置，当前分类继承时能继续找到祖父级及更高祖先，不停在直接父类。
- [ ] 价格表默认或任意祖先分类设置固定价、商品保持继承时，商品计价只显示本层方式/模板选择；每个已勾选具体 SKU 的规格行旁直接显示独立固定价输入，227g、454g 等规格分别生成各自金额和来源，不能共用或串值。
- [ ] 已解析到父类或更高祖先的计价方式时不再显示“未设置计价方式”。固定价 SKU 未填正数时，其规格行显示 `未填写固定价`；阶梯模板或价格计算模板缺失时显示对应缺项。未完成值可保留在当前草稿，但预览不得补 0 元或旧价格，正式发布必须拒绝。
- [ ] 恢复含旧平铺固定价的当前可编辑配置时，只把可精确解析的 `${sku_id}:fixed_price` 键一对一回填到同一 SKU；已有 SKU 正数 `fixed_unit_price` 优先，无精确 SKU、重复冲突和仅靠名称/规格文本的旧值不迁移，兄弟 SKU 不会获得该金额。
- [ ] “黄波旁水洗生豆”位于没有旧生豆模板的普通商品分类时，不显示“未挂到带生豆模板的分类”，可用通用固定价、阶梯模板或价格计算模板生成正数平铺价格行；商品不因缺少旧模板被隐藏或阻止。
- [ ] “小菠萝生豆”的父类或更高祖先已有计价方式时，商品行显示正确继承来源，不再提示到商品价格表重新设置计价方式；若具体参数缺失，只显示真实缺项。
- [ ] 后端草稿和发布校验继续拒绝固定价 0、非正数最终价、缺单位/换算或不完整平铺行；无旧生豆模板元数据但通用价格行完整的生豆请求可以通过同一 API 合同。
- [ ] 历史已发布价格表、旧生豆价格档、PDF、订单和财务快照保持原值且不被重算或回写；没有 schema/data migration，没有新增操作日志类型，未执行 development 或 production 部署。自动化证据和待 Van 验收项记录在 `orderapp-remote/docs/acceptance/2026-08-03-price-list-fixed-inheritance-green-template.md`。

### K48. 员工小程序图片分享入口系统开关（PR-575-MINIAPP-SHARE-IMAGE-ENTRANCE-SETTING）
- [ ] 未保存过配置时，员工读取接口返回 `image_need_show_entrance=true`；管理员在个人中心关闭、刷新和重新登录后仍返回 `false`，再次开启后返回 `true`，证明是全局服务端设置而非本机缓存。
- [ ] 只有员工管理员且具备设置权限时个人中心显示开关并可保存；普通销售和客户账号页面不显示。普通销售、客户、无令牌、过期令牌及缺字段的伪造 PUT 分别返回 403/401/400，配置和成功审计记录均不变化。
- [ ] 保存成功后，操作日志能按“小程序设置”或操作者查到“分享图片携带小程序入口”的旧值和新值；配置 upsert 与审计同事务提交，审计失败时配置回滚，不存在“设置已变化但无日志”。
- [ ] 开关关闭时，销售单图片和发货单图片调用微信图片分享均明确传 `needShowEntrance=false`；开启时均传 `true`。每次 PNG 分享前读取当前值，其他管理员修改后旧会话下一次分享使用新值。
- [ ] 设置读取失败时，图片仍可按 `false` 安全分享并提示；PDF 分享不请求也不使用该设置。直接分享不支持或失败时继续回退图片预览，手册明确该低版本路径由微信客户端控制。
- [ ] 本需求不改 PNG 内容、单据快照或版本，不追溯修改已经发送的图片消息；查看设置、下载和再次分享不新增业务日志。定向 Go/Vitest、受影响 Go 套件、miniapp 类型检查与 development 构建、支持合同和 `git diff --check` 通过，证据见 `orderapp-remote/docs/acceptance/2026-08-03-miniapp-share-image-entrance-setting.md`。
- [ ] 代码只推送功能分支并合入 `develop`；未部署服务器、未同步固定小程序包、未执行微信 DevTools 上传/审核/正式发布，旧已发布小程序版本不会自动获得该开关。

### K49. 员工小程序客户粘贴收货信息（PR-579-MINIAPP-CUSTOMER-ADDRESS-PASTE）
- [ ] `DEV-579-SHARED-RECIPIENT-PARSE-API`：ERP 客户档案与员工小程序对既有五类收货文本调用同一个 `POST /api/customer-recipient/parse`，均得到相同 `recipient_name/phone/address`；代码检查确认小程序和 Vue 页面没有复制地址解析正则或建立第二个解析接口。
- [ ] ERP 登录用户与具备 `customers.read` 的员工小程序令牌可解析；无令牌、客户账号、缺权限、空文本、超长文本、格式错误分别返回明确 4xx。接口不创建或更新客户，数据库和操作日志不记录粘贴原文。
- [ ] `DEV-579-MINIAPP-CUSTOMER-PASTE`：从“简易 ERP → 客户维护”新增和编辑客户时可粘贴收货信息；从录单页新增或维护客户时同样可用。成功后联系人、联系电话、联系地址正确回填，客户名和其他已填字段不被意外覆盖，三个结果字段仍可手工修改；切换客户或修改粘贴原文后，上一请求的迟到成功/失败不更新字段或错误提示。小程序保存的联系电话在 ERP 可见，ERP 再保存 phone-only 客户后电话仍保留。
- [ ] 解析失败时当前表单完全不变；解析中不能重复提交，切换客户、关闭编辑器或重新打开后，旧请求的迟到响应不能写入新客户。最终保存仍执行 PR-569 的负责人和 `customers.write` 权限，合法保存写操作日志，单独解析不写客户变更日志。
- [ ] `DEV-579-DOCS-ACCEPTANCE` 的定向 Go、Vue、miniapp 测试、类型检查、构建、支持合同与格式检查通过，证据记录在 `orderapp-remote/docs/acceptance/2026-08-04-miniapp-customer-address-portal-external-user.md`；`REV-579-MINIAPP-CUSTOMER-ADDRESS-PASTE` 等待 Van 在 development 手工验收两处入口。

### K50. 客户门户外部账号关联与 ERP 工作台授权分离（PR-580-CUSTOMER-PORTAL-EXTERNAL-USER-CAPABILITY-TEMPLATE）
- [ ] `DEV-580-EXTERNAL-ACCOUNT-WORKBENCH-SEPARATION`：客户 active 且门户已开通、模板为零售商城/其他非工作台模板或模板键为空时，可创建外部账号、列表读取、重置密码、启停登录；设置密码并启用后可从客户小程序密码登录，不再提示 `ERP workbench unavailable for capability template`。门户未开通/已关闭或客户停用时，创建、重置和启用明确提示门户未启用；关闭门户后仍可禁用已有账号。
- [ ] `DEV-580-ERP-SESSION-GATE`：同一非工作台、空模板和未知模板外部账号通过 ERP 密码登录返回 403，不签发 `login_sessions`；预置的旧 ERP token 在模板从工作台降级后立即失效。明确暴露工作台的外部账号仍可通过密码登录 ERP，内部员工密码登录及预置有效验证码兼容登录保持可用；渠道客户短信登录统一拒绝且不消费验证码。非工作台账号的客户小程序密码登录仍成功。
- [ ] `DEV-580-AUTH-BOOTSTRAP-HARDENING`：匿名和已登录请求调用旧 `/api/auth/password/set` 均不能创建员工、写密码或解禁账号；未配置真实短信发送通道时 `/api/auth/sms/send` 返回服务不可用，响应不含 code，且员工、验证码表均无新增。未知或停用手机号即使伪造/预置 code 也不能自动建员、签发 session 或提前消费验证码；既有 active 内部员工的预置有效 code 回归仍可登录。管理员密码重置/启停对内部员工保持可用，对 `channel_customer` 拒绝且不写成功审计。
- [ ] 上述账号进入 ERP 客户工作台时按无有效工作台绑定返回 `customer ERP binding not found` 或 403，不能读取客户工作台数据。显式激活 ERP 工作台绑定时，零售商城、非工作台和未知模板仍返回 `ERP workbench unavailable for capability template`，且不写工作台授权或隐藏角色。
- [ ] `DEV-580-EXTERNAL-USER-PERMISSION`：外部用户列表 GET 要求 `customers.read`；创建、重置密码和启停登录要求 `customers.write`。只具备库存权限不能维护外部用户，其他 `/api/customer-fulfillment` 库存接口仍要求 `stock.read/write`。
- [ ] `DEV-580-EXTERNAL-USER-AUDIT`：创建外部账号、重置密码和启停登录分别在同一事务写入 `customer_external_user` 业务操作日志，页面显示“客户管理 / 客户门户配置”、外部用户名、已认证操作者和可读动作；伪造 `X-User` 不改变操作者，密码明文及密码哈希不出现在任何日志字段。审计失败时业务修改回滚，拒绝请求不产生成功日志。
- [ ] `DEV-580-MINI-SESSION-REVOCATION`：外部账号通过密码和手机号授权分别登录小程序后，停用账号、替换 active 账号、停用客户或关闭门户都会让旧 token 下一次读取立即失败；之后恢复配置也不能自动复活旧 token，重新登录后才恢复。普通微信人工 approved 绑定与内部员工小程序登录不被误伤。
- [ ] `DEV-580-ACTIVE-BINDING-MUTATION`：把外部账号从客户 A 替换到 B 后，A 的历史 inactive 行仍可查看，但 A 调用重置密码或启停登录必须拒绝；B 的密码、登录状态及 A/B 操作日志不变。B 当前 active 行仍可正常维护。
- [ ] 同一外部账号不能 active 关联第二个客户；账号、客户或关联停用后不能继续登录。本需求不新增数据库字段或迁移。
- [ ] `DEV-580-DOCS-ACCEPTANCE` 的隔离 PostgreSQL、应用、HTTP、权限与支持合同测试通过，证据记录在同一合并验收文件；`REV-580-CUSTOMER-PORTAL-EXTERNAL-USER-CAPABILITY-TEMPLATE` 等待 Van 在 development 验收外部账号登录和 ERP 工作台隔离。

### K51. 商品行业字段与功能自选分组模板（PR-584-PRODUCT-MULTI-GROUP-TEMPLATES）
- [ ] 商品档案可同时选择两份行业字段模板，读写 `industry_field_template_ids` 有序数组并兼容旧标量；字段并集、同名键合并、已有值保留、移除单份模板和清空全部模板行为正确，保存及审计原子完成。
- [ ] 分组模板页只维护模板与分类树，不显示或保存“功能引用”；商品档案、物料档案、生产 BOM、仓库库存分别在自己的页面多选并保存所用模板，刷新后顺序与选择保持一致，清空选择后平铺且不能移动归类。模板保存和历史模板侧引用请求不能覆盖功能选择；商品档案可同时选择“商品-咖啡豆”和“商品-挂耳”。
- [ ] 商品档案同时展示所有已选模板的空/非空大类和小类，未选模板不出现，每个商品只出现一次且只有一个全局未分类；没有选择模板时平铺且隐藏分类、折叠和移动入口。取消选择不删除模板、分类、商品、既有归类或历史快照，操作日志可追溯。
- [ ] 商品价格表不提供自己的分组模板选择；商品档案选中咖啡豆、挂耳两份模板后，价格表商品类型仅出现与这两份模板一一对应的两个选项且顺序一致。取消一份后对应类型退出，历史 `price_list` 引用不产生额外类型，已发布快照不回改。
- [ ] 收起大类会隐藏全部后代分类标题和商品，重新展开恢复子类原折叠状态、分页与勾选状态；同级分类和未分类不受影响。
- [ ] Go 单元/API/PostgreSQL、Vue 定向/完整测试、Vite 构建、手册守卫和独立复审通过；合入 `develop` 并部署 development，production 不操作，Van 验收待办。

### K52. 价格试算主商品选择与生产 BOM 单展开分页（PR-588-PRICING-TRIAL-PARENT-PICKER-BOM-ACCORDION）
- [ ] 打开商品价格管理的价格试算并点击商品选择框，只显示启用主商品；同一主商品派生出的 227g、454g、磅、盒装等子规格 SKU 不作为独立候选。输入子 SKU 名称、规格或编码时不得新增一条子 SKU 商品候选，选择结果仍是所属主商品。
- [ ] 商品候选包含多种类型时，选择框显示与录单一致的 `全部 / 熟豆 / 挂耳 / 生豆 / 速溶咖啡` 点击过滤；类型和关键词可组合使用，点击主商品立即选中并关闭候选。当前只有一种可用类型时不额外显示类型按钮行。
- [ ] “生产 BOM列表”标题下不再显示“生产端主档案 / 归类保存到接口”的描述。前往分组模板、分类与移动模板、移动到分类、目标分类、已选数量和设置分组模板等分组处理在桌面端为一行，窄屏最多两行，不再占用截图中的大块竖向空间。
- [ ] 进入有多份已选模板的生产 BOM 列表时，只展开第一顶层组，其余模板和全局 `未分类` 收起；点击第二组后第二组展开且第一组及其他顶层组自动收起，任一时刻只有一个顶层组展开。
- [ ] 分页总条数只等于当前展开顶层组中的 BOM 数，不把模板、大类、小类标题或收起组计入分页；第一页能直接看到当前展开组的 BOM 行。切换顶层组、状态或搜索词后回到第 1 页；未选择模板时平铺 BOM 列表仍可正常分页。
- [ ] 移动到分类继续调用既有业务分组归类接口并写原有操作日志；商品类型过滤、分组展开收起和分页本身不写业务数据或新增日志类型。完成最小定向 RED/GREEN、前端构建和支持合同后合入 `develop` 并最轻量部署 development，不做浏览器或业务验证，等待 Van 人工验收；`main` 和 production 不操作。

### K53. 价格试算选择具体销售规格并与商品价格表统一（PR-589-PRICING-TRIAL-PRODUCT-SPECS）
- [x] 选择一个具有 227g、454g、礼盒 A、礼盒 B 等子规格的主商品后，“销售规格”只显示该父商品 active 且派生状态为空或 `active` 的具体子 SKU；`template_disabled`、`template_removed` 及其他父商品规格不出现。礼盒 A、礼盒 B 即使销售单位同为“盒”也必须保留两项，不按单位合并；全局单位和仅可换算单位不进入候选。
- [x] 首次选择主商品时默认选中 `default_sku_id` 对应的有效规格。切换主商品或客户时清空旧商品和规格；切换销售规格时保留新规格，切换价格模板时保留当前主商品和规格；上述切换均清空 BOM、工艺路线和旧结果，在途旧试算响应不得回填新上下文。没有有效子规格时与价格表一致回退有效主商品自身及其权威销售单位。
- [x] 单次试算请求沿用 `product_id + quote_unit`，其中 `product_id` 为所选具体 SKU，`quote_unit` 为该 SKU 权威销售单位；界面不再提供独立任意销售单位选择，也不新增 API 字段。costing API 能按原合同绑定并回显具体 SKU/单位，现有 SKU → 父商品 BOM 回退保持可用。
- [x] 对同一具体 SKU 使用相同价格计算模板、客户、BOM、工艺路线和临时参数时，价格试算与商品价格表的报价单位、BOM/工序成本口径及最终单价一致；同单位兄弟 SKU 的身份、换算和结果互不串用。
- [x] 试算和选择均为只读，不写模板、商品价格表、订单或操作日志；已发布价格表、历史订单和历史价格快照不回算、不改写。本需求不增加独立防伪或跨父归属校验。
- [x] PR-589 前端定向/全量测试、costing API、支持合同和 Vite build 通过，合入 `develop` 并最轻量部署 development；Van 于 2026-08-10 确认验收完成，`main` 和 production 未由 PR-589 操作。

### K54. 订单非空文本字段兼容与小程序草稿提交安全（PR-590-MINIAPP-BLANK-TRACKING-SUBMIT）
- [ ] production 员工小程序恢复一份有效服务器录单草稿，不填写物流方式、物流单号、备注和快递费直接提交；服务端成功生成一张正式订单，对应 `orders` 非空文本字段保存为空字符串而非 SQL `NULL`，不再返回数据库内部错误。
- [ ] `SaveOrder` 新建/完整更新和 `updateOrderHeader` 都不把空 `ship_method`、`ship_tracking_no`、`notes`、`express_fee` 转换为 SQL `NULL`；订单明细空 `unit` / `spec` 指针通过 `notNullTextPtr` 保存为空字符串。填写非空值时仍按原值及既有物流明细规则保存，不影响 ERP 网页录单或订单编辑。
- [ ] 批量失效订单未填写原因时，`VoidMany` 把 `void_reason` 保存为空字符串；订单行内更新清空备注时把 `notes` 保存为空字符串，两条路径均不违反非空约束且原有操作语义不变。
- [ ] 正式提交的订单头、明细、物流、当前员工草稿清理和原有操作日志处于同一事务；模拟任一步失败或提交失败后不得留下半张订单，重新进入录单仍能恢复原草稿。只有完整提交成功后草稿才消失。
- [ ] 操作日志继续显示既有订单创建/更新与订单草稿清除记录，不新增日志类型，不记录 SQL 内部信息；失败请求不得产生成功订单或成功清草稿日志。
- [x] 销售仓储定向测试、支持合同和受影响范围门禁通过；已合入 `develop@062c9be73bc3` 并部署 development，再从最新 `main` 合入已验证 develop、通过 production 预检并部署 `main@91cda66ad151`。两套环境均记录源码备份、回滚镜像和外部登录 HTTP 200；小程序源码、请求合同和微信上传/提审/发布不在变更范围内。

### K55. 小程序上拉品牌标识与当前账号防自停用（PR-591-MINI-PULL-BRAND-SELF-LOGIN-GUARD）
- [ ] 小程序除瞬时启动路由外的 13 个实际业务页面都接入同一品牌组件；正常打开、浏览内容和到达页面正常底部时看不到标识，继续向上拉后才出现 `Drived By` 与棵凡四字字标。
- [ ] 字标是透明背景、银灰单色、横向紧凑且资源已压缩；组件不使用 fixed，不遮挡表单、商品/订单列表、键盘、底部导航和安全区。固定底栏页面与无底栏页面都能在继续上拉时完整显示。
- [ ] 当前管理员在 ERP 员工维护页不能关闭当前登录账号自身，开关禁用并显示原因；直接调用账号启停 API 关闭自身返回 409 `cannot disable current account`，登录状态、当前会话和业务成功审计均不变化。
- [ ] BasicAuth 运维恢复通道仍可恢复有效内部员工；管理员仍可启停其他员工，并可保持或恢复自己的启用状态。拒绝自停用不会影响其他账号维护、密码重置或权限管理。
- [x] production 目标内部员工账号已通过现有账号启停 API 即时恢复，原有审计可追溯，需求与验收证据未记录账号、密码或其他个人信息。
- [x] Go API、Vue/Vite、miniapp 定向与全量测试、透明资源检查、构建和 PR-591 支持合同通过；功能已合入并部署 `develop@d4e144b0f0709f8dfa7a88e0c3c85ef4028905b8`，开发小程序固定包已同步且外部 smoke HTTP 200。微信上传、提审、发布和 production 代码部署未执行，`REV-591-MINI-PULL-BRAND-SELF-LOGIN-GUARD` 等待 Van 人工验收。

### K56. BOM 原料损耗按总投料损耗率反算（PR-592-BOM-LOSS-GROSS-INPUT）
- [ ] 初晓 1kg 使用当前有效 BOM 和“熟豆24磅模板-正常-418”试算时，哥伦比亚行显示 BOM组成 25%、原料损耗 19.5%、损耗后用量 31.0559%，单价 78元/kg，折算成本四舍五入为 24.22元。
- [ ] BOM 物料成本、价格试算、新生产计划、组件需求、WIP、领料和新工单均只读取 BOM `material_loss_rate`，公式统一为 `净用量 ÷ (1 - 原料损耗率)`；0 损耗等于净用量，整体产出率、商品预期损耗和价格模板损耗不得再次放大。
- [ ] 新冻结物料快照写入 `loss_calculation_mode=yield_denominator`；历史无标记快照和已冻结 `loss_calculation_mode=additive` 快照保持各自原数值和原算法，历史价格表、订单、工单和库存流水不回算。
- [ ] 后端成本/生产定向与全量单元测试、前端定向/全量测试和 Vite build 通过后合入并部署 development；不做浏览器、API 或业务验证，由 Van 人工验收，production 不部署。
