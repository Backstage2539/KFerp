# HOW_TO_WORKFLOW - 交付流程（需求→开发→测试→验收）

> 目的：把每个需求的推进过程“显性化、可追溯、可验收”，避免只靠口头/聊天记录。

## 总原则（Van 定义，必须执行）
每个需求点完成必须经过：
1) **单元测试（Unit Tests）**
2) **API 层测试（接口级验证，尽量不走 UI）**
3) **操作手册更新（用户操作变化必须同步更新手册）**
4) **需求验收（按 REQUIREMENTS / ACCEPTANCE_TESTS 对照勾选，产出证据）**

前端相关新增原则（强制）：
5) **凡涉及前端改动，统一采用 Vue 3 + Vite 架构**
6) **若需求当前落在 html 模板实现，改动时需同步迁移到新前端架构（至少完成同功能入口迁移）**

## 操作手册规则（强制）
- 开发时只要新增功能、改变字段、改变按钮/入口、改变权限、改变导入导出、改变异常处理或改变用户流程，就必须更新操作手册。
- 单个大功能必须有一个独立操作手册；小改动更新所属大功能手册，不把多个大功能混在一个长文档里。
- 操作手册必须同时覆盖：入口路径、适用角色、前置条件、标准操作步骤、结果校验、常见异常/失败处理、关联单据或数据影响。
- 现有操作手册要持续查缺补漏。每次触碰某个功能时，先检查该功能手册是否缺少新入口、新字段、流程顺序、权限、导入导出或排障说明，再开发/验收。
- 手册更新是同一个需求的一部分，不是上线后的可选补充。验收证据必须包含手册文件路径或前端手册入口。
- 手册总索引为 `OPERATION_MANUALS.md`；新大功能手册优先采用 `OP_MANUAL_<FEATURE>.md` 命名。已有 `*-user-manual.md` 或 Vue/Vite 手册页的功能，可沿用既有手册，但必须在总索引登记，并同步到 `orderapp-remote/docs/` 或前端手册入口。

## 工作流（PR/DEV 进度表 + Superpower 测试证据）
在系统 UI 中必须维护 PR/DEV，任何时候 Van 打开就能看到当前产品需求和开发进度。UT/API/REV 不再按旧 UI 表流程强制录入，测试和验收证据按 Superpower/TDD 流程保留在测试输出、验收文档和最终说明中。

1) **产品需求表**（PR-xxx）
- 用户/业务视角的“功能块”描述
- 包含：目标、范围、验收口径、验收人（例如 VA）

2) **开发需求表**（DEV-xxx）
- 从产品需求拆解出的具体开发任务/技术实现点
- 颗粒度：可以独立实现、独立测试、独立部署

3) **单元/API/验收证据**
- 不再强制写入 UT/API/REV UI 表
- 仍必须先写测试、确认 RED、实现、确认 GREEN
- 证据记录在测试命令输出、相关测试文件、验收文档和最终交付说明中

## 推荐执行顺序（每个需求都按此闭环）
1. 产品需求表：写清楚 PR（功能块 + 验收口径 + 验收人）
2. 开发需求表：拆 DEV（可实现/可测试/可部署）
3. 按 Superpower/TDD 设计测试：为每个 DEV 写清楚应覆盖的单元/API/集成行为
4. 先编写单元测试代码和 API 测试代码，并运行确认新测试在实现前失败（RED）
5. 实现代码：只实现让测试通过所需的业务/API/前端代码
6. 操作手册：新增或更新对应大功能手册，并补齐现有手册缺口
7. 单元测试：跑通对应测试并记录命令、结果、关键日志/commit 等证据
8. API 测试：跑通 handler/API 级验证并记录请求、响应断言和证据
9. 需求审核：按 PR/ACCEPTANCE_TESTS 更新验收文档或最终说明，证据包含手册路径
10. 部署上线：仅在测试、API、手册和验收证据完整后执行，并记录部署版本与验证结果

## 证据要求（最低标准）
- 对于“操作手册类”需求：
  - `OPERATION_MANUALS.md` 或对应 `OP_MANUAL_<FEATURE>.md` 已更新
  - `orderapp-remote/docs/` 中的线上展示副本已同步
  - 如影响前端操作入口，Vue/Vite 前端手册入口或相关页面帮助文本已同步
- 对于“页面/文档类”需求：
  - HTTP 状态码 200（curl 输出）
  - 页面内容包含关键字（例如文件名/标题）
- 对于“业务逻辑类”需求：
  - 单测结果
  - API 返回断言（优先 `httptest` / handler 级测试；必要时补 curl 请求+响应片段）
  - 必要时补充截图/录屏

## 架构合规检查（前端需求必做）
- 先判断需求是否触碰用户可见页面。
- 若触碰页面且当前仍在 `templates/*.html`，必须先迁移到 Vue 3 + Vite 页面，再实现新功能。
- 新用户可见页面必须通过 JSON API 获取数据，不新增 HTML template 作为目标实现。
- 旧 HTML template 只能作为过渡期兼容入口、重定向或只读遗留页面，不再承载新功能。
