# HOW_TO_WORKFLOW - 交付流程（需求→开发→测试→验收）

> 目的：把每个需求的推进过程“显性化、可追溯、可验收”，避免只靠口头/聊天记录。

## 总原则（Van 定义，必须执行）
每个需求点完成必须经过：
1) **单元测试（Unit Tests）**
2) **API 层测试（接口级验证，尽量不走 UI）**
3) **需求验收（按 REQUIREMENTS / ACCEPTANCE_TESTS 对照勾选，产出证据）**

前端相关新增原则（强制）：
4) **凡涉及前端改动，统一采用 Vue 3 + Vite 架构**
5) **若需求当前落在 html 模板实现，改动时需同步迁移到新前端架构（至少完成同功能入口迁移）**

## 工作流（5张表）
在系统 UI 中维护 5 张表，任何时候 Van 打开就能看到进度与证据：

1) **产品需求表**（PR-xxx）
- 用户/业务视角的“功能块”描述
- 包含：目标、范围、验收口径、验收人（例如 VA）

2) **开发需求表**（DEV-xxx）
- 从产品需求拆解出的具体开发任务/技术实现点
- 颗粒度：可以独立实现、独立测试、独立部署

3) **单元测试表**（UT-xxx）
- 每个开发需求对应的单测清单
- 记录：通过/不通过、日志/截图/commit 等证据

4) **API 测试表**（API-xxx）
- 每个开发需求对应的接口级验证清单
- 优先：curl/脚本/自动化验证，少走 UI

5) **需求审核表**（REV-xxx）
- 需求完成后逐条审核：通过/不通过/证据
- 验收人签字（例如 VA）

## 推荐执行顺序（每个需求都按此闭环）
1. 产品需求表：写清楚 PR（功能块 + 验收口径 + 验收人）
2. 开发需求表：拆 DEV（可实现/可测试/可部署）
3. 实现代码
4. 单元测试：补 UT（并记录证据）
5. API 测试：补 API（并记录证据）
6. 部署上线
7. 需求审核：补 REV（验收人确认通过/不通过 + 证据）

## 证据要求（最低标准）
- 对于“页面/文档类”需求：
  - HTTP 状态码 200（curl 输出）
  - 页面内容包含关键字（例如文件名/标题）
- 对于“业务逻辑类”需求：
  - 单测结果
  - API 返回断言（请求+响应片段）
  - 必要时补充截图/录屏
