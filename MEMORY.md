# MEMORY.md

## Van (profile)
- Name: Van (Asia/Shanghai)
- Prefers bullet points; wants a crisp assistant.
- Coding: Go + VB in VS Code; ~8 years backend.
- Runs a coffee roastery (ToB/wholesale to coffee shops).

## Current direction (2026-02)
- 目标不变：替代 Excel，做“订单→生产”的轻量协作系统（中文数据）。
- 产品方向补充（2026-05-02）：KFerp 不能只按 Van 当前公司的流程做定制系统。Van 当前是小规模纳税人，但未来系统希望开源给不同类型的咖啡烘焙厂使用，甚至可服务相邻的鲜果加工厂；产品主身份仍是“咖啡品类 ERP”。后续财务、税务、库存、生产、成本模型必须可配置、行业化，不要硬编码 Van 当前纳税人身份或单一业务流程。
- 财务模块方向（2026-05-02）：第一阶段优先做“老板看本月利润/税费 + 月度结账/经营报告”。默认结账模式为强锁账：结账后历史月份不能直接改，必须走调整记录；允许隐藏高级开关在强锁账/轻确认之间切换，但仅指定白名单用户可见且后端必须校验、记录审计日志。
- 财务月结调整规则（2026-05-02）：第一版历史月份调整只做金额调整，不强制关联原始订单/费用单/生产批次；调整记录必须包含月份、类型、金额、原因、操作人和审计痕迹。来源单据级调整留到后续版本。
- 财务月报形态（2026-05-02）：第一版月度经营报告采用“老板简报 + 明细报表”并存；默认打开一页式简报，展示收入、成本、费用、利润、税费、异常，明细作为展开内容或 Tab 二级内容。
- 财务月报导出（2026-05-02）：第一版月度经营报告要支持 PDF 和 Excel 导出，但系统内查看仍是主体验。PDF 面向老板/合伙人，呈现简报和关键表；Excel 面向财务/代账，包含收入、成本、费用、税费估算、调整、异常等结构化明细工作表。
- 财务税费估算（2026-05-02）：第一版采用中等深度估算，支持小规模/一般纳税人两套口径，展示销项税、进项税、应纳增值税估算、附加税估算、企业所得税预估。产品必须标注为经营管理估算，不作为正式申报结果。
- 目标客户扩展（2026-05-02）：除咖啡烘焙厂外，KFerp 还应支持咖啡贸易商、咖啡壳豆/生豆加工厂等客户类型。这两类客户重点不在烘焙配方，而在出入库、批次归属、结算、应收应付、毛利和月度财务报告。
- 企业所得税估算（2026-05-02）：第一版采用中等估算，支持小型微利企业优惠估算，不只是利润乘固定税率；仍必须标注为经营管理估算，月结前允许人工确认/调整。
- 壳豆/生豆加工厂业务模型（2026-05-02）：产品模型需同时支持自购自销和受托加工，但第一版默认优先自购自销；受托加工保留为后续扩展，避免初始设计堵死货权归属、加工费收入、客户货库存等能力。
- 财务月结一期实现状态（2026-05-02）：功能在分支 `codex/finance-monthly-closing-20260502` 完成并通过本地验证，包含财务首页、费用管理、月度结账、经营报告、财务设置、财务手册、PDF/Excel 导出、强锁账、金额调整、权限和 PR/DEV/UT/API/REV 种子。锁账模式白名单可由数据库或 `FINANCE_CLOSE_MODE_ADMIN_USERS` 环境变量配置。
- 部署方案已更新：不再走 Synology NAS/DSM，改为云服务器部署。
- 发布环境规则（2026-05-03）：未来线上正式发布走 `production` 正式环境，默认命令 `./deploy_orderapp.sh`，要求 `main/origin/main`，目标 `/opt/stacks/erp-production`；开发环境保留旧链路，命令 `./deploy_orderapp.sh development`，要求 `develop/origin/develop`，目标 `/opt/stacks/erp`。不要再把 develop 发布当作正式线上发布。
- 公网入口切换规则（2026-05-03）：`erp.qacoohee.com` 由当前绑定 443 的 Caddy 决定。正式环境 Caddy 在 `/opt/stacks/erp-production`（`erp_prod_caddy`），开发环境 Caddy 在 `/opt/stacks/erp`（`erp_caddy`）。切公网只停非目标环境 `caddy` 并启动目标环境 `caddy`，不要为了切换停止两边的 `orderapp/postgres`。已把脚本固化为 `./deploy_orderapp.sh --switch-public production|development`；手动兜底为 production: 停 `/opt/stacks/erp` caddy、起 `/opt/stacks/erp-production` caddy；development 反向操作。
- 现网：1.12.242.58（SSH 22，root），已为 JJ 加入公钥，允许直接登录做验收与排查。
- 现网运行形态：Docker（erp_orderapp + erp_caddy + erp_postgres）。Postgres 用户/库：nocodb/nocodb；业务 schema：p2rms15pepb5ciz。
- 交付流程约束（Van 要求，后续每个需求点都必须执行）：
  1) 单元测试（unit tests）
  2) API 层测试（接口级验证，尽量不走 UI）
  3) 操作手册更新（凡改变用户操作流程、入口、字段、权限、导入导出或异常处理，都必须同步更新手册）
  4) 需求验收（按 REQUIREMENTS/ACCEPTANCE_TESTS 对照打勾，产出通过/不通过与证据）
- 操作手册约束（Van 2026-05-07）：开发时必须更新操作手册；单个大功能必须有一个独立操作手册；现有操作手册要查缺补漏，REV 证据必须包含手册路径或前端手册入口。
- 需求管理产出物（Van 14:05）：给到“产品需求”后，JJ 必须在 UI 中维护 5 张表，供 Van 随时查看：
  1) 产品需求表：从原始描述抽象/结构化后的产品需求
  2) 开发需求表：从产品需求拆解出的开发任务/技术实现点
  3) 单元测试表：每个开发需求对应的单测清单/结果/证据
  4) API 测试表：每个开发需求对应的接口测试清单/结果/证据
  5) 需求审核表：需求完成后逐条审核是否实现（通过/不通过/证据）
  - 工作流：产品需求 →（抽象入表）→ 拆解开发需求 →（实现）→ 单元测试/ API 测试入表 → 完成后做需求审核入表
  - 测试数据：验收/测试阶段允许 JJ 自造数据（如烘焙机器、载量区间、库存/批次）。

- Van 在 2026-02-17 新确认的“强制执行开发流程”（必须严格按顺序）：
  1) 收到产品需求后，先拆解为开发需求，并写入开发需求表（DEV）。
  2) 按开发需求逐条开发：
     - 2.1 先编写单元测试代码，并在单元测试表（UT）新增条目；同时编写 API 测试代码，并在 API 测试表（API）新增条目。
     - 2.2 再进行代码开发实现。
     - 2.3 单元测试通过后，更新 UT 表状态（含证据）。
     - 2.4 API 测试通过后，更新 API 表状态（含证据）。
  3) 完成后通知 Van 进行产品需求验收。
  4) Van 在需求审核表（REV）验收通过后，产品需求表（PR）状态自动更新为 done。

## 技术栈（2026-02-18 更新）

### React 前端（BOM 配方维护页面）
- **框架**：React 18 + TypeScript + Vite
- **状态管理**：TanStack Query (React Query)
- **HTTP 客户端**：Axios
- **样式**：Tailwind CSS
- **访问路径**：`/app/bom-react`
- **源码位置**：`orderapp-remote/frontend/`

## KFerp 产品决策（2026-04-30）
- 贴牌/客户账号未来发布的豆单必须是客户自己的快照：发布时锁定内容和价格，不随棵凡官方豆单更新自动改价。
- 客户需要手动复制棵凡官方豆单价格来源，并复制自己的历史样式配置后发布新版。
- 2026-05-02：Van 明确喜欢“仓库库存”这种一个页面多功能的工作台样式。后续涉及一个页面内多功能操作时，优先采用左侧分类/主区域表单或列表/右侧抽屉处理细节的直观布局。
