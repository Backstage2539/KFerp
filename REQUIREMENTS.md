# orderapp 业务需求（精简版）

> 范围：描述“要做什么”和业务规则；不记录修 bug 的过程。

## 1. 目标与范围
- 建一个轻量的订单/档案/审计系统（Web）。
- 核心模块：
  - 录单
  - 订单列表、订单详情
  - 作废/恢复（代替删除）
  - 商品档案 + 阶梯价
  - 客户档案（含默认值、快速录入、复制地址）
  - 操作审计日志（全量留痕）

## 2. 计价与单位（关键业务规则）
### 2.1 阶梯价
- 阶梯价单位：**元/磅（lb）**。
- 换算：**1lb = 454g**。
- 阶梯匹配：按“**单行**”的折算磅数匹配阶梯档位。

### 2.2 录单明细输入（规格 + 件数）
- 每一行明细代表“一个商品 + 一个规格”。
- 输入字段：
  - **规格(g)**：下拉（常用规格），默认 **454g**。
  - **数量(件)**：只能输入整数（不允许小数）。
- 计算：
  - 总重量(g) = 规格(g) × 数量(件)
  - 总重量(lb) = 总重量(g) ÷ 454

### 2.3 行小计与合计
- 单价：默认来自自动匹配的阶梯价（元/磅）。
- 行小计：`line_total = unit_price(元/磅) × 总重量(lb)`
- 低于最低档：
  - 必须提示（非阻断）
  - 仍允许下单
  - 默认按“最低档单价”计算
  - 操作员可切换“手动单价（元/磅）”覆盖

## 3. 订单（Orders）
### 3.1 页面
- 录单：`/app/order`
- 订单列表：`/app/orders`
- 订单详情：`/app/orders/:id`

### 3.2 删除/作废规则
- 订单不允许真删除。
- 通过“作废/恢复”实现撤销：
  - 作废后列表默认不显示
  - 可筛选查看作废/全部

### 3.3 列表快速编辑
- 订单列表支持快速修改：收款状态、发货状态、备注。
- 所有修改必须写入审计日志。

## 4. 商品档案（Products）
- 维护商品与阶梯价，用于录单自动计价。
- 商品下拉必须支持：点击展开、输入过滤、×清空、拼音/首字母匹配。
- 支持打印/导出报价（PDF）。

## 5. 客户档案（Customers）
### 5.1 客户字段
- 客户至少包含：
  - 客户名（正式）
  - 联系人（姓名）
  - 电话
  - 地址
  - 启用/停用（active）
  - 默认来源（default_source_id）
  - 默认订单类型（default_order_type_id）
  - 原始信息（raw_name，仅保留，不影响展示）

### 5.2 客户维护
- `/app/customers`：列表内可直接编辑并保存（写日志）。
- 支持停用/“删除”：
  - 客户删除采用安全策略（不真删，改为停用）。
- 支持“一键复制发货地址”：复制格式固定为：
  - 姓名：xxx（联系人为空则用客户名）
  - 电话：xxx
  - 地址：xxx

### 5.3 客户快速录入
- 客户新建/编辑页支持“快速粘贴”一整段文本，一键解析到：姓名/电话/地址。

### 5.4 客户联动录单
- 录单选择客户后：
  - 自动**覆盖**录单的“来源/订单类型”（来自客户档案默认值）
- 录单客户下拉：仅显示启用客户（active=true）。

## 6. 操作审计日志（Audit）
- 原则：**所有关键操作都必须留痕**。
- 查询页面：按日期范围 + 关键字 + 类型查询。
- 展示要求：
  - old/new 尽可能翻译为人可读内容
  - 对象要显示可读名称并可跳转（如订单号、商品名、客户名）

## 7. 财务月度经营结账中心（PR-FIN-001，规划中）
- 第一版目标：老板能按月查看收入、主营成本、毛利、费用、税费估算和经营净利估算，并完成月度结账和经营报告导出。
- 适用企业类型：
  - 咖啡烘焙厂
  - 咖啡贸易商
  - 咖啡壳豆/生豆加工厂
  - 综合型企业
- 财务首页默认显示本月，所有指标支持下钻来源明细。
- 月度结账默认使用强锁账：结账后历史月份不能直接修改，修正走金额调整记录。
- 结账模式支持隐藏高级开关，但仅白名单用户可见且后端必须校验并记录审计日志。
- 税费为经营管理估算，不作为正式申报结果；支持小规模/一般纳税人两套口径和小型微利企业所得税优惠估算。
- 月度经营报告默认打开老板简报，明细以 Tab 或展开区呈现。
- 报告支持 PDF 和 Excel 导出。

## 8. 操作手册强制治理（PR-DOCS-001）
- 开发时只要改变用户操作流程、入口、字段、权限、导入导出或异常处理，必须同步更新操作手册。
- 单个大功能必须有一个独立操作手册，命名为 `OP_MANUAL_<FEATURE>.md`。
- 小改动更新所属大功能手册，不能把多个大功能混写在一个长文档里。
- 每次触碰功能时，要检查现有手册是否缺入口、缺字段、缺步骤、缺权限说明、缺导入导出说明、缺排障说明。
- 需求审核证据必须包含手册文件路径或前端手册入口。
- 部署时必须同步 `OPERATION_MANUALS.md` 和 `OP_MANUAL_*.md` 到线上 `/docs`。
