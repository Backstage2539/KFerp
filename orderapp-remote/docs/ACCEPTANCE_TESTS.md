# orderapp 验收清单与测试用例（截至 2026-02-13）

> 目标：给日常验收/回归测试用。每次上线前按此清单抽测。

## A. 录单（/app/order）

### A1. 客户选择与联动
- [ ] 客户下拉仅显示 active=true 的客户（停用客户不出现）。
- [ ] 客户选择框支持客户名、拼音、首字母模糊搜索。
- [ ] 选择客户后：来源、订单类型自动填充并**覆盖**原值。
- [ ] 新录订单默认付款状态为已付款、发货状态为未发货。
- [ ] 录单页点击“新增客户”从右侧打开抽屉，不离开当前订单；粘贴“姓名/电话/地址”后可自动识别并填充，默认来源为微信、订单类型为批发，保存后自动选中新客户。

### A2. 明细输入（规格 + 件数）
- [ ] 选择商品后：规格默认该商品第一个价格梯度的规格。
- [ ] 规格下拉包含 36g、80g、100g、227g、454g、500g、1000g、2.5kg。
- [ ] 数量(件)只能按整数处理（输入小数时，系统行为符合预期：建议前端拦截/或后端拒绝；以实际上线行为为准）。
- [ ] 总重量实时显示：xxx g / yy lb。

### A3. 自动匹配阶梯价
- 用例：选择一个有阶梯价的商品。
  - [ ] 页面展示该商品所有规格/数量梯度和单价。
  - [ ] 规格 454g、数量 1：能算出单价/小计。
  - [ ] 规格 454g、数量 10：匹配到更高档（若存在）。

### A4. 低于最低档（非阻断）
- 用例：规格 50g、数量 1（总 lb 很小）。
  - [ ] 页面出现“低于最低档”的提示。
  - [ ] 仍然可以保存订单。
  - [ ] 默认按最低档单价计算。

### A5. 手动单价覆盖
- 用例：对同一行选择“手动单价”，输入元/磅。
  - [ ] 小计按手动单价重新计算。
  - [ ] 保存后订单金额与页面一致。
  - [ ] 点击恢复自动价后，按当前商品/规格/数量重新匹配梯度价。

### A6. 保存
- [ ] 保存后跳转/提示“已保存订单号”。
- [ ] 保存订单时不进行顺丰发货处理，不生成顺丰发货 Excel 下载入口。

---

## B. 订单列表（/app/orders）

### B1. 查询/筛选
- [ ] 支持关键字搜索。
- [ ] 支持日期范围筛选。
- [ ] 作废筛选：默认不显示作废单，可切换查看作废/全部。

### B2. 列表内联编辑 + 审计
- [ ] 修改收款状态/发货状态/备注后能保存成功。
- [ ] 修改会写入审计日志（/app/audit 可查询到）。

### B3. 可发货订单发货处理
- [ ] 点击“只看可发货”后列表筛选到生产完成、无需生产和库存待发货订单。
- [ ] 只有生产完成、无需生产或库存待发货订单可以勾选发货处理。
- [ ] 勾选一个或多个订单后，可生成顺丰发货 Excel 并展示下载入口。
- [ ] 下载的 Excel 使用服务器快递模板，收件人来自客户档案，寄件人默认来自发货人设置的默认寄件人。
- [ ] 本次生成前可以选择默认寄件人；已勾选订单中的某几单也可以单独选择寄件人，Excel 对应行使用对应寄件人资料。
- [ ] Excel 中包裹件数为 1，托寄物为茶叶，重量为 0.1kg，备注包含对应订单明细但不包含单价和小计。
- [ ] 生成 Excel 后页面展示发货批次号。
- [ ] 订单列表将寄件人和快递单号合并展示为“快递信息”，将收款、发货、生产和发票合并展示为“订单状态”。
- [ ] 点击订单号打开订单抽屉后，“快递信息”里没有“加入本次快递录单”选项；寄件人默认跟随顶部本次寄件人，并可直接选择其他寄件人。
- [ ] 点击订单号打开订单抽屉后，可以在抽屉中回填单个快递单号；订单列表顶部不再显示本批次单号输入框。
- [ ] 点击订单号打开订单抽屉后，可以在同一抽屉编辑并保存订单；列表和抽屉不再出现独立“编辑”跳转入口，保存后停留当前筛选分页。
- [ ] 回填快递单号后，订单列表显示该单号，发货状态为已发货。
- [ ] 上传顺丰寄件列表回传 Excel 后，系统按备注中的订单号回填对应订单快递单号，并将发货状态更新为已发货。

### B4. 发货人设置
- [ ] 发货人设置页面展示寄件人列表。
- [ ] 可以新增/编辑寄件人，并设置一个默认寄件人。

---

## C. 订单详情（/app/orders/:id）
- [ ] 页面能正常打开。
- [ ] 数量/单价/小计显示正常（无 %!f(*float64=...) 乱码）。
- [ ] 支持作废/恢复（并且不真删除）。

---

## D. 商品档案（/app/products）
- [ ] 商品搜索/筛选可用。
- [ ] 商品阶梯价可维护。
- [ ] 录单商品选择框直接输入过滤并选择商品；支持拼音/首字母匹配；页面不再出现单独商品搜索框。

---

## E. 客户档案（/app/customers）

### E1. 列表内编辑
- [ ] 客户名/联系人/电话/地址可在列表直接修改并保存。
- [ ] 客户公司名称、公司地址、联系电话可在客户录入/编辑界面保存；公司名称为空时销售单默认使用客户名。
- [ ] 默认来源、默认订单类型可在列表直接修改并保存。

### E2. 一键复制发货地址
- [ ] 点击“一键复制发货地址”后，剪贴板内容为：
  - 姓名：xxx（联系人为空则用客户名）
  - 电话：xxx
  - 地址：xxx

### E3. 删除（停用）
- [ ] 点击“删除”二次确认。
- [ ] 删除后客户被停用（active=false），录单客户下拉不再出现。
- [ ] 删除动作写入审计日志。

### E4. 快速粘贴解析（/app/customers/new 或 /:id）
- 用例：粘贴
  - `姓名：张三\n电话：13800138000\n地址：上海市...`
  - [ ] 一键解析后能填充联系人/电话/地址。

---

## F. 操作审计日志（/app/audit）
- [ ] 可按日期范围查询。
- [ ] 可按关键字查询。
- [ ] 客户 create/update/delete 能在日志中查到。

---

## G. 采购入库（/vue-shell?view=purchase）
- [ ] 能新增或更新供应商。
- [ ] 能用供应商和物料创建采购单。
- [ ] 对采购单收货后，收货记录展示库存批次号。
- [ ] 原料批次、库存流水和原料仓库存同步增加。
- [ ] 物料采购价更新为本次收货单价。

### G1. 原料入库（/vue-shell?view=materialReceipts）
- [ ] 物料选择在同一个下拉框内支持输入名称或编号后过滤候选项，不出现独立搜索框。
- [ ] 包装物料不会出现在原料入库候选项中。
- [ ] 选择过滤后的物料并提交入库后，返回原料批次号。

### G2. 库存作业（/vue-shell?view=stockOperations）
- [ ] 原料入库、WIP领退/转仓、成品转仓都能在物料/成品下拉框内直接输入并过滤候选项。
- [ ] 原料入库、WIP领退/转仓、成品转仓和库存调整四个页面使用一致的面板和网格排版，输入框高度、列宽和间距对齐。

---

## H. 用户权限与账号（/vue-shell?view=userPermissions）
- [ ] 管理员能为员工启用/停用登录。
- [ ] 停用员工无法密码登录或短信登录。
- [ ] 停用员工已有 Bearer token 不能继续访问接口。
- [ ] 用户权限页对未设置密码的员工显示“设置密码”，对已有密码的员工显示“重置密码”。
- [ ] 管理员设置或重置密码后，员工可以使用新密码登录。
- [ ] 用户名或手机号密码登录成功后自动进入系统，页面 API 请求携带 Bearer token。
- [ ] 登录成功后的首屏能直接看到默认展开的功能菜单，不需要刷新页面才出现功能入口。
- [ ] 手机从 `/app` 入口打开系统时，只需输入一次外层 `order` 认证和一次系统账号；登录成功、刷新、页面跳转和退出回登录都不跳出 `/app` 前缀。
- [ ] 旧 Bearer token、系统账号过期或 API 401 时，页面应回到系统登录或得到 JSON 401，不应反复弹出浏览器原生 `order` 密码框。
- [ ] admin 角色账号登录后能看到全权限菜单。
- [ ] 点击顶部“退出”后回到登录页，当前 token 被清除且会话被注销；直接打开系统地址不能绕过登录。

---

## I. 生产制造（/vue-shell?view=producePlan / produceRunning / qualityInspections）

### I1. 库存批次与库存不足
- [ ] 生产计划页面不再显示“物料需求计划”按钮或区块。
- [ ] 库存不足和库存充足分开展示；库存充足只提示，不能勾选进入生产计划。
- [ ] 库存不足（需生产）标题左侧提供唯一批量复选框；勾选即全选库存不足行，取消即全不选，单独勾选部分行时显示半选减号；顶部不再有“库存不足全选/全取消”“全选库存不足”“全取消”批量按钮。
- [ ] 录单保存时如果成品批次足够，页面提示可用批次；如果只有旧成品库存余额且没有 FP 批次，页面提示 `LEGACY-FP-商品ID-规格` 库存余额；确认使用后订单生产状态变为“库存待发货”。
- [ ] 录单保存时如果操作员拒绝使用现有批次，订单记录为待生产，生产计划即使看到物理库存也按库存不足缺口展示。
- [ ] 使用成品批次或历史库存余额的订单，生成顺丰发货 Excel 时不扣库存；回填快递单号标记已发货时扣减对应成品库存并写库存流水，重复回填不会重复扣减。
- [ ] 已发货但生产状态为空的历史订单不再进入生产计划；库存待发货的未发货预留会占用成品可用库存。
- [ ] 库存不足商品生成计划后，物料需求汇总仍展示 `WIP可用(g)` 和 `建议领到WIP(g)`。
- [ ] 订单列表点击“只看可发货”时，同时包含“生产完成”“无需生产”和“库存待发货”的订单。
- [ ] “库存待发货”和“无需生产”订单可以生成顺丰发货 Excel；回填快递单号后发货状态变为“已发货”。
- [ ] 订单列表顺丰导出入口显示为“顺丰发货”，生成按钮显示“生成顺丰发货 Excel”。

### I2. WIP 占用
- [ ] 开始生产后，工单建立 WIP 软占用。
- [ ] 另一个工单开始生产时，不能重复占用已被前一个工单占用的 WIP 库存。
- [ ] 取消生产后，相关 WIP 占用释放。
- [ ] 生产工单列表展示 WIP 已占、已耗和剩余占用。
- [ ] 仓库库存点击“WIP占用”后，可按工单查询占用、调整占用克重、释放废弃工单占用。
- [ ] 接口 `/api/produce/wip-reservations`、`/adjust`、`/release` 可返回/处理 WIP 占用。

### I3. 部分完工
- [ ] “生产中”每一行直接展示并可编辑投料(g)、成品件数和余料(g)。
- [ ] 初始未编辑时，实际出品率显示 BOM 出品率；修改投料或成品数后，实际出品率实时按 `成品总克重 / 投料数` 更新。
- [ ] 在“生产中”填写本次产出，勾选“部分完工（保留剩余）”。
- [ ] 如果提交完成时 WIP 库存不足，页面右侧自动打开库存作业抽屉，并默认进入 WIP领退/转仓。
- [ ] 提交后，本次产出进入成品仓，本次消耗从 WIP 扣减。
- [ ] 剩余需求和剩余投料仍保留在“生产中”，后续可继续完成。

### I4. 生产质检
- [ ] 进入“生产质检”，可新增原料、生产工单或成品批次质检记录。
- [ ] 页面采用仓库库存式工作台布局；工单质检显示“选择工单”、原料质检显示“选择原料批次”、产品质检显示“选择产品批次”，右侧抽屉打开后只展示当前类型对应对象并回填单据号/批次号。
- [ ] 结果支持通过、待处理/待定、不合格/不通过；把工单质检改为“不通过”或“待定”时不会再提示 `scope, reference_no and result required`。
- [ ] 质检列表可按范围和结果筛选。
- [ ] 工单质检为“待处理”或“不通过”后，对应 `FP-...` 成品批次显示质检状态；原料批次、WIP 仓库存和成品批次列表也显示同一状态。
- [ ] 冻结批次不能继续领料到 WIP、不能被生产完工扣料，也不能被成品出库/转仓误用。

### I5. 生产验收
- [ ] 进入“生产验收”，能看到仓库、原料库存、WIP、生产工单、WIP 占用、生产日志、质检和成品追溯检查项。
- [ ] 接口 `/api/produce/acceptance-smoke` 返回检查项、状态、数量、说明和目标入口。
- [ ] 仓库相关检查项点击“打开”后进入对应仓库和库存类型，例如 WIP 检查项进入 WIP 在制仓，不停留在“全部仓库”。
- [ ] 生产手册同步说明生产验收、录单批次确认、库存待发货、拒绝使用批次进入生产计划、建议领到 WIP、WIP 占用调整/释放的操作。

### I6. 仓库批次追溯
- [ ] 在“仓库库存”输入 `FP-...` 成品批次，可看到成品、工单、生产日志和消耗原料批次。
- [ ] 输入 `MB-...` 或 `LEGACY-MAT-...` 原料批次，可看到该原料批次的供应/期初说明和当前仓库位置。
- [ ] 批次追溯详情展示质检状态；待处理或不通过批次以冻结状态提示。
- [ ] `LEGACY-MAT-...` 页面说明这是系统升级时按旧物料库存生成的期初批次，点击查询不再提示 `batch not found`。

---

## J. 豆单发布（/vue-shell?view=costing）
- [ ] 生成豆单抽屉默认仍为“棵凡官方豆单”，发布后官方公开链接正常访问。
- [ ] 切换为“我的客户豆单”后，可选择一条官方已发布豆单作为“复制官方价格来源”。
- [ ] 客户豆单发布时保存官方价格来源和自己的历史样式来源。
- [ ] 客户豆单发布后，内容和价格以发布时快照为准；后续官方豆单更新不会自动改写该客户豆单。
- [ ] 管理员账号可以发布/撤回豆单；客户账号看不到发布/撤回入口，保存修改后只能生成 PDF 下载。
- [ ] 客户账号直接调用发布/撤回接口会被拒绝，保存草稿接口返回该客户自己的 owner。

---

## K. 销售单客户信息（/vue-shell?view=salesOrder）
- [ ] 订单列表点击某行“销售单”后，在当前页面打开销售单抽屉，不跳走也不丢失列表筛选和分页。
- [ ] 销售单抽屉内点击“销售单设置”后，打开设置抽屉，可维护说明、收款方式、收款码和公章。
- [ ] 进入订单销售单页面后，点击“客户信息”会从右侧打开客户编辑抽屉。
- [ ] 抽屉可保存客户公司名称、公司地址、联系电话；保存后页面摘要同步刷新。
- [ ] 未设置销售单公司名称且客户公司名称为空时，生成销售单不会再报 `company_name required`，PDF 公司名使用客户名。
- [ ] PDF 中展示客户公司、联系电话和公司地址。
- [ ] 页面先展示销售单预览，确认生成按钮在预览加载后才可点击。
- [ ] 刷新预览不会新增销售单历史版本；确认生成后才新增一个版本。
- [ ] 历史版本下载和下载最新版都返回 PDF，不再出现 `invalid document id`。
- [ ] 公司名称在“公司设置”中维护，销售单设置页不再维护公司名称。
- [ ] 销售单预览展示收款码图片、公章图片、多行收款方式和多行个性化说明；下载 PDF 与预览关键信息和版式一致。
- [ ] 销售单设置页和销售单预览页都可拖动公章位置，点击公章内部开始拖动时公章不跳走，销售单设置页可调整公章大小，下载 PDF 中公章盖在公司名称附近且不拉伸变形。
- [ ] 销售单设置页拖动公章后松手自动保存；刷新预览后坐标不丢，重新点击“确认生成图片”并下载最新版图片时公章使用最新位置。
- [ ] 公司设置页可维护公账收款户名、开户行、账号、纳税人识别号和公司地址，并支持一键复制公账收款信息；销售单设置页不再出现公账输入项。
- [ ] 销售单设置页点击“去除背景”后，当前公章变为透明 PNG，红色印章内容保留。
- [ ] 销售单预览和下载 PDF 中客户公司地址完整换行不截断；单个收款码放大展示，多个收款码自适应填充空白区域。
- [ ] 销售单抽屉点击“确认生成图片”后生成 PNG 图片版本；图片历史版本和“下载最新版图片”都返回 `image/png`。
- [ ] 生成销售单图片不会改变 PDF 最新版，PDF 和图片各自保留独立版本历史。
- [ ] 重新生成销售单图片后，图片内公司抬头、订单信息、商品表、收款说明和公账信息文字不重叠、不越界，二维码区域保持清晰。
- [ ] 销售单生成 PDF 后，点击“分享PDF到微信”会调起系统分享面板并直接发送 PDF 文件，不发送链接。
- [ ] 销售单生成图片后，点击“分享图片到微信”会调起系统分享面板并直接发送 PNG 图片文件，不发送链接。
- [ ] 分享到微信的销售单 PNG 至少按 2 倍 A4 画布生成，整体文字清晰偏粗，收款二维码明显放大且可被微信识别。
- [ ] 浏览器不支持直接分享文件时，页面提示下载最新版后手动发送到微信，不自动复制分享链接。

## L. 出库单维护与下载（/vue-shell?view=deliveryNote）
- [ ] 已发货订单行点击“出库单”后，在当前页面打开出库单抽屉，不跳走也不丢失列表筛选和分页。
- [ ] 出库单抽屉可保存出库日期、出库仓库、发货方式、快递单号和备注。
- [ ] 页面先展示出库单预览，确认生成按钮在预览加载后才可点击。
- [ ] 刷新预览不会新增出库单历史版本；确认生成后才新增一个版本。
- [ ] 未发货订单调用出库单预览或生成接口会返回错误。
- [ ] 历史版本下载和下载最新版都返回 PDF，不出现 `invalid document id`。
- [ ] 出库单生成 PDF 后，点击“分享到微信”会调起系统分享面板并直接发送出库单 PDF 文件，不发送链接。
- [ ] 销售单 PDF、销售单图片和出库单 PDF 的分享都通过同一个 `/api/share-resources` API 定位文件，再通过同一个前端 helper 分享文件。
- [ ] 出库单点击“公章设置”可上传公章、去除背景、调整位置和大小；预览中拖动公章后，新生成出库单 PDF 使用最新公章位置。
- [ ] 库存管理进入“出库日志”后，可看到已生成的出库单文档；点击“查看出库单”打开出库单抽屉，点击“下载出库单”下载对应 PDF。

## L2. 订单发票申请与文件（/vue-shell?view=orders）
- [ ] 订单列表点击某行“发票”后，在当前页面打开发票抽屉，不跳走也不丢失列表筛选和分页。
- [ ] 发票抽屉点击“申请发票”后，订单发票状态变为“已申请”，订单列表同步显示状态。
- [ ] 开票后上传 PDF 发票文件成功，抽屉和订单列表都显示文件入口。
- [ ] 上传 PNG/JPG/GIF/WebP 图片发票文件成功。
- [ ] 上传文本、SVG 或其他非 PDF/图片文件时，接口拒绝并提示只支持 PDF 和图片。

---

## M. 财务月结（/vue-shell?view=financeDashboard / financeClosing）
- [ ] 财务首页选择月份后，展示不含税收入、毛利、净利、税费估算、含税收入、主营成本、期间费用和异常提醒。
- [ ] 财务设置可选择咖啡烘焙厂、咖啡贸易商、咖啡壳豆加工厂、综合咖啡工厂；可切换小规模纳税人和一般纳税人参数。
- [ ] 小规模纳税人税费估算包含增值税免税阈值、附加税、企业所得税和小微优惠估算；一般纳税人展示销项、进项、可抵扣进项、应交增值税、附加税和企业所得税。
- [ ] 费用管理可新增期间费用和主营成本，新增后月度报表金额刷新。
- [ ] 费用日期与当前筛选月份不一致时，保存后页面自动切到费用发生月份并展示该费用。
- [ ] 费用管理新增费用时可选择关联员工；保存后列表显示员工名，点击员工名后只展示该员工相关费用，并可清除员工筛选。
- [ ] 费用管理新增费用时，类别和付款方式为下拉候选输入；空值展示常用候选，输入关键字可模糊搜索，候选外的自定义值也能保存。
- [ ] 费用管理新增费用时，可填写订单ID、客户ID、商品ID、批次号和维度说明；保存后列表展示这些业务维度。
- [ ] 月度结账页面展示月结前检查，包含来源异常、来源明细、票税台账、成本配比和会计交接检查项。
- [ ] 经营报告页面展示来源明细，能看到收入、主营成本、期间费用和票税来源的单据、往来方和金额。
- [ ] 票税台账页面可按月份查询，并可新增销售发票、采购发票、税款缴纳或其他票税记录。
- [ ] 会计交接 Excel 下载入口可用，下载文件包含经营汇总、月结前检查、来源明细、票税台账和凭证草稿。
- [ ] 默认锁账模式是强锁账；月份结账后，再新增同月费用会被后端拒绝。
- [ ] 锁账模式切换入口只有 `can_manage_close_mode=true` 的用户可见；直接调用切换接口也必须校验 `finance.close_mode.manage` 和白名单。
- [ ] 月度结账后，允许新增金额调整；调整后报表状态变为已调整，调整金额进入老板简报和明细表。
- [ ] 月度经营报告同时展示老板简报和利润/税费明细。
- [ ] 报告导出 PDF 和 Excel 两个入口可用，下载接口分别返回 `application/pdf` 和 Excel MIME。
- [ ] 财务菜单受权限控制：`finance.read` 可看首页/报表，`finance.write` 可维护费用/设置，`finance.close` 可月结和调整。
- [ ] 财务需求和验收结果已录入 PR/DEV/UT/API/REV 五张表。

---

## N. 客户门户小程序主题（/vue-shell?view=customerPortalSettings / 微信开发者工具）
- [ ] ERP 客户门户配置页中，同一客户可以选择“咖啡工厂专业风 / 清爽业务工具风 / 品牌会员高级风”之一并保存。
- [ ] 小程序登录该客户后，首页和服务页视觉跟随 ERP 选择的主题；切换客户后主题跟随当前客户变化。
- [ ] 未配置主题的客户默认显示咖啡工厂专业风；小程序用户端没有自行切换主题入口。

---

## O. 客户门户商城（/vue-shell?view=mallSettings / 微信开发者工具）
- [ ] ERP “客户门户配置”中可为客户选择小程序首页为“订单处理”或“商城”；保存为商城后，客户登录小程序直接进入商城页。
- [ ] ERP “客户门户配置”中可启停“商城下单”能力；未启用时小程序不展示商城入口，直接访问商城 API 返回无能力。
- [ ] ERP “商城管理”可选择已有产品，填写标题、副标题、描述、规格、售价、模板、状态和排序并保存。
- [ ] ERP “商城管理”上传商品图片后，图片 URL 写入商品并可通过 `/assets/mall_products/...` 公开访问。
- [ ] ERP “商城管理”右侧预览随当前表单内容实时变化，不需要先保存。
- [ ] 小程序商城只展示已上架商品，商品图片、标题、规格和售价可见。
- [ ] 小程序商城可把商品加入购物车、增减数量、填写收货信息并提交订单。
- [ ] 提交后 ERP 订单列表能看到新订单，订单来源服务为 `mall`，订单明细与小程序购物车数量一致。
- [ ] 商城需求和验收结果已录入 PR/DEV/UT/API/REV 五张表。

## P. 操作手册强制治理（PR-DOCS-001）

### P1. 手册规则
- [ ] `HOW_TO_WORKFLOW.md` 明确要求操作手册更新先于 REV。
- [ ] `AGENTS.md` 和 `AGENTS.zh-CN.md` 明确要求开发时同步更新操作手册。
- [ ] 手册规则包含“单个大功能一个独立操作手册”和“现有手册查缺补漏”。

### P2. 手册文件
- [ ] 存在 `OPERATION_MANUALS.md` 总索引。
- [ ] 当前主要大功能已有初版 `OP_MANUAL_*.md` 或对应既有用户手册。
- [ ] `orderapp-remote/docs/` 中存在线上展示副本。

### P3. 部署与验证
- [ ] `deploy_orderapp.sh` 同步 `OPERATION_MANUALS.md` 和 `OP_MANUAL_*.md`。
- [ ] docs raw route 能读取 `OPERATION_MANUALS.md`。
- [ ] PR/DEV/UT/API/REV 需求种子包含 PR-DOCS-001。

### O4. 大功能菜单内展示手册（PR-DOCS-002）
- [ ] ERP 左侧不新增单独“手册”菜单。
- [ ] 订单销售、生产管理、库存管理、商品与配方、财务管理、设置、需求管理各自菜单内能看到对应操作手册入口。
- [ ] 客户履约账户仍可从设置菜单和客户履约账户页面顶部进入客户履约手册。
- [ ] 手册页面读取 `OP_MANUAL_*.md?raw=1`，界面内容与部署 docs 文件一致。
- [ ] 被合并的旧 `*-user-manual.md` 不再出现在 `orderapp-remote/docs/` 和线上 docs 列表。
