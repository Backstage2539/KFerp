# PR-527 行业字段模板移除计算预览验收记录

## 需求

删除 `设置 → 行业设置 → 行业字段模板` 编辑页中的独立“计算预览”区域。页面继续承担行业字段模板和字段定义维护，不改变已有模板数据或后台兼容接口。

## 实现范围

- 删除计算预览标题、业务预设、需求产出、损耗率、原料单价、工序分钟、工时费、预览按钮和结果展示。
- 删除前端计算预览草稿状态、预览请求和专用样式。
- 保留模板名称、状态、说明、字段定义、新增字段、保存模板和停用模板。

## TDD 证据

- RED：`node --test src/lib/industry-field-templates.test.js` 因 `IndustryFieldTemplatesView.vue` 仍包含 `calculator-panel` 失败。
- GREEN：删除面板及前端依赖后，同一命令通过 1/1。

## 验收

- [ ] 行业字段模板编辑页不再出现“计算预览”或相关计算字段。
- [ ] 模板字段维护、保存和停用流程保持可用。
- [ ] 已有行业字段模板及商品生产配置数据保持不变。

## 数据与接口影响

本需求不迁移或修复业务数据，不新增用户触发写入；行业字段模板 API 和历史兼容计算接口保持不变。
