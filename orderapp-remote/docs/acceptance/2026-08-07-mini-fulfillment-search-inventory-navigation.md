# PR-582 小程序发货查询与库存多选生产验收

## 范围

- 发货中心以目的地和收件人为主标题，按包裹有效发货时间、系统当前客户名称、收件客户/公司、收件人、电话和目的地服务端筛选并分页。
- 我的库存提供商品名称服务端搜索、独立详情页、跨搜索/跨页多选和一次性多商品生产工单预填。
- 继续复用现有发货、中央库存、批次和生产申请接口；不新增数据库字段、业务写接口或操作日志类型。

## RED 证据

- 发货 RED：旧卡片以 `request_no/DSR-*` 为标题，列表 API 只有 `limit`，无法传关键词、发货日期或页码。补应用/HTTP/PostgreSQL 合同测试后先因查询结构和分页返回缺失失败。
- 物流兼容 RED：真实 PostgreSQL 用例先创建 `shipped_at=NULL` 的待发货关系，再写订单抽屉物流记录；修复前按 `2026-08-02` 查询返回 `Total:0`，卡片状态与时间筛选不一致。
- 库存 RED：旧实现把批次详情追加在列表底部，只能通过 URL 预填一个商品；独立页面、一次性多规格预填和客户隔离测试在实现前因模块/路由不存在失败。

## GREEN 证据

- customer fulfillment 应用、HTTP、PostgreSQL 定向包通过；临时真实 PostgreSQL customer fulfillment 全包通过（25.638s），覆盖空 `shipped_at` 物流回退、多包裹任一命中、上海时区显示和筛选一致。
- `go test ./... -count=1` 通过；PR-582 verifier 通过应用/仓储/HTTP/需求接口定向测试。
- miniapp 全量 29 个测试文件 / 187 项、`vue-tsc --noEmit`、development 微信包构建、14 个声明页面和 56 文件清单通过；跨美西设备时区日期用例通过。
- 独立代码复核结论：P0/P1 为 0；已修复详情迟到响应、汇总消失仍显示旧批次，以及待发货关系为空时间时压制物流兼容时间的问题。
- 微信开发者工具交互、development API smoke、合并和部署：待完成。

## 手工验收

- [ ] 发货卡片标题为目的地和收件人，不出现 `DSR-*`；电话、完整地址、商品、包裹和物流信息仍正确。
- [ ] 依次验证当天、三天内、一周内、当月和自定义发货时间；首尾日期包含，未发货申请只在清除时间筛选后出现。
- [ ] 多包裹申请任一包裹命中时整张申请出现，全部包裹仍可见；真实发货时间优先，旧物流兼容时间按上海时区显示。
- [ ] 分别用收件客户/公司、收件人、电话号码和目的地片段搜索；其他客户同关键词记录不可见。
- [ ] 验证总条数、总页数、当前页、上下页、跳页和每页条数。
- [ ] 点击库存卡片进入独立详情，顶部返回和右滑返回正常；详情批次、历史生产日期提示、仓库、入库时间、数量和质量状态正确。
- [ ] 搜索库存商品并跨搜索/页码勾选多个规格，进入一张生产工单且全部规格预填、数量为 0/待填写且不能直接提交；从详情返回保留列表条件和勾选项，返回或提交后不重复预填。
- [ ] 从库存详情添加单商品仍正常；无可用 BOM 的库存商品显示明确提示。

## 交付状态

- 功能分支：`codex/mini-fulfillment-search-inventory-multiselect-20260807`
- `develop` 合并、development 部署和微信开发者工具开发包导入：待完成。
- `main`、production、微信上传/审核：不执行。
