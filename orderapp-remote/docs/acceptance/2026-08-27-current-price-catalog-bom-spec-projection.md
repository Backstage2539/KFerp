# PR-614 当前价格表目录与 BOM 规格发布价投影验收记录

日期：2026-08-27  
环境：development  
分支：`codex/mini-current-price-catalog-sync`

## 现场复现

- 开发数据库当前商品分组选择为 `咖啡豆`（分组 1532，对应发布分类 `8000000000001532`）。
- 当前发布 `V3.0.21 / #110` 只有主商品 `1063 初晓-商品` 的 BOM 规格 7 / 变体 422 四个阶梯价格。
- 旧发布 `V3.0.31 / #107` 属于已被替换的分类 221，但旧规则仍把它作为另一分类的默认表，因此员工小程序合并显示历史商品。
- 初晓处于 `cutover`，默认已发布 BOM 为 18587 / V001，存在 3 个 BOM 规格但没有旧子 SKU 映射；旧订单查询使用 `legacy_child_product_id=0` 取价，导致 ERP 当前规格没有价格并显示空目录。

## 自动验收

- [x] 定向 RED：当前分类过滤、cutover 主商品取价和规格隔离函数不存在，测试编译失败。
- [x] 定向 GREEN：当前分类只保留 #110；cutover 使用主商品 1063 读取发布价，规格 7 获得阶梯，规格 3/8 不继承；legacy 子 SKU 路径保持不变。
- [ ] Go、Vue、小程序和统一发布门禁通过。
- [ ] 合入 `develop` 并部署 development；服务、接口和固定开发小程序包复查通过。

## Van 页面验收

- [ ] ERP 录单能看到初晓及价格表已发布的对应 BOM 规格，不为空。
- [ ] 员工小程序重新打开商品选择器后只显示初晓，不显示旧熟豆价格表中的其他商品。
- [ ] 选择有效阶梯数量后单价同步刷新并可正常录单。

production 与微信正式上传不在本次变更范围。
