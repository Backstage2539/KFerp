# PR-616 挂耳咖啡多级生产与价格模板右抽屉验收

## 范围

- development only；`main` 与 production 不在本次范围。
- 修复平铺价格行的价格模板编辑界面定位。
- 复用初晓现有烘焙半成品与挂耳商品，补齐研磨、挂耳包装和盒装包装链，完成价格表与录单。

## RED 证据

- 前端定向测试在实现前失败：`CostingView.vue` 使用 `settings-drawer-mask`，但当前 scoped style 没有该类的固定定位、视口遮罩和右对齐规则，因此编辑面板进入普通文档流并落在页面底部。
- development 数据预检显示初晓已有默认烘焙物料 BOM 和挂耳商品，但缺少咖啡粉、挂耳包两个中间产出对象及对应默认 BOM；历史挂耳商品 BOM 直接消费熟豆商品，不能证明用户要求的四阶段链路。

## GREEN 证据

- 待完成：抽屉定向/全量测试、Vite、Go、统一验证器和 development 浏览器布局。
- 待完成：development 真实 BOM 默认绑定、递归制造图、成本试算、价格表发布、录单保存和操作日志查询。

## 验收边界

- 业务数据只通过现有 API/服务写入并核对操作日志。
- 已发布历史版本保留，不删除历史快照；验收样例使用明确的 PR-616 说明和可追溯来源。
- Van 的页面业务验收在自动化与 development 交付完成后仍保留为 `REV-616`。
