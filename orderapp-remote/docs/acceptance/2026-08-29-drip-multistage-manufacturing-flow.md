# PR-616 挂耳咖啡多级生产与价格模板右抽屉验收

## 范围

- development only；`main` 与 production 不在本次范围。
- 修复平铺价格行的价格模板编辑界面定位。
- 复用初晓现有烘焙半成品与挂耳商品，补齐研磨、挂耳包装和盒装包装链，完成价格表与录单。

## RED 证据

- 前端定向测试在实现前失败：`CostingView.vue` 使用 `settings-drawer-mask`，但当前 scoped style 没有该类的固定定位、视口遮罩和右对齐规则，因此编辑面板进入普通文档流并落在页面底部。
- development 数据预检显示初晓已有默认烘焙物料 BOM 和挂耳商品，但缺少咖啡粉、挂耳包两个中间产出对象及对应默认 BOM；历史挂耳商品 BOM 直接消费熟豆商品，不能证明用户要求的四阶段链路。

## GREEN 证据

- 抽屉定向测试 53/53 通过；全量 Vue、Vite、Go 和 `scripts/verify_kferp.sh all` 通过。development 浏览器实测遮罩覆盖完整视口，抽屉固定在右侧；补充 `border-box` 后边框、内边距与抽屉本体严格收敛在 `100vh` 内。
- development 真实链路已发布并绑定：初晓熟豆半成品 BOM 版本 `1852`，咖啡粉 BOM `21371/V001`（版本 `2057`），挂耳包 BOM `21372/V001`（版本 `2058`），盒装商品 BOM 版本 `2059/V004`。计划 `#90` 的依赖为盒装工单 `#48` → 挂耳包 `#49` → 咖啡粉 `#50` → 烘焙熟豆 `#51`，四张工单均已释放。
- 两个真实计划阻断已通过 TDD 修复：多级计划只校验当前根商品可达的默认 BOM，不再被无关商品坏 BOM 阻断；离散单位物料按计划库存数量校验工序拆分，不再错误要求销售规格件数。
- 盒装规格成本试算完整：BOM 物料成本 `12.26/盒`、工序成本 `0.80/盒`、标准制造成本 `13.06/盒`、最终价 `17.49/盒`。已发布价格表 `#111 / V3.0.21`，并保存订单 `#1589 / SO-20260828-0001`，数量 `1盒`、价格 `17.49/盒`，关联同一 BOM 规格和发布快照。
- 操作日志覆盖物料、采购收货、库存转入在制、工艺路线、BOM 创建/更新/发布/默认绑定、价格表发布、订单、计划创建/拆分/提交；计划 `#90` 提交日志记录生成 4 张工单。历史已发布 BOM 与其他快照未删除或回算。
- 功能与两个计划修复分别合入并部署至 development（`03319a7b`、`fbf50822`、`6e78df0a`）；最终抽屉精确高度及验收文档随本次 development 发布交付。`main` 与 production 未操作。

## 验收边界

- 业务数据只通过现有 API/服务写入并核对操作日志。
- 已发布历史版本保留，不删除历史快照；验收样例使用明确的 PR-616 说明和可追溯来源。
- Van 的页面业务验收在自动化与 development 交付完成后仍保留为 `REV-616`。
