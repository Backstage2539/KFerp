# PR-622 商品规格权威完全收敛到生产 BOM

日期：2026-09-02

## 范围

- 商品档案规格、库存单位、默认 SKU 和子 SKU 下线。
- 商品 BOM 统一规格组并允许手工维护规格、条码和全局单位。
- 新价格、订单、库存、生产、成本与履约只使用默认已发布 BOM 规格。
- 开发与生产旧子商品、销售规格模板绑定和迁移表按锁定清单清理。

## TDD 证据

- RED：旧合同仍允许商品单一产出、商品档案规格写入、迁移写路由和录单未配置商品。
- GREEN：`product_output_requires_bom_spec`、`product_unit_owned_by_bom_spec`、`product_bom_spec_not_configured`、手工规格草稿、商品组件规格必选和活动商品响应净化均有 Go/API/前端定向测试。
- 清理工具：确定性清单与校验和、数据库顾问锁、主机留存的完整备份凭证、单事务、活动依赖阻断、旧关系删除和幂等验证有代码合同测试；`apply` 强制提交备份路径、文件大小和 SHA-256，并写入操作日志。实际双环境报告在部署后补入本文件。
- 授权测试数据处理：默认仍阻断；只有显式 `--discard-unmapped-test-data` 才会在同一事务内作废未映射测试订单、撤回价格表、归零库存、取消生产记录和清除履约行，并将数量写入清理报告及操作日志。开发授权不自动适用于生产。

## 发布与数据证据

- [x] 功能分支同步最新 `origin/develop` 并完成全量验证。
- [x] 合入 `develop`、开发代码部署及健康检查。
- [x] 开发 preview、完整备份校验、apply、重启、verify，旧子商品为 0。
- [x] 合入 `main`、生产代码部署及健康检查。
- [ ] 生产 preview、完整备份校验、apply、重启、verify，旧子商品为 0。生产 apply 因真实依赖阻断，未执行。
- [ ] 移除一次性命令和剩余迁移兼容代码，同步 `develop/main` 并完成最终双环境代码部署。

### 开发环境结果

- 部署提交：`c3bc1be14e0e1c8562262abcdda8f814189ff16e`；登录 200、应用 303、受保护接口 401、容器重启次数 0。
- apply 前备份：`/opt/stacks/erp/backups/pr622/development-before-pr622-apply-20260902T1135CST.dump`，14,757,626 字节，SHA-256 `22a51e8ccc4dc00cf1cd1756cdc377d7aa42dc2c1209c279e14d254ac15cc025`。
- 锁定清单：`PR-622-1c011c390b1df23c`。清理 416 个旧规格子商品、18 个旧单位模板；转换 11 个单一产出 BOM，失效 25 个不完整单一产出 BOM，改写 17 条可映射引用。
- 经环境负责人授权，仅开发环境作废/清理测试依赖 115 条、撤回测试发布 16 个、作废测试订单 23 个；相关主迁移与两次启动回归修正均写入操作日志。
- 重启后 verify 清单 `PR-622-492fcf120bc37067`：旧规格子商品 0、旧单位模板 0、活动商品单一产出 BOM 0、旧价格引用 0、迁移关系不存在；重复 apply 返回“已完成、未写入变化”。

### 生产环境结果

- 部署提交：`9feefafb95911d85894c6f4f1cf1513d0d9796d3`；登录 200、受保护接口 401、容器重启次数 0。
- 只读 preview 清单：`PR-622-30fecdc95cd1f060`；421 个旧规格子商品、134 个可映射子商品、9 个可转换单一产出 BOM、19 个旧单位模板和 20 个活动商品单一产出 BOM。
- 阻断依赖：当前发布价格表分组项 23 条、价格行 54 条，以及未完订单行 1 条仍引用无法映射的旧规格子商品。生产未使用 `--discard-unmapped-test-data`，正式 apply 未执行。
- 完整备份：`/opt/stacks/erp-production/backups/pr622/production-before-pr622-blocked-preview-20260903T010243CST.dump`，3,212,197 字节，SHA-256 `a1a6952ba52610ef35c22d3ef467a970f78b290f6a6647498c647374d1f002fd`。
- 备份已恢复到临时数据库；预览清单与生产一致，无丢弃参数的 apply 返回 3 组未解决依赖并拒绝写入，拒绝前后清单一致，临时数据库已删除。

## 恢复边界

清理会删除旧迁移、映射和升级关系，不提供逐行逻辑回滚。应用后的恢复方式仅为停写后恢复本次 apply 前的完整数据库备份；历史 BOM 版本和业务单据自身快照不删除。
