# PR-624 录单 BOM 规格价格与单位统一验收记录

## 生产只读复现

- 订单 `SO-20260903-0001` 的商品为曲奇，保存身份为主商品、BOM 规格 222、发布变体 272，规格为 `2Kg袋装`，数量与库存单位均为袋。
- 当前已发布工厂供货价格表在该规格的 10–24 袋档冻结最终价 162.30、价格单位袋、库存单位袋、数量口径 `sales_spec_count`。
- 原页面用主商品 ID 查找规格，因此找不到 BOM 规格 222，自动价为空；人工录入后价格来源又未冻结单价单位。ERP 重新打开时按规格重量回退猜成元/磅，小程序则按订单行单位显示元/袋。
- 只做数据库查询，没有修改订单、价格表、BOM 或生产业务数据。

## TDD 证据

- RED：Vue 录单测试新增曲奇 `2Kg袋装 × 13袋` 后，价格单位实际为元/磅；BOM 规格历史行同样回退为元/磅；视图没有通过 `bom_spec_id` 恢复所选规格，共 3 项失败。
- GREEN：`node --test src/lib/order-entry.test.js` 通过 133/133，覆盖按 BOM 规格命中 162.30、元/袋、件数阶梯不做重量换算、旧订单单位回退及视图恢复路径。
- GREEN：真实临时 PostgreSQL 执行 `TestOrderAPICutoverProductUsesBOMSpecBusinessIdentity`，确认保存和编辑回读均冻结主商品、BOM 规格、当前变体、规格名、袋单位、人工最终价及人工调整标记。
- GREEN：`TestManualBOMSpecPriceSnapshotFreezesSpecificationUnitAndPrice` 通过；员工小程序录单定向测试 44/44 通过，原有数量和阶梯价原子同步以及规格单位显示不回归。

## 行为边界

- 修复只影响 BOM 规格权威商品的当前选价和单位解释，不修改价格表数值、历史订单成交价或历史金额。
- 旧订单缺少 `price_unit` 时可由自身已冻结的 `bom_spec_id + unit` 正确展示；新保存人工价会完整冻结单位和最终价。
- 本需求未授权环境部署，生产仍保持原代码与原数据；环境验收需在后续明确部署后执行。
