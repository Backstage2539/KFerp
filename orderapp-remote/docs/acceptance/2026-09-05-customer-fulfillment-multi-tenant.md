# PR-627 履约客户多租户开发环境验收记录

## 验收范围与环境

- 验收日期：2026-09-05（Asia/Shanghai）
- 环境：开发环境 `https://dev.qacoohee.com`
- 运行版本：`aeffcdadd8e3dad26ae75c2603d0c7543bf405fd`（`codex/customer-fulfillment-multi-tenant`）
- 开发部署：`./deploy_orderapp.sh development` 完成；源代码备份 `/opt/stacks/erp/orderapp.backup.deploy-20260905062443-aeffcdad`；回滚镜像 `kferp-orderapp-rollback:development-20260905062443-aeffcdad`
- 健康检查：开发登录 HTTP 200、公开入口 HTTP 200、开发数据库 healthy；生产容器仅只读检查，未重启、未写入业务数据。

本次使用独立验收客户、外部账号、商品、仓库、价格表和订单。验收数据保留在开发数据库供复查。

## 验收数据索引

| 对象 | 客户 A（代加工） | 客户 B（批发） |
| --- | --- | --- |
| 客户 | `296` `代加工客户A-PR627-0905` | `297` `批发客户B-PR627-0905` |
| 外部账号 | employee `27` | employee `28` |
| 客户专属商品 A | `1069` `PR627-A2-客户来料`，`material_source_mode=customer` | `1070` `PR627-B2-工厂供料`，`material_source_mode=factory` |
| 公共商品 B | `1043` `晚香玉`；客户引用 `15`，客户名称 `客户商品B` | `1043` `晚香玉`；客户引用 `17`，客户名称 `晚香玉` |
| 公共商品 C | `1035` `晨曦·娜依`；客户引用 `16`，客户名称 `PR627客户商品C` | 未加入本场景价格表 |
| 客户仓 | `customer_296_raw`、`customer_296_packaging`、`customer_296_finished` | 仅 `customer_297_finished`，无原料/包材仓 |

客户 A 的专属 BOM 为 BOM `29893`、版本 `2470 / PR627-V1`、规格 `393 / PR627-227G`、变体 `486`；客户 B 的专属 BOM 为 BOM `29896`、版本 `2473 / PR627-V1`、规格 `396 / PR627-B-227G`、变体 `487`。公共商品 B/C 仍分别使用原 BOM `13460 / 版本1617`、`13458 / 版本1616`，验收准备没有修改其商品身份、BOM 或工艺路线。

## 四个开发环境场景

### 1. 代加工客户 A：客户来料 A + 公共 B/C 混合订单

通过。实际创建客户专属商品 `1069`，创建独立 BOM 并将供料方式设为客户来料；通过客户引用 `15`、`16` 将公共商品 B/C 加入客户价格表，并仅把 B 的客户展示名设为“客户商品B”。最终发布价格表 `#121 / PR627-A-V6`，其中 B 行同时保留规范商品名“晚香玉”和客户展示名“客户商品B”，C 行保持公共商品的规范名称和身份；客户引用 C 的展示名在订单行快照中按客户范围保存。

实际保存三行订单 `1604 / SO-20260905-0005`，三行均成功：

| 行 | 商品身份 | 客户展示名 | 供料方式 | 价格来源 |
| --- | --- | --- | --- | --- |
| 1 | `1069` | `PR627-A2-客户来料` | customer | `#121 / PR627-A-V6`，成交价 `1` |
| 2 | `1043`，引用 `15` | `客户商品B` | factory | `#121 / PR627-A-V6`，成交价 `1` |
| 3 | `1035`，引用 `16` | `PR627客户商品C` | factory | `#121 / PR627-A-V6`，成交价 `1` |

订单行落库冻结了 `product_id`、客户显示名、引用 ID、供料方式、价格表版本和成交价。生产核对中，A 行进入客户来料需求 `customer_order_production_demands`（订单 `1604`、行 `1`、客户 `296`、状态 `planned`）；B/C 没有被重复生成普通生产需求，仍按公共商品原流程处理。客户 A 的专属生产计划 `99`、工单 `53` 已完成，客户原料领料流水 `entry 24` 的货主为 `296`，成品入库落在 `customer_296_finished`，货主仍为 `296`。公共 B/C 的商品 ID、BOM 版本和工艺路线未发生改变。

价格表页面实际刷新并展开 `#121` 的内容预览，截图中可见 B 行 `name=客户商品B`、`display_name_snapshot=客户商品B`、`product_name=晚香玉`、`customer_product_reference_id=15`。此前发现旧验收版本预览仍显示规范名后，重新发布 V3–V6 并复核，最终以 V6 作为订单价格版本。

### 2. 批发客户 B：工厂供料专属 A + 公共 B

通过。客户 B 只开通成品仓，专属商品 `1070` 明确配置为工厂供料；价格表 `#117 / PR627-B-V2` 同时包含专属 A 和公共商品 B。实际保存两行订单 `1601`，两行均成功，专属 A 使用 BOM `29896 / 2473`，不要求客户原料仓或包材仓；公共 B 仍为商品 `1043`、客户引用 `17`、展示名“晚香玉”。

批发客户 B 的生产计划 `100`、工单 `54` 已完成。重试后的领料单 `entry 28` 使用工厂货主 `0` 的原料批次 `46`，完工单 `entry 29` 产出商品 `1070` 到工厂成品仓，货主为 `0`。验收中发现一次故意错误的初始领料 `entry 27` 选择了客户 A 批次，已取消并核对冲销流水 `200/201`，之后按工厂供料重新领料；最终链路没有使用客户 B 原料仓。

### 3. 客户之间隔离

通过。分别使用客户 A/B 的外部账号访问商品目录、搜索、录单候选、客户库存和价格下载接口，并发送篡改的 `customer_id`：

- `/api/mini/processing/catalog` 始终以令牌绑定客户为准。A 账号只返回商品族 `1069`，带 `customer_id=297` 仍返回 `current_customer_id=296`；B 账号只返回商品族 `1070`，带 `customer_id=296` 仍返回 `current_customer_id=297`。
- 客户库存批次接口：A 请求 B 的商品 `1070/spec396/variant487` 返回空行；B 请求 A 的商品 `1069/spec393/variant486` 返回空行；A 请求自己的商品返回 3 条自有批次。
- 价格表下载：A 可下载 `/api/mini/bean-lists/121`（HTTP 200），请求 B 的 `117` 返回 HTTP 404；B 可下载 `117`（HTTP 200），请求 A 的 `121` 返回 HTTP 404。
- 公共商品 `1043` 可以在双方价格表中共享，但“客户商品B”只存在客户 A 的引用 `15` 和快照，客户 B 仍显示“晚香玉”。

因此对方专属商品、价格表和库存既不出现在列表/搜索/录单候选，也不能通过直接接口取得。

### 4. 工作台价格表与仓库绑定

通过。客户 A 工作台页面实际显示“我的价格表”，包含已发布的 `PR627-A-V6` 及其历史版本；客户 B 页面显示 `PR627-B-V2` 及其历史版本。A 工作台截图显示客户名、价格表、客户原料/包材/成品卡片和订单汇总；B 工作台截图显示自己的价格表，库存表显示“暂无库存”，没有客户 A 的库存。

库存接口与落库核对均按“账号绑定客户 + 该客户有效绑定仓库 + 库存货主”过滤：A 自有成品批次只出现在 `customer_296_finished`；B 不返回 A 的原料、在制品或成品批次；请求未绑定的工厂仓或其他客户仓不扩大结果范围。客户 B 没有原料/包材仓，仍可查看自己的价格表并完成工厂供料订单。

## 关键分支与一致性核对

### 客户来料缺口

通过。订单 `1603`（客户 A 专属商品 A，数量 `20`）的未生产查询返回客户缺口 `22g`。计划 `102` 的物料缺口为：需求 `22000g`、客户可覆盖 `7000g`、缺口 `15000g`，状态 `unresolved`，没有默认客户物料 BOM 时提示补料/采购。工厂货主原料另有 `4000g`，没有被计入客户 A 可用量。计划取消后状态为 `cancelled` 且没有生成工单；补录客户库存后可重新校验。

### 客户来料领料、完工与批发工厂供料

通过。客户 A 计划 `99` 的工单 `53` 使用客户货主 `296`，领料流水 `entry 24` 从客户货主原料转入客户货主在制，完工产出 `1069` 入客户成品仓，成品批次货主为 `296`。客户 B 工单 `54` 使用工厂货主 `0` 的领料 `entry 28` 和完工 `entry 29`，没有要求客户原料仓。

### 生产需求、重复提交与取消

通过。混合订单只有客户来料商品 A 产生客户生产需求（订单 `1604`、需求行 `6`）；B/C 和批发订单没有重复的普通需求。对已完成计划 `100` 再次提交返回 HTTP 400 `production plan must be draft to submit`，工单仍只有 `54`。取消计划 `102` 后没有遗留工单或继续扣库。

### 客户库存补录与幂等

通过。客户 A 原料补录 `entry 16`（材料 `8`、5000g）进入客户原料仓，包材补录 `entry 17`（材料 `66`、100 个）进入客户包材仓，成品补录 `entry 20 / SE-0000000020` 进入客户成品仓。使用相同幂等键 `PR627-A-FIN-1` 重复提交返回同一 `entry 20 / item49`，没有重复批次、余额或流水。

订单创建接口的重复 POST 没有作为独立测试项；本次使用计划提交保护和库存补录幂等作为重复操作核对，历史订单快照未被重写。

## 自动化检查与交付

- 针对性前端客户引用/价格表测试：20/20；新增客户引用展示名快照测试通过。
- Vue 全量测试：1066/1066；Vite 构建通过。
- 小程序测试：220/220；类型检查和构建通过。
- Go 全量测试、统一 `scripts/verify_kferp.sh all` 检查和 Docker 构建通过。
- 开发环境发布后已核对部署版本、容器健康、Caddy 配置、公开登录入口和 56 个页面文件指纹。
- 操作手册：`docs/OP_MANUAL_CUSTOMER_FULFILLMENT.md` 已补充客户独立商品、客户引用、供料方式、客户仓、`customer_receipt`、缺料和工作台隔离流程。
- 页面截图证据：本次任务记录保留了客户 A 价格表 V6 展开预览截图（含“客户商品B”快照）和客户 B 工作台截图；接口越权请求的 HTTP 状态与响应范围已在本记录列出。

结论：四个指定场景及关键分支在开发环境通过，代码运行版本为 `aeffcdad`。本次不发布生产环境；验收数据仅保留在开发环境供复查。
