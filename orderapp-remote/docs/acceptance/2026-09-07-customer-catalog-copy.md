# PR-634 商品与分类复制到客户验收记录

- 分支：`codex/customer-catalog-copy-20260907`，基于 `develop 5496efc4`。
- DEV-634-CATALOG / DEV-634-PRICING / DEV-634-DELIVERY；产品状态 review，等待 VA 验收。
- 范围：development。生产部署、微信上传/审核不在本次发布范围。

## 自动验证

- RED：目录仓储/API/前端测试分别记录新契约缺失；报价草稿刷新测试记录丢失继承报价的失败。原始日志 `outputs/pr634-customer-catalog/pr634-*-red.log`。
- GREEN：PostgreSQL 16 独立测试 schema 实测全量（只启用公共）、所选/单个、完整祖先路径、未分类、排除空分类、重复/并发幂等、停用恢复、跨客户改名、整批失败无日志、旧引用预览/补齐/幂等。
- 回滚脚本实测恢复原分类链接/排序；迁移后分类被改名则整批拒绝回滚。`scripts/sql/pr634_customer_catalog_rollback.sql` 根据迁移操作日志执行，日志保存迁移前后引用及新建节点。
- API 测试：复制请求校验、客户目录读取、内部岗位权限、禁止客户账号维护/越权读取、仅管理员迁移。
- Vue 测试：三入口统一复制接口、客户目录投影、别名不改主档、精确商品/规格报价匹配、保留客户表与草稿、缺报价、刷新保留调整。完整 Go/Vue 测试及 Vite 构建通过；小程序 235 项、类型检查和 development 构建通过。
- 网页/小程序录单继续使用同一默认价格表筛选服务；既有按类型客户表优先、公共回退、规格快照、历史价格、预付款与订单复制回归由完整测试覆盖。

## 开发环境验证

独立验收客户 A=300（PR634验收-目录客户A）、B=301（PR634验收-目录客户B），业务数据保留供复查。开发发布日志、接口响应、截图与数量证据集中在 `outputs/pr634-customer-catalog/`。

## 数据与回滚

复制事务只写 `product_customer_references`、`customer_product_catalog_nodes` 和 `audit_logs`。商品、BOM、规格、组件与库存保留共用身份；运行迁移前后独立核对主档数量与库存指纹。
迁移接口 `POST /api/product-settings/customer-catalog/migration` 默认 `apply=false` 预览，显式 `apply=true` 执行。回滚时传入数据库 schema 与迁移日志 ID，发现分类/引用后续变化立即中止，禁止覆盖后续业务操作。

## 手册

`OP_MANUAL_INVENTORY_MATERIALS.md`、`OP_MANUAL_COSTING.md`、`OP_MANUAL_ORDER_SALES.md`、`OP_MANUAL_MINIAPP_EMPLOYEE_ERP.md`；通过现有 Vue 手册入口提供。

## 首轮开发环境实测（2026-09-07）

- development `18b71539`，旧引用 15 条：预览 15、执行 15、复查 0；迁移日志 8484。迁移前后商品/BOM/规格/库存指纹一致。
- 独立客户 A=300、B=301；页面搜索“初晓”后全量复制到 A，实际创建 86 个有效公共商品引用，日志 8485；页面复选商品 1063/594 到 B，单个商品 1084 到 B。
- A 商品 1063 改为“PR634客户初晓”，分类“意式咖啡”改为“PR634客户意式”；目录与价格表选品一致。
- 发现并修复：成功消息未渲染、价格表筛选只取旧履约范围、平铺报价行仍展示主档名、客户未分类商品缺少选品入口。追加 RED/GREEN 回归并重新部署后复测。

## 开发环境验收结果

| 项目 | 结果与证据 |
| --- | --- |
| 全量复制 | A 创建 86 个公共商品引用；当时页面搜索“初晓”仅显示 5 款，全量范围独立于搜索与分页，其他客户专属商品排除 |
| 复选和单个 | B 复选 1063、594，再单个复制 1084，共 3 个引用；相关分类路径随商品建立 |
| 幂等和恢复 | A 再复制返回已有 86；B 停用 1084 后复制恢复 1 条，无重复有效引用 |
| 整批回滚 | B 同批选择有效公共商品 1064 和其他客户商品 1073 返回 400，1064 未写入，复制操作日志未增加 |
| 名称隔离 | A 的 1063 改为 PR634客户初晓；源分类 1532/24122 改名 PR634客户意式；B 和工厂保留初晓-商品、意式咖啡 |
| 分类与选品 | 商品归属与价格表归属读取同一客户目录，客户未分类入口可选；不创建缺失规格 |
| 首次报价 | A 从公共表 122 / V3.0.22 带入商品 1063 的 BOM 规格 3（227g）四个阶梯与价格 28、33、26、24 |
| 客户已有报价 | B 草稿 123 的 37.50 保留；页面修改第一阶梯为 38.25，切换 A/B 并刷新后仍保留，A 不受影响 |
| 实际发布 | B 页面发布表 124 / V3.0.5；A 页面发布表 125 / V3.0.5。分类与客户名称已冻结，商品身份字段继续保留工厂名称 |
| 缺规格报价 | A 追加 454g 后明确显示待报价，取消后可正常发布；不生成零元报价 |
| 默认客户表 | `/api/order/form?customer_id=300/301` 分别返回 125/124 为默认；网页实际选择客户后显示客户价格表 V3.0.5 |
| 客户接口隔离 | 两个独立客户账号认证成功后，读取对方客户目录及提交复制均返回 403；以错误客户 ID 改名分类返回 400 |
| 旧引用迁移 | 预览 15、迁移 15、重复检查 0；迁移操作日志 8484 保存完整前后数据和创建节点，迁移回滚脚本本地 PostgreSQL 验证通过 |
| 主档与库存 | 操作前后商品、BOM、规格、组件、库存批次/余额/流水的数量与内容指纹一致 |

## 录单闭环补充

实际网页录单发现：共享 BOM 规格的公共报价覆盖了客户商品的报价，客户价格表已被默认选中，但客户商品不能搜索选用。新增网页与小程序服务端 RED 测试复现；现按商品真实归属区分公共规格报价与客户引用报价，引用商品仅带入其已发布的匹配 BOM 规格报价，未报价规格不进入候选。

- RED：`pr634-order-name-red.log`、`pr634-mini-quote-red.log`。
- GREEN：真实开发接口数据离线回放，A/B 均得到各自名称、227g 规格和独立报价；Go 全量、Vue **1093/1093**、Vite 构建及统一检查通过，见 `pr634-order-closure-checks.log`。
- 本次没有小程序前端代码改动；小程序 **235/235**、类型检查与 development 构建通过。微信上传、审核、发布及小程序真机手工验收未执行。
- 新验收订单尚未保存；报价选择在网页验证，订单保存、历史订单与复制预付款回归由 Go/Vue/小程序测试覆盖。
