# PR-642 EFS 履约录单与客户首页

状态：针对性自动验证及合并最新 develop 后的全量门禁完成，待开发/生产发布与原单调整；业务验收由 Van 完成。

## 实现与范围

- 首页只取汇总，能力对应独立 Vue 页面；我的订单与共享录单页分开。价格列表仅取元数据，预览投影公开价格字段，不暴露成本快照，不生成或下载价格表文件。
- 客户和员工共用 OrderEntryView、价格表/商品/规格/梯度逻辑及 sales.SaveOrder。旧客户提交入口适配到该服务，保持响应字段和名称录单兼容。
- 客户绑定及能力在服务端实时校验，限制成交价格、优惠和支付/发货状态。独立收件信息更新仅修改三个相关字段并留操作日志，数据库发货保护覆盖其他写入路径。
- 客户请求标识有客户范围唯一约束、事务锁和内容校验；成功后重试、并发重试返回同一订单，价格表后续变动不影响已有请求回放。
- 员工复用现有组合销售单，未修改该功能的渲染规则或缓存版本。

## RED / GREEN 与自动验证

1. 新增客户提交策略及页面能力映射测试在实现前分别因缺少 ValidateCustomerOrder、缺少 customer-workspace 模块失败；实现后通过。
2. `customer_workspace_test.go` 使用本地隔离 PostgreSQL 数据集：连续 8 单及总额、重复提交、并发相同请求、请求内容冲突、失败后重试、公开目录/绑定隔离、禁用能力、禁止客户端改价、缺收件信息禁止发货、补收件信息不改金额、发货后禁止改收件信息、价格表撤回后请求回放。
3. `workspace_test.go`：元数据列表不含价格明细；预览仅公开字段；跨客户/不存在版本拒绝；模板能力撤回实时生效。
4. `shared_order_test.go`：旧单商品名称请求通过客户范围解析，并调用共享销售服务，保留返回单号/ID；客户运费仍由后端保护。
5. `authz_api_test.go`：客户即使携带其他客户参数及内部权限字段也不会获得内部客户覆盖通道。
6. 全量 Go 检查通过；前端完整测试首次发现旧页面文字断言不适用，已随独立页面更新，并保留共享表单、分页、明细、收件信息及客户隔离契约。最终门禁见发布记录。

## 指定原单调整

`SO-20260908-0002` 至 `SO-20260908-0009`（ID 42–49、客户 14）由赠送 6 改为批发 3，保留代发归属。仅正式发布之后执行 `scripts/operations/efs_orders_wholesale_20260909.sql`；必须先导出受保护备份。事务内校验全部 8 单、客户默认类型和代发标识，对比完整订单（仅排除订单类型）及所有订单明细，操作日志按单记录。重复执行已完成订单不重复修改或写日志。

## 操作说明

单一手册：`orderapp-remote/docs/OP_MANUAL_CUSTOMER_FULFILLMENT.md`；Vue 首页导航、录单帮助和价格表预览说明同步更新，手册索引已登记。

## 发布前完成证据

- 已合并最新 `origin/develop`（68163316，已完成的 PR-641 验收和单据修复），保留其改动。
- `scripts/verify_kferp.sh all`：Go 全量、Vue 1121/1121、生产构建通过。
- 本地 PostgreSQL 八单集成含 PDF/PNG：8 个原订单分组、订单日期与单据日期分别保留，合计 480.00；预览 PDF 2 页、PNG 长图有效。已检查长图及 PDF 第二页，文字与金额完整，无重叠。测试产物 `/private/tmp/kferp-efs-artifacts/efs-eight-combined.pdf`、`efs-eight-combined.png`（模拟数据）。
- 原单调整 SQL 已在隔离测试数据验证：无备份拒绝；首次修改 8 单，二次 0 单；操作日志保持 8 条；历史快照完整一致。
- 价格表选择、跨客户版本拒绝、原价快照冻结和缺规格阻止继续由共享销售测试覆盖。

- 发布收尾复查发现详情接口原先未返回保存的行金额，已补齐 line_total，避免新订单明细页显示 0；客户响应移除内部成本/生产追溯，商品与规格目录也仅保留公开计价依据。新增针对性 RED/GREEN。
