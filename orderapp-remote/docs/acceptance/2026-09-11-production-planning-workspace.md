# PR-649 生产计划三步工作台与草稿重算

业务范围：在 PR-648 多层生产、半成品备货、在途供应和分批入库基础上，重构生产计划的用户工作台。开发环境交付；正式环境不在本次范围，Van 保留最终业务验收。

## 实现结果

- 页面改为 `选需求 → 核对缺口 → 安排生产` 三步。选需求支持商品/客户/订单搜索、商品与规格三态选择、订单客户数量明细和具体配置异常。
- 缺口核对先显示结论，再分为“商品缺口”和一个统一“生产用料”清单。生产用料显示需要、现货、在产、剩余缺口及需制造/待补料/在产覆盖/库存满足，不向计划员展示 BOM 层级和依赖术语。
- 安排生产保留来源仓、货主、工位产能拆分、提交工单与撤销草稿。草稿生成后先展示摘要，由计划员主动安排生产。
- 新增草稿原单重算：返回修改需求后以原计划 ID、计划号和并发版本更新；保留身份完全兼容的目标仓、来源仓和工序拆分，变化部分重新确认。重复请求幂等，旧版本冲突返回 409，写入操作日志。
- PR/DEV 进度表和生产手册同步更新。

## 自动验证

- RED：前端 helper 与页面结构测试先因三步工作台、商品/物料缺口 helper、草稿更新契约缺失而失败；后端集成测试先因计划 revision 和更新服务缺失而编译失败。
- GREEN：草稿原单重算隔离数据库 API 测试通过，覆盖同计划号、并发 revision、幂等重试、操作日志、兼容拆分/来源仓保留、需求变化清理旧拆分及已提交禁改。
- GREEN：生产计划前端目标测试 74/74，通过真实 Vue 模板渲染“有选择、无草稿”、商品/规格归并、同名不同商品隔离、订单客户数量、统一生产用料和刷新选择参数。
- GREEN：`scripts/verify_kferp.sh all` 通过；全部 Go 包通过，Vue/Node 1172/1172，通过冲突标记与格式检查，Vue 生产构建通过。

## 开发环境验收

- 已以 merge commit `031c9aa878011b579b20ba9a715bef7dfa2a42f5` 合入 develop，并通过 `deploy_orderapp.sh development` 发布至 `https://dev.qacoohee.com/app/vue-shell?view=productionFlow&demand_status=unplanned`。发布输出 `Release completed`，外部登录与生产计划 API 均返回 HTTP 200。
- 浏览器实际页面显示三步工作台；普通入口和携带历史 `plan=1&selected=...` 参数的旧链接均正常渲染，没有白屏，浏览器错误日志为空。
- 当前待计划订单全部因历史 BOM 配置异常不可选择。页面已分别显示“BOM 缺少物料明细”“默认 BOM 已停用或不再产出此商品”“订单锁定的 BOM 规格或版本不可用”“存在冲突的默认 BOM 配置”，替代笼统的“资料待完善”。
- 半成品备货只读预览以“初晓 1 kg”为例：初晓新增生产 1 kg；孟连水洗5T批次、孟连水洗A、生豆-巴布亚之光-石光、耶加雪菲G2 均由现货覆盖，剩余采购缺口为 0。没有点击“创建备货草稿”。
- 服务容器运行中、重启次数 0，发布后 15 分钟致命错误日志 0。回滚源为 `/opt/stacks/erp/orderapp.backup.deploy-20260911153142-031c9aa87801`，回滚镜像为 `kferp-orderapp-rollback:development-20260911153142-031c9aa87801`。
- 浏览器验收没有创建、提交、修改或撤销真实生产计划；草稿原单重算由隔离数据库 API 测试覆盖。Van 保留最终业务验收。
