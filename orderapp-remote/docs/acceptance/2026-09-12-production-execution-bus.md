# PR-654 生产执行任务总线与完工入库

实施日期：2026-09-12。分支 `codex/production-execution-bus-20260912`，目标 `develop` 和 development；production 不在本次范围。产品验收人为 Van。

## 交付范围

- 生产工单列表缩为六组核心信息；工单详情成为任务分配与状态汇总总线。
- 工序记录合入工单详情并保留兼容入口；工位按工序和拆分批次执行。
- 从工位一次开工，任务级校验人员、工位、WIP、前序交接和质检；首次自动建立运行记录。
- 质检使用普通表单、默认未检查并关联具体任务；“生产验收”改为“完工入库”。
- 修复空运行编号串出全局成本/日志，以及冻结产出类型、规格和数量显示错误。

## TDD 证据

RED：新增 `production-execution-bus.test.js` 后，工单六列、工单总线、工位任务、质检和完工入库五组断言均失败；Go 新测试因缺少任务级用量缩放方法和质检默认状态不符而失败。

GREEN：

- `go test ./...`：通过。
- 前端全量 `node --test src/lib/*.test.js`：1175/1175 通过。
- `npm run build`：通过；仅保留既有大分块提示。
- 定向覆盖包括任务批次 WIP 只分配一次、完工入库单位聚合、任务级库存单据缩放、未开工任务一次开工，以及质检默认未检查。

## 业务边界

- 预览和查看不写业务数据。执行人分配、领取、开工、报工、质检及库存过账使用现有权限、事务和操作日志。
- 新工单使用任务级校验；旧接口保留兼容但不能绕过任务校验。历史已执行工单的库存和成本不回算。
- development 页面核对采用只读方式，除非另建隔离验收数据；不改现有生产工单。

## development 交付与截图

待合入和部署后补充：运行 commit、回滚源、容器/API smoke，以及生产工单、工单详情、工序记录、工位视图、生产质检、完工入库六页截图。

Van 业务验收未代签。
