# PR-655 生产管理精简与工序人员排程

日期：2026-09-12。目标：开发环境。业务验收人：Van。

## 交付内容

工序维护可执行员工、一名默认负责人及多名默认协作人员；排程按工序任务查询、分页、带入、预览、确认并整批保存。工位和工单详情复用同一人员安排，调整人员保留原时间、班次、备注和工位。独立生产视图已删除，旧链接转到工位视图，原分配入口转到排程。

排程已删除日历、甘特、独立负载切换及 MRP 面板。缺料仍可提前安排，实际开工沿用已有齐套与前序校验；未维护可用工时显示待配置。历史任务只读，原始批次不在排程重新拆分。无工资、计件、成本计算或新增权限变化。

## 自动验证

- RED：工序人员、默认带入、人员补丁和批量接口测试先失败；日志位于 `/private/tmp/kferp-pr655-evidence/` 的 `backend-red.log`、`frontend-red.log`、`schedule-red.log`、`api-red.log`。
- GREEN：Go 全包测试、前端 1203 项测试及 Vite 构建通过。
- `TestPR655ScheduleStaffPostgresLifecycle` 使用独立临时 schema，并在结束时清理。真实 PostgreSQL 验证预览不写入、重复与并发请求只写一次、跨工位协作人员冲突、冲突变化重新确认、资格/停用/重复人员拦截、默认配置不覆盖已安排任务、版本冲突、整批失败回滚、单条兼容接口不清空时间班次备注、旧任务补工位不拆批、共享读模型、服务端分页和操作日志。
- 旧生产视图跳转保留工单和工序任务参数；新版 API 使用既有 `production.read` / `production.run` 权限。

## 浏览器验证与隔离数据

本地绑定 `127.0.0.1:8655`，schema `pr655_browser_20260912`，只使用 `WO-QA-655-*`、测试工序及测试员工。未修改真实开发/生产业务数据，也未修改 PP-0000000113。

已操作验证：工序缺人员时拒绝启用保存；补齐候选、默认负责人和协作后保存；排程跨工序批量带入；共用协作员工的冲突预览及确认保存；刷新后保留同一任务安排；工位直接展示负责人和多名协作人员；缺齐套资料任务仍可保存安排。

原生“未保存修改”确认框触发后，浏览器自动化操作超时；取消该原生框、工位临时调整的后续点击操作及窄屏滚动操作待补验。随后使用直接链接继续只读检查和截图，未把无响应点击计为通过。人员调整保留时间等字段已经由真实接口回归验证。该工具限制与产品业务验收分开记录。

截图源目录：`/private/tmp/kferp-pr655-evidence/`：`01-operation-staff.png`、`02-schedule-list.png`、`03-batch-review.png`、`04-conflict.png`、`05-workstation-staff.png`、`06-narrow.png`。390px 窄屏筛选默认折叠，首屏先展示任务；桌面保留完整筛选区。截图为隔离数据实际页面，非设计示意图。

## 发布

独立分支 `codex/production-scheduling-staff-20260912`，基于并整合 `origin/develop`。部署前完整检查后推送功能分支，再合入 develop，开发发布使用仓库发布脚本。生产发布不在本次范围内。

首次开发发布已完成：`6df328eb203ef124043c4e06456d2f31c67c5a21`；功能分支推送提交 `cc0d3f29`。本记录的后续集成仅回写验收记录和 PR/DEV 状态。

- 发布：`KFERP_SKIP_MINIAPP_EXPORT=1 ./deploy_orderapp.sh development`，服务端 Vue 1203 项、Vite、miniapp 类型/产物验证、Go 全包及 Docker 构建通过；日志确认 `Release completed`。未上传或发布小程序。
- 回滚源：`/opt/stacks/erp/orderapp.backup.deploy-20260912212752-6df328eb203e`；镜像：`kferp-orderapp-rollback:development-20260912212752-6df328eb203e`。
- 发布后只读：认证 shell 200；排程候选配置 8 道工序、26 名启用员工、11 个产能档；待安排 15 项，包含服务端总数/分页和人员字段，已完成/已取消混入数为 0。入口未认证跳转登录 303，单独核对 API 鉴权结果见交付日志。
- `erp_orderapp` 运行且重启计数 0；PostgreSQL 正常运行，原有重启计数 44，本次未重启数据库。新启动应用日志无错误。
- PR-655 和 DEV-655-DELIVERY 保持待验收，明确原生离开提示及后续点击复验限制；其余三个开发项已完成。业务验收由 Van 完成，未把自动检查或容器运行当作业务验收。
