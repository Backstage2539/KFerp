# 用户权限与账号用户手册

## 操作要点

| 区域 | 操作 |
| --- | --- |
| 角色 | 在用户权限页为员工勾选角色并保存，菜单和 API 会按角色权限控制。 |
| 登录状态 | 在“账号”列切换可登录/已停用；停用后员工不能再登录，已有 Bearer 会话也不能继续访问。 |
| 设置/重置密码 | 员工未设置密码时按钮显示“设置密码”；已有密码时显示“重置密码”。输入密码后点击对应按钮；密码至少 6 位，保存后账号自动恢复为可登录。 |
| 用户名/手机号登录 | 在 `/login` 输入用户名或手机号 + 密码，登录成功后系统会自动进入 Vue 工作台，并按默认菜单展开功能入口；短信登录仍只支持手机号。 |
| 退出登录 | 在系统顶部点击“退出”，系统会注销当前登录会话、清除本机 token，并回到登录页；退出后直接打开系统地址也会回到登录页。 |
| 审计 | 账号启停、密码设置/重置和角色变更都会进入操作日志，便于追踪。 |

## 验收检查

- 管理员能为员工启用或停用登录。
- 停用员工使用密码或短信登录会被拒绝。
- 停用员工已有登录 token 不能继续解析为有效员工。
- 未设置密码的员工显示“设置密码”，已有密码的员工显示“重置密码”；管理员设置或重置后员工可用新密码登录。
- 用户名或手机号密码登录成功后可直接进入系统并正常访问 API，首屏能直接看到默认展开的功能菜单。
- admin 角色账号登录后显示全权限菜单。
- 点击“退出”后返回登录页，当前会话不再继续使用；没有本机 token 时不能靠浏览器 BasicAuth 缓存重新打开 Vue 工作台。
